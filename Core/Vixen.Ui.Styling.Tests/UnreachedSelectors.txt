# The type selectors the sheets declare that no C# creation call and no `.vxml` element writes.
#
#     <name>  <#issue>  <reason>
#
# Held against the measured set by `TypeSelectorReachTests.Every_type_selector_names_a_tag_the_repository_writes`,
# EXACTLY and in both directions. A name that starts being created must leave this file; a name that
# stops being created cannot arrive without a row. A floor — "no more than six" — would let a row
# outlive the rule it excuses and the next dead selector take the vacated seat, which is the guard
# this repository has twice had eaten by success.
#
# ⚠ A row here is a finding and not a shrug. Every one of these is a rule that fires on nothing, and
# the reason column says which of the two answers it is owed: the rule goes, or the element arrives.
# The one case a row could legitimately be permanent is a tag composed at run time —
# `Add($"row-{kind}")` — which this scan cannot see; there is no such row today.
#
# ⚠ **The file is empty, and an empty census is the finished state rather than a disabled test.**
# It held six names when it was written — `notification-list`, `notification-row`,
# `asset-picker-list`, `prefab-banner`, `model-facts`, `material-facts` — and #693 answered all six
# the same way: every one was a name left behind by a design that had been replaced under different
# tags, not a feature waiting to be built. The rules are gone and each deletion left a ⚠ note in the
# sheet saying what replaced it.
#
# ⚠ So what stops this reading green on the day the scan breaks is not this file. It is
# `The_scan_actually_ran` and `The_mirror_scan_actually_ran`, which refuse a `Declared` or a
# `Written` that has collapsed — an empty census compared against an empty measured set is exactly
# what a broken regex also produces, and only those two can tell the difference.
