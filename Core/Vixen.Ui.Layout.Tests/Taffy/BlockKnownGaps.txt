# Block fixtures in Taffy's corpus that Vixen does not yet get right.
#
# Same rules as KnownGaps.txt one file over, and the same reason for existing: a name here is a
# fixture that LAYS OUT and disagrees with Chrome, which is a different thing from the fixtures
# TaffyStyleMap refuses for a property this store has no field for. Names are given without the
# __{border,content}_box_{ltr,rtl} suffix, so one line covers up to four fixtures.
#
# TaffyBlockConformanceTests asserts both that every failure is listed here AND that the totals are
# exactly the committed counts, so a line cannot quietly widen and a fix cannot quietly land.
#
# 722 of the 884 block fixtures pass, plus 24 of the 28 blockflex. 42 fail and 120 are refused.
#
# ⚠ THE SHAPE OF THIS FILE IS THE RESULT. Every one of the 42 is in LayoutTree.Absolute.cs, and
# NOT ONE is in block formatting. Margin collapsing — the thing that makes this block rather than a
# stretch flex column, and about 180 of the 884 fixtures — is entirely green: sibling collapse,
# parent/first-child, parent/last-child, collapse-through, every one of the twelve things that
# blocks a collapse, and the sign rule where a positive and a negative margin add rather than
# maximise. So are auto margins, the intrinsic-width probe, RTL placement, relative insets,
# align-content over the stack, and percentage margins resolving against the inline size.
#
# Both buckets below are pre-existing engine gaps that block layout merely reaches for the first
# time. Neither is caused by this algorithm and neither is fixable inside it.

# ── aspect-ratio on an ABSOLUTELY POSITIONED box, against a minimum or a maximum ──
#
# ⚠ This is the same bucket KnownGaps.txt already lists under "aspect-ratio against min/max and
# stretch", reached from the other side — four of the five families below appear there verbatim,
# spelled `absolute_aspect_ratio_fill_max_width` and so on, and fail identically for flex parents.
# The ratio is applied once and never re-applied after the cross size is clamped, so the transfer
# goes one way only. `block_absolute_aspect_ratio_fill_max_width`: a leaf with aspect-ratio 2 and
# max-height 20 should be 40x20; Vixen leaves it 360 wide.
#
# The in-flow path does get this right — see LayoutTree.Block.ResolveBlockChildBox, which transfers
# the ratio through the minimums and maximums as well as the preferred size, and the content-based
# automatic minimum below it. That code is deliberately local to the block walk rather than shared:
# the absolute path resolves its sizes against a containing block instead of a parent and would
# need the same rule written again, which belongs with the flex bucket's fix and not with this one.
block_absolute_aspect_ratio_aspect_ratio_overrides_height_of_full_inset
block_absolute_aspect_ratio_fill_max_height
block_absolute_aspect_ratio_fill_max_width
block_absolute_aspect_ratio_fill_min_height
block_absolute_aspect_ratio_fill_min_width

# ── AUTO MARGINS on an absolutely positioned box ──
#
# CSS 2.1 §10.3.7: with `left`, `right` and `width` all resolved and a margin set to `auto`, the
# equation is over-constrained and the slack goes to the auto margin — both of them equally when
# both are auto. `block_absolute_margin_auto_left_with_inset` is the clean case: a 50-wide box in a
# 200-wide block with `left: 10px; right: 20px; margin-left: auto` belongs at x=130, and Vixen puts
# it at 10 because it never expands the margin.
#
# ⚠ Not implemented rather than approximated, and the reason is scope: this is absolute
# positioning's rule, not block formatting's, and LayoutTree.Absolute.cs is shared with flex and
# with Yoga's 534. Yoga does not implement it either, the flex corpus has no fixture for it — the
# `absolute_margin_auto_*` families exist only under `block/` — so there is no oracle here that
# would catch a wrong version of it, which is exactly the situation the corpus README warns about.
# It wants its own change, judged by these 22 fixtures and by WPT's `css/css-position/`.
#
# The IN-FLOW auto margin is implemented and green: `block_margin_auto_left_and_right`,
# `block_margin_auto_multiple_children` and the ten other `block_margin_auto_*` families all pass.
# It is only the out-of-flow one that is missing.
block_absolute_margin_auto_bottom_and_top_with_inset
block_absolute_margin_auto_left_and_right_with_inset
block_absolute_margin_auto_left_child_bigger_than_parent_with_inset
block_absolute_margin_auto_left_fix_right_child_bigger_than_parent_with_inset
block_absolute_margin_auto_left_with_inset
block_absolute_margin_auto_multiple_children_with_inset
block_absolute_margin_auto_right_with_inset
block_absolute_margin_auto_top_with_inset

# ── What is REFUSED rather than failing, and why each is honest ──
#
# 120 block fixtures and 4 blockflex never reach arithmetic. Listed here for the record because a
# refusal is a scoping decision and should be as visible as a failure:
#
#   88  scrollbar-width  — LayoutStyle has no scrollbar width, so a scroll container reserves no
#                          gutter. That is a real omission and it is doc 43 § F3's, not B1's.
#   16  text-align       — inline alignment. Doc 43 § B3.
#    8  display:flow-root — a block box that always establishes a formatting context. One line to
#                          add once there is a reason to spell it; `overflow: hidden` already gets
#                          the same behaviour out of this implementation today.
#    4  align-content: safe end — `safe` alignment has no expression in this store's Align enum.
#    4  float            — doc 43's 84 float fixtures, plus these four. See the remarks on
#                          LayoutTree.Block for where floats would attach and why nothing here
#                          forecloses them.
