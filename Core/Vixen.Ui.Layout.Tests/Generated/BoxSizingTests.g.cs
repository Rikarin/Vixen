// SPDX-FileCopyrightText: Copyright (c) Rikarin
// SPDX-License-Identifier: Apache-2.0
//
// <auto-generated>
//     Translated by Tools/Vixen.YogaTestGen from Yoga's YGBoxSizingTest.cpp, which Yoga in turn
//     generates from an HTML fixture laid out by a real browser. Every number below is
//     Chrome's answer, which is what makes this a conformance suite rather than a
//     regression suite. Do not edit: re-run the tool.
// </auto-generated>

using Xunit;

namespace Vixen.Ui.Layout.Tests.Generated;

public class BoxSizingTests {
    const float Tolerance = 0.0001f;

    [Fact]
    public void Box_sizing_content_box_simple() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(10.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(130.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(130.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(130.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(130.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_simple() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(10.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_percent() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Percent(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Percent(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(4.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(16.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_percent() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Percent(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Percent(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(4.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(16.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_absolute() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Percent(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(12.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(8.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPositionType(rootChild0, PositionType.Absolute);
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_absolute() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Percent(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(12.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(8.0f));
        tree.SetPositionType(rootChild0, PositionType.Absolute);
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_comtaining_block() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(12.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(8.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);

        var rootChild0 = tree.CreateNode();
        tree.SetPositionType(rootChild0, PositionType.Static);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Percent(25.0f));
        tree.SetPositionType(rootChild0Child0, PositionType.Absolute);
        tree.InsertChild(rootChild0, rootChild0Child0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(140.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(140.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(20.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(31.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(140.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(140.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(20.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(31.0f, tree.GetHeight(rootChild0Child0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_comtaining_block() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(12.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(8.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetPositionType(rootChild0, PositionType.Static);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Percent(25.0f));
        tree.SetPositionType(rootChild0Child0, PositionType.Absolute);
        tree.InsertChild(rootChild0, rootChild0Child0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(20.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(60.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(21.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(20.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(60.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(21.0f, tree.GetHeight(rootChild0Child0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_padding_only() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(5.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(110.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(110.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(110.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(110.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_padding_only_percent() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(150.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(75.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Percent(10.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(150.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(70.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(95.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(150.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(30.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(70.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(95.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_padding_only() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(5.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_padding_only_percent() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(150.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(75.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Percent(10.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(150.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(75.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(150.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(75.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_border_only() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(10.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(120.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(120.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(120.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(120.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_border_only_percent() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Percent(50.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_border_only() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(10.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_border_only_percent() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Percent(50.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_no_padding_no_border() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_no_padding_no_border() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_children() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(10.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild2, 2);

        var rootChild3 = tree.CreateNode();
        tree.SetDimension(rootChild3, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild3, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild3, 3);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(130.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(130.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(15.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(130.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(130.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(90.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(15.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(90.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(90.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(90.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_children() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(10.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild2, 2);

        var rootChild3 = tree.CreateNode();
        tree.SetDimension(rootChild3, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild3, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild3, 3);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(15.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(15.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(15.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_siblings() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetBoxSizing(rootChild1, BoxSizing.ContentBox);
        tree.SetPadding(rootChild1, Edge.All, StyleLength.Points(10.0f));
        tree.SetBorder(rootChild1, Edge.All, StyleLength.Points(10.0f));
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild2, 2);

        var rootChild3 = tree.CreateNode();
        tree.SetDimension(rootChild3, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild3, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild3, 3);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(115.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(35.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(115.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_siblings() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetPadding(rootChild1, Edge.All, StyleLength.Points(10.0f));
        tree.SetBorder(rootChild1, Edge.All, StyleLength.Points(10.0f));
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild2, 2);

        var rootChild3 = tree.CreateNode();
        tree.SetDimension(rootChild3, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild3, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild3, 3);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(60.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild3), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_max_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetMaxDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_max_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetMaxDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_max_height() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetMaxDimension(rootChild0, Dimension.Height, StyleLength.Points(50.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_max_height() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetMaxDimension(rootChild0, Dimension.Height, StyleLength.Points(50.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_min_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetMinDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_min_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetMinDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(40.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_min_height() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetMinDimension(rootChild0, Dimension.Height, StyleLength.Points(50.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(90.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(90.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_min_height() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(50.0f));
        tree.SetMinDimension(rootChild0, Dimension.Height, StyleLength.Points(50.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(15.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(25.0f));
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(50.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(75.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(50.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_no_height_no_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(2.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(7.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_no_height_no_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(2.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(7.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_nested() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.SetPadding(root, Edge.All, StyleLength.Points(15.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(3.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(20.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(2.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(7.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Points(5.0f));
        tree.SetBoxSizing(rootChild0Child0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0Child0, Edge.All, StyleLength.Points(1.0f));
        tree.SetBorder(rootChild0Child0, Edge.All, StyleLength.Points(2.0f));
        tree.InsertChild(rootChild0, rootChild0Child0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(136.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(136.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(18.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(38.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(38.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(9.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(16.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(11.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(136.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(136.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(80.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(38.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(38.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(13.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(16.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(11.0f, tree.GetHeight(rootChild0Child0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_nested() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(15.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(3.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(20.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(2.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(7.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Points(5.0f));
        tree.SetPadding(rootChild0Child0, Edge.All, StyleLength.Points(1.0f));
        tree.SetBorder(rootChild0Child0, Edge.All, StyleLength.Points(2.0f));
        tree.InsertChild(rootChild0, rootChild0Child0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(18.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(9.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(6.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(62.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(18.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(20.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(1.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(6.0f, tree.GetHeight(rootChild0Child0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_nested_alternating() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.SetPadding(root, Edge.All, StyleLength.Points(3.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(2.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(40.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(40.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(8.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(2.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0, Dimension.Width, StyleLength.Points(20.0f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetBoxSizing(rootChild0Child0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0Child0, Edge.All, StyleLength.Points(3.0f));
        tree.SetBorder(rootChild0Child0, Edge.All, StyleLength.Points(6.0f));
        tree.InsertChild(rootChild0, rootChild0Child0, 0);

        var rootChild0Child0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0Child0, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetDimension(rootChild0Child0Child0, Dimension.Height, StyleLength.Points(5.0f));
        tree.SetPadding(rootChild0Child0Child0, Edge.All, StyleLength.Points(1.0f));
        tree.SetBorder(rootChild0Child0Child0, Edge.All, StyleLength.Points(1.0f));
        tree.InsertChild(rootChild0Child0, rootChild0Child0Child0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(110.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(110.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(5.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(5.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(38.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(43.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        Assert.Equal(9.0f, tree.GetLeft(rootChild0Child0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild0Child0Child0), Tolerance);
        Assert.Equal(5.0f, tree.GetHeight(rootChild0Child0Child0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(110.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(110.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(65.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(5.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(40.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(-8.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(38.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(43.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        Assert.Equal(19.0f, tree.GetLeft(rootChild0Child0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild0Child0Child0), Tolerance);
        Assert.Equal(5.0f, tree.GetHeight(rootChild0Child0Child0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_nested_alternating() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.All, StyleLength.Points(3.0f));
        tree.SetBorder(root, Edge.All, StyleLength.Points(2.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(40.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(40.0f));
        tree.SetBoxSizing(rootChild0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(8.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(2.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0, Dimension.Width, StyleLength.Points(20.0f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetPadding(rootChild0Child0, Edge.All, StyleLength.Points(3.0f));
        tree.SetBorder(rootChild0Child0, Edge.All, StyleLength.Points(6.0f));
        tree.InsertChild(rootChild0, rootChild0Child0, 0);

        var rootChild0Child0Child0 = tree.CreateNode();
        tree.SetDimension(rootChild0Child0Child0, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetDimension(rootChild0Child0Child0, Dimension.Height, StyleLength.Points(5.0f));
        tree.SetBoxSizing(rootChild0Child0Child0, BoxSizing.ContentBox);
        tree.SetPadding(rootChild0Child0Child0, Edge.All, StyleLength.Points(1.0f));
        tree.SetBorder(rootChild0Child0Child0, Edge.All, StyleLength.Points(1.0f));
        tree.InsertChild(rootChild0Child0, rootChild0Child0Child0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(5.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(5.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(60.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(60.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(10.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(20.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        Assert.Equal(9.0f, tree.GetLeft(rootChild0Child0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0Child0), Tolerance);
        Assert.Equal(14.0f, tree.GetWidth(rootChild0Child0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetHeight(rootChild0Child0Child0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(35.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(5.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(60.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(60.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(30.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(20.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        Assert.Equal(-3.0f, tree.GetLeft(rootChild0Child0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetTop(rootChild0Child0Child0), Tolerance);
        Assert.Equal(14.0f, tree.GetWidth(rootChild0Child0Child0), Tolerance);
        Assert.Equal(9.0f, tree.GetHeight(rootChild0Child0Child0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_flex_basis_row() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetFlexDirection(root, FlexDirection.Row);

        var rootChild0 = tree.CreateNode();
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(10.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(30.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(30.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_flex_basis_column() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(25.0f));
        tree.SetPadding(rootChild0, Edge.All, StyleLength.Points(5.0f));
        tree.SetBorder(rootChild0, Edge.All, StyleLength.Points(10.0f));
        tree.InsertChild(root, rootChild0, 0);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetHeight(rootChild0), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(50.0f, tree.GetHeight(rootChild0), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_padding_start() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.Start, StyleLength.Points(5.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_padding_start() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.Start, StyleLength.Points(5.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_padding_end() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.End, StyleLength.Points(5.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_padding_end() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetPadding(root, Edge.End, StyleLength.Points(5.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_border_start() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBorder(root, Edge.Start, StyleLength.Points(5.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_border_start() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBorder(root, Edge.Start, StyleLength.Points(5.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_content_box_border_end() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBorder(root, Edge.End, StyleLength.Points(5.0f));
        tree.SetBoxSizing(root, BoxSizing.ContentBox);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(105.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }

    [Fact]
    public void Box_sizing_border_box_border_end() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetBorder(root, Edge.End, StyleLength.Points(5.0f));
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);
    }
}
