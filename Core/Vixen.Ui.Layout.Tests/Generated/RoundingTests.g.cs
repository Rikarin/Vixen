// SPDX-FileCopyrightText: Copyright (c) Rikarin
// SPDX-License-Identifier: Apache-2.0
//
// <auto-generated>
//     Translated by Tools/Vixen.YogaTestGen from Yoga's YGRoundingTest.cpp, which Yoga in turn
//     generates from an HTML fixture laid out by a real browser. Every number below is
//     Chrome's answer, which is what makes this a conformance suite rather than a
//     regression suite. Do not edit: re-run the tool.
// </auto-generated>

using Xunit;

namespace Vixen.Ui.Layout.Tests.Generated;

public class RoundingTests {
    const float Tolerance = 0.0001f;

    [Fact]
    public void Rounding_flex_basis_flex_grow_row_width_of_100() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetFlexDirection(root, FlexDirection.Row);

        var rootChild0 = tree.CreateNode();
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(33.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(33.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(34.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(67.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(33.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(67.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(33.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(33.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(34.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(33.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_flex_basis_flex_grow_row_prime_number_width() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(113.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetFlexDirection(root, FlexDirection.Row);

        var rootChild0 = tree.CreateNode();
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);

        var rootChild3 = tree.CreateNode();
        tree.SetFlexGrow(rootChild3, 1.0f);
        tree.InsertChild(root, rootChild3, 3);

        var rootChild4 = tree.CreateNode();
        tree.SetFlexGrow(rootChild4, 1.0f);
        tree.InsertChild(root, rootChild4, 4);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(113.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(23.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(23.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(22.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(45.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(23.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(68.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(22.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild3), Tolerance);

        Assert.Equal(90.0f, tree.GetLeft(rootChild4), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild4), Tolerance);
        Assert.Equal(23.0f, tree.GetWidth(rootChild4), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild4), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(113.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(90.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(23.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(68.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(22.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(45.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(23.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild2), Tolerance);

        Assert.Equal(23.0f, tree.GetLeft(rootChild3), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild3), Tolerance);
        Assert.Equal(22.0f, tree.GetWidth(rootChild3), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild3), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild4), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild4), Tolerance);
        Assert.Equal(23.0f, tree.GetWidth(rootChild4), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild4), Tolerance);
    }

    [Fact]
    public void Rounding_flex_basis_flex_shrink_row() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(101.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(100.0f));
        tree.SetFlexDirection(root, FlexDirection.Row);

        var rootChild0 = tree.CreateNode();
        tree.SetFlexBasis(rootChild0, StyleLength.Points(100.0f));
        tree.SetFlexShrink(rootChild0, 1.0f);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetFlexBasis(rootChild1, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetFlexBasis(rootChild2, StyleLength.Points(25.0f));
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(101.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(51.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(51.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(76.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(101.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(50.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(51.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(25.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_flex_basis_overrides_main_size() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.0f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(64.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(64.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_total_fractial() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.4f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(87.4f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.3f));
        tree.SetFlexGrow(rootChild0, 0.7f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.3f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.6f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.7f));
        tree.SetFlexGrow(rootChild2, 1.1f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(59.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(59.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(30.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(59.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(59.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(30.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_total_fractial_nested() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.4f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(87.4f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.3f));
        tree.SetFlexGrow(rootChild0, 0.7f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.3f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild0Child0 = tree.CreateNode();
        tree.SetPosition(rootChild0Child0, Edge.Bottom, StyleLength.Points(13.3f));
        tree.SetDimension(rootChild0Child0, Dimension.Height, StyleLength.Points(9.9f));
        tree.SetFlexGrow(rootChild0Child0, 1.0f);
        tree.SetFlexBasis(rootChild0Child0, StyleLength.Points(0.3f));
        tree.InsertChild(rootChild0, rootChild0Child0, 0);

        var rootChild0Child1 = tree.CreateNode();
        tree.SetPosition(rootChild0Child1, Edge.Top, StyleLength.Points(13.3f));
        tree.SetDimension(rootChild0Child1, Dimension.Height, StyleLength.Points(1.1f));
        tree.SetFlexGrow(rootChild0Child1, 4.0f);
        tree.SetFlexBasis(rootChild0Child1, StyleLength.Points(0.3f));
        tree.InsertChild(rootChild0, rootChild0Child1, 1);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.6f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.7f));
        tree.SetFlexGrow(rootChild2, 1.1f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(59.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(-13.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(12.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0Child1), Tolerance);
        Assert.Equal(25.0f, tree.GetTop(rootChild0Child1), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0Child1), Tolerance);
        Assert.Equal(47.0f, tree.GetHeight(rootChild0Child1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(59.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(30.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(59.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0Child0), Tolerance);
        Assert.Equal(-13.0f, tree.GetTop(rootChild0Child0), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0Child0), Tolerance);
        Assert.Equal(12.0f, tree.GetHeight(rootChild0Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0Child1), Tolerance);
        Assert.Equal(25.0f, tree.GetTop(rootChild0Child1), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild0Child1), Tolerance);
        Assert.Equal(47.0f, tree.GetHeight(rootChild0Child1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(59.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(30.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(87.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_fractial_input_1() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.4f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(64.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(64.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_fractial_input_2() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.6f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(114.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(114.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(65.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_fractial_input_3() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetPosition(root, Edge.Top, StyleLength.Points(0.3f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.4f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(114.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(114.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(65.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_fractial_input_4() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetPosition(root, Edge.Top, StyleLength.Points(0.7f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(113.4f));
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(100.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(20.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.SetFlexBasis(rootChild0, StyleLength.Points(50.0f));
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(1.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(64.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(1.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(113.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(64.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(64.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(25.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(89.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(100.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(24.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_inner_node_controversy_horizontal() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(320.0f));
        tree.SetFlexDirection(root, FlexDirection.Row);

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild1Child0 = tree.CreateNode();
        tree.SetDimension(rootChild1Child0, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1Child0, 1.0f);
        tree.InsertChild(rootChild1, rootChild1Child0, 0);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(320.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(107.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(107.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(106.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child0), Tolerance);
        Assert.Equal(106.0f, tree.GetWidth(rootChild1Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild1Child0), Tolerance);

        Assert.Equal(213.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(107.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(320.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(213.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(107.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(107.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(106.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child0), Tolerance);
        Assert.Equal(106.0f, tree.GetWidth(rootChild1Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(107.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(10.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_inner_node_controversy_vertical() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(320.0f));

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild1Child0 = tree.CreateNode();
        tree.SetDimension(rootChild1Child0, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild1Child0, 1.0f);
        tree.InsertChild(rootChild1, rootChild1Child0, 0);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Width, StyleLength.Points(10.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(107.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild1Child0), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(213.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(107.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child0), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild1Child0), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(213.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(10.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild2), Tolerance);
    }

    [Fact]
    public void Rounding_inner_node_controversy_combined() {
        using var tree = new LayoutTree();

        var root = tree.CreateNode();
        tree.SetPositionType(root, PositionType.Absolute);
        tree.SetDimension(root, Dimension.Width, StyleLength.Points(640.0f));
        tree.SetDimension(root, Dimension.Height, StyleLength.Points(320.0f));
        tree.SetFlexDirection(root, FlexDirection.Row);

        var rootChild0 = tree.CreateNode();
        tree.SetDimension(rootChild0, Dimension.Height, StyleLength.Percent(100.0f));
        tree.SetFlexGrow(rootChild0, 1.0f);
        tree.InsertChild(root, rootChild0, 0);

        var rootChild1 = tree.CreateNode();
        tree.SetDimension(rootChild1, Dimension.Height, StyleLength.Percent(100.0f));
        tree.SetFlexGrow(rootChild1, 1.0f);
        tree.InsertChild(root, rootChild1, 1);

        var rootChild1Child0 = tree.CreateNode();
        tree.SetDimension(rootChild1Child0, Dimension.Width, StyleLength.Percent(100.0f));
        tree.SetFlexGrow(rootChild1Child0, 1.0f);
        tree.InsertChild(rootChild1, rootChild1Child0, 0);

        var rootChild1Child1 = tree.CreateNode();
        tree.SetDimension(rootChild1Child1, Dimension.Width, StyleLength.Percent(100.0f));
        tree.SetFlexGrow(rootChild1Child1, 1.0f);
        tree.InsertChild(rootChild1, rootChild1Child1, 1);

        var rootChild1Child1Child0 = tree.CreateNode();
        tree.SetFlexGrow(rootChild1Child1Child0, 1.0f);
        tree.SetDimension(rootChild1Child1Child0, Dimension.Width, StyleLength.Percent(100.0f));
        tree.InsertChild(rootChild1Child1, rootChild1Child1Child0, 0);

        var rootChild1Child2 = tree.CreateNode();
        tree.SetDimension(rootChild1Child2, Dimension.Width, StyleLength.Percent(100.0f));
        tree.SetFlexGrow(rootChild1Child2, 1.0f);
        tree.InsertChild(rootChild1, rootChild1Child2, 2);

        var rootChild2 = tree.CreateNode();
        tree.SetDimension(rootChild2, Dimension.Height, StyleLength.Percent(100.0f));
        tree.SetFlexGrow(rootChild2, 1.0f);
        tree.InsertChild(root, rootChild2, 2);
        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Ltr);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(640.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(213.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(213.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child0), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child0), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child1), Tolerance);
        Assert.Equal(107.0f, tree.GetTop(rootChild1Child1), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child1), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1Child1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child1Child0), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child1Child0), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1Child1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child2), Tolerance);
        Assert.Equal(213.0f, tree.GetTop(rootChild1Child2), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child2), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild1Child2), Tolerance);

        Assert.Equal(427.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(213.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(rootChild2), Tolerance);

        tree.CalculateLayout(root, float.NaN, float.NaN, Direction.Rtl);

        Assert.Equal(0.0f, tree.GetLeft(root), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(root), Tolerance);
        Assert.Equal(640.0f, tree.GetWidth(root), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(root), Tolerance);

        Assert.Equal(427.0f, tree.GetLeft(rootChild0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild0), Tolerance);
        Assert.Equal(213.0f, tree.GetWidth(rootChild0), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(rootChild0), Tolerance);

        Assert.Equal(213.0f, tree.GetLeft(rootChild1), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(rootChild1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child0), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child0), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child1), Tolerance);
        Assert.Equal(107.0f, tree.GetTop(rootChild1Child1), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child1), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1Child1), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child1Child0), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild1Child1Child0), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child1Child0), Tolerance);
        Assert.Equal(106.0f, tree.GetHeight(rootChild1Child1Child0), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild1Child2), Tolerance);
        Assert.Equal(213.0f, tree.GetTop(rootChild1Child2), Tolerance);
        Assert.Equal(214.0f, tree.GetWidth(rootChild1Child2), Tolerance);
        Assert.Equal(107.0f, tree.GetHeight(rootChild1Child2), Tolerance);

        Assert.Equal(0.0f, tree.GetLeft(rootChild2), Tolerance);
        Assert.Equal(0.0f, tree.GetTop(rootChild2), Tolerance);
        Assert.Equal(213.0f, tree.GetWidth(rootChild2), Tolerance);
        Assert.Equal(320.0f, tree.GetHeight(rootChild2), Tolerance);
    }
}
