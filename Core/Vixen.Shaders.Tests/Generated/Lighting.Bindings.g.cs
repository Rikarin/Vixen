// <auto-generated/>
// Generated by Vixen.Shaders.Generators from Lighting.reflect.json.
// Offsets are Raven's own, copied rather than recomputed. Do not edit.
#nullable enable

using Vixen.Shaders;

namespace Vixen.Shaders.Generated;

/// <summary>
///     Typed keys for shader <c>Lighting</c>.
/// </summary>
public static class LightingKeys {
    /// <summary>The shader's name, as the effect system asks for it.</summary>
    public const string ShaderName = "Lighting";

    // --- Permutation keys: these select a variant, and changing one recompiles.

    /// <summary>Affects the generated code, so a distinct value is a distinct variant.</summary>
    public static readonly PermutationKey<bool> UseShadows = ParameterKeys.NewPermutation<bool>(false, "Lighting.UseShadows");

    /// <summary>Affects the generated code, so a distinct value is a distinct variant.</summary>
    public static readonly PermutationKey<int> MaxLights = ParameterKeys.NewPermutation<int>(4, "Lighting.MaxLights");

    /// <summary>Declared but unused: setting it produces the same shader.</summary>
    public static readonly PermutationKey<bool> Unused = ParameterKeys.NewPermutation<bool>(true, "Lighting.Unused");

    /// <summary>The permutation keys that actually change the generated code.</summary>
    public static readonly ParameterKey[] UsedPermutationKeys = [
        UseShadows,
        MaxLights,
    ];

    // --- Resource keys: what the descriptor set is filled from, and where each one goes.

    /// <summary>set 2, binding 1.</summary>
    public static readonly ParameterKey<global::Vixen.Graphics.TextureViewHandle> Albedo = ParameterKeys.New<global::Vixen.Graphics.TextureViewHandle>("Lighting.albedo");

    /// <summary>Which descriptor set holds <c>albedo</c>.</summary>
    public const int AlbedoSet = 2;

    /// <summary>Which binding within set 2 <c>albedo</c> occupies.</summary>
    public const uint AlbedoBinding = 1;

    /// <summary>set 2, binding 2.</summary>
    public static readonly ParameterKey<global::Vixen.Graphics.SamplerHandle> Linear = ParameterKeys.New<global::Vixen.Graphics.SamplerHandle>("Lighting.linear");

    /// <summary>Which descriptor set holds <c>linear</c>.</summary>
    public const int LinearSet = 2;

    /// <summary>Which binding within set 2 <c>linear</c> occupies.</summary>
    public const uint LinearBinding = 2;

    /// <summary>set 2, binding 3.</summary>
    public static readonly ParameterKey<global::Vixen.Graphics.BufferHandle> Overflow = ParameterKeys.New<global::Vixen.Graphics.BufferHandle>("Lighting.overflow");

    /// <summary>Which descriptor set holds <c>overflow</c>.</summary>
    public const int OverflowSet = 2;

    /// <summary>Which binding within set 2 <c>overflow</c> occupies.</summary>
    public const uint OverflowBinding = 3;

    // --- Vertex attributes: where the vertex stage expects each one. See the remarks above.

    /// <summary>How many attributes the vertex stage takes from a buffer.</summary>
    public const int VertexAttributeCount = 2;

    /// <summary>Which location the vertex stage reads <c>position</c> from.</summary>
    public const uint PositionLocation = 1;

    /// <summary>Which location the vertex stage reads <c>texcoord</c> from.</summary>
    public const uint TexcoordLocation = 2;

    // --- Value keys: what a name-driven caller fills the block through.

    /// <summary><c>worldViewProjection</c> at byte 0.</summary>
    public static readonly ParameterKey<global::Vixen.Core.Mathematics.Matrix4x4> WorldViewProjection = ParameterKeys.New<global::Vixen.Core.Mathematics.Matrix4x4>("Lighting.worldViewProjection");

    /// <summary><c>normalMatrix</c> at byte 64.</summary>
    public static readonly ParameterKey<global::Vixen.Core.Mathematics.Matrix3x3> NormalMatrix = ParameterKeys.New<global::Vixen.Core.Mathematics.Matrix3x3>("Lighting.normalMatrix");

    /// <summary><c>ambient</c> at byte 112.</summary>
    public static readonly ParameterKey<global::Vixen.Core.Mathematics.Vector3> Ambient = ParameterKeys.New<global::Vixen.Core.Mathematics.Vector3>("Lighting.ambient");

    /// <summary><c>exposure</c> at byte 124, declared <c>1</c>.</summary>
    public static readonly ParameterKey<float> Exposure = ParameterKeys.New<float>("Lighting.exposure", 1f);

    /// <summary><c>lightCount</c> at byte 128, declared <c>2</c>.</summary>
    public static readonly ParameterKey<int> LightCount = ParameterKeys.New<int>("Lighting.lightCount", 2);

    /// <summary><c>enabled</c> at byte 132, declared <c>true</c>.</summary>
    public static readonly ParameterKey<bool> Enabled = ParameterKeys.New<bool>("Lighting.enabled", true);

    /// <summary>The uniform block's size in bytes — what to allocate.</summary>
    public const int ConstantBufferSize = 336;

    /// <summary>Which descriptor set holds <c>the uniform block</c>.</summary>
    public const int ConstantBufferSet = 2;

    /// <summary>Which binding within set 2 <c>the uniform block</c> occupies.</summary>
    public const uint ConstantBufferBinding = 0;
}

/// <summary>
///     One element of <c>Lighting.lights</c>.
/// </summary>
public struct LightingLightsElement {

    /// <summary><c>lights[].position</c> — 12 bytes at 0 into the element.</summary>
    public global::Vixen.Core.Mathematics.Vector3 Position;

    /// <summary><c>lights[].range</c> — 4 bytes at 12 into the element.</summary>
    public float Range;

    /// <summary><c>lights[].color</c> — 12 bytes at 16 into the element.</summary>
    public global::Vixen.Core.Mathematics.Vector3 Color;

    /// <summary><c>lights[].intensity</c> — 4 bytes at 28 into the element.</summary>
    public float Intensity;

    /// <summary>How many elements the shader declared room for.</summary>
    public const int Count = 4;

    /// <summary>Bytes from one element to the next.</summary>
    public const int Stride = 32;

    /// <summary>Byte offset of element zero within the block.</summary>
    public const int BaseOffset = 208;

    /// <summary>Writes this element into slot <paramref name="index"/>.</summary>
    /// <exception cref="System.ArgumentOutOfRangeException">The slot is outside the declared array.</exception>
    public readonly void Write(System.Span<byte> buffer, int index) {
        System.ArgumentOutOfRangeException.ThrowIfNegative(index);
        System.ArgumentOutOfRangeException.ThrowIfGreaterThanOrEqual(index, Count);

        var at = BaseOffset + index * Stride;

        // lights[].position, 0 bytes into the element.
        ShaderConstants.Write(buffer, at + 0, in Position);

        // lights[].range, 12 bytes into the element.
        ShaderConstants.Write(buffer, at + 12, Range);

        // lights[].color, 16 bytes into the element.
        ShaderConstants.Write(buffer, at + 16, in Color);

        // lights[].intensity, 28 bytes into the element.
        ShaderConstants.Write(buffer, at + 28, Intensity);
    }
}

/// <summary>
///     The values of <c>Lighting</c>'s uniform block, in C# types.
/// </summary>
/// <remarks>
///     Field order follows the shader's declaration order, not the block's byte order — the
///     two differ wherever std140 inserted padding, and the writer knows the difference.
/// </remarks>
public struct LightingConstants {

    /// <summary><c>worldViewProjection</c> at byte 0.</summary>
    public global::Vixen.Core.Mathematics.Matrix4x4 WorldViewProjection;

    /// <summary><c>normalMatrix</c> at byte 64.</summary>
    public global::Vixen.Core.Mathematics.Matrix3x3 NormalMatrix;

    /// <summary><c>ambient</c> at byte 112.</summary>
    public global::Vixen.Core.Mathematics.Vector3 Ambient;

    /// <summary><c>exposure</c> at byte 124.</summary>
    public float Exposure;

    /// <summary><c>lightCount</c> at byte 128.</summary>
    public int LightCount;

    /// <summary><c>enabled</c> at byte 132.</summary>
    public bool Enabled;

    /// <summary><c>weights</c> at byte 144.</summary>
    public float[]? Weights;

    /// <summary>The block's size in bytes.</summary>
    public const int Size = 336;

    /// <summary>Writes every value into <paramref name="buffer"/> at its baked offset.</summary>
    /// <exception cref="System.ArgumentException">The buffer is shorter than <see cref="Size"/>.</exception>
    public readonly void Write(System.Span<byte> buffer) {
        if (buffer.Length < Size) {
            throw new System.ArgumentException(
                $"A LightingConstants block needs {Size} bytes; this one has {buffer.Length}.",
                nameof(buffer)
            );
        }

        // worldViewProjection, 64 bytes.
        ShaderConstants.Write(buffer, 0, in WorldViewProjection);

        // normalMatrix, 48 bytes.
        ShaderConstants.Write(buffer, 64, in NormalMatrix, 16);

        // ambient, 12 bytes.
        ShaderConstants.Write(buffer, 112, in Ambient);

        // exposure, 4 bytes.
        ShaderConstants.Write(buffer, 124, Exposure);

        // lightCount, 4 bytes.
        ShaderConstants.Write(buffer, 128, LightCount);

        // enabled, 4 bytes.
        ShaderConstants.Write(buffer, 132, Enabled);

        // weights: 4 × 16 bytes apart.
        if (Weights is not null) {
            ShaderConstants.WriteArray<float>(buffer, 144, 16, 4, Weights);
        }
    }
}
