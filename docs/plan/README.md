# Vixen — Implementation Plan

Vixen is a .NET 10 / C# 14 game engine **and** application framework: the same stack that ships a
shipped game also ships Photoshop- or Blender-class desktop tooling. The editor is written in the
engine, using the engine's own UI framework, and is the primary proof that the framework is
general-purpose.

This directory is the authoritative design record: what Vixen is meant to be, and why each decision
was taken. Read 00–02 first; after that, treat each file as the spec for its subsystem.

**These documents do not say what is built.** [`../overview.md`](../overview.md) does, and it is
checked against the code — so where it and a document here disagree, it says so and it wins. Keeping
the two apart is what lets a design record stay useful for its reasoning without also having to be a
status board.

Documents 18 and above amend or extend an earlier one rather than opening new ground; each says which,
and the document it amends points back. **A ⚠️ means the amendment changes a decision, so read the pair
together; a ✅ means it has since been carried out.**

| # | Document | Scope |
|---|---|---|
| 00 | [Vision and Principles](00-vision-and-principles.md) | Non-negotiables, quality bars, what Vixen is *not* |
| 01 | [Technology Decisions](01-technology-decisions.md) | Every dependency, pinned version, and the ADR register |
| 02 | [Repository Layout](02-repository-layout.md) | Folder tree, project graph, naming, monorepo migration |
| 03 | [Core Foundation](03-core-foundation.md) | Math, memory, collections, jobs, VFS, serialization, reflection |
| 04 | [ECS and Scripting](04-ecs-and-scripting.md) | Archetype ECS, `Behavior` layer, system scheduler, transforms |
| 05 | [Graphics RHI](05-graphics-rhi.md) | Backend-agnostic device layer over Silk.NET; Vulkan/D3D12/GL/WebGPU |
| 06 | [Rendering Pipeline](06-rendering-pipeline.md) | Render features/stages, forward+, deferred, PBR, shadows, post FX |
| 07 | [Raven Shader Pipeline](07-raven-shader-pipeline.md) | Raven → SPIR-V → all targets, effects, permutations, generated bindings |
| 08 | [Asset Pipeline and Addressables](08-asset-pipeline-and-addressables.md) | `.meta` sidecars, importers, ODB, bundles, remote content |
| 09 | [UI Framework](09-ui-framework.md) | VXML markup, signals, flexbox, VCSS + utility preprocessor, hot reload |
| 10 | [Platforms](10-platforms.md) | Windows, Linux, macOS, Android, iOS, Web — per-target reality check |
| 11 | [Editor](11-editor.md) | Editor architecture, docking, inspectors, node graphs (shader + VFX) |
| 12 | [Build, CI and Testing](12-build-ci-and-testing.md) | Nuke, GitHub Actions, NuGet layout, test strategy and gates |
| 13 | [Diagnostics](13-diagnostics.md) | Logging, profiling, tracing, debug rendering, remote inspector |
| 14 | [Roadmap](14-roadmap.md) | Phases, exit criteria, sequencing, effort |
| 15 | [Risks and Open Questions](15-risks-and-open-questions.md) | Ranked risks, mitigations, decisions that need your input |
| 16 | [Networking](16-networking.md) | Transports, tick, replication, interest management, lag compensation, security |
| 17 | [App Heads and Shipping](17-app-heads-and-shipping.md) | What a shipped game *is*, build variants, dedicated server, play-mode topology, trimming policy |
| 18 | [Raven Parser Migration](18-raven-parser-migration.md) | ✅ Amends ADR-009 — why ANTLR gave way to a hand-written parser, and why the `.g4` files were kept as a differential oracle |
| 19 | [Lighting and Global Illumination](19-lighting-and-global-illumination.md) | ⚠️ Amends 06 — retires baked lightmaps and tetrahedral probes for a Lumen-shaped dynamic path: distance fields, an irradiance field, screen probes, surface cache |
| 20 | [Editor Parity](20-editor-parity.md) | ⚠️ Extends 11 — the editor's *surface*: every panel, window, menu line and verb an Unreal or Unity user reaches for, with the milestones to build them |
| 21 | [Realtime Collaboration](21-realtime-collaboration.md) | ⚠️ Extends 11 and 20 — what Unreal's Multi-User Editing is, why intent replicates better than diffs, and what the five milestones cost. Post-1.0 except the first |
| 22 | [Virtualized Geometry](22-virtualized-geometry.md) | ⚠️ Extends 06 — a Nanite-class pipeline: a cluster DAG built offline, streamed pages, hierarchical culling on the device |
| 23 | [Bindless Materials](23-bindless-materials.md) | ⚠️ Extends 05 and 06 — one descriptor array for the frame, so a draw is an index rather than a set; what compacted draws and GPU-driven submission were waiting for |
| 24 | [Blockout Tools](24-blockout-tools.md) | ⚠️ Extends 11 and 20 — in-viewport grey-boxing: a mesh kernel that survives editing, sub-object selection, the fifteen verbs, snapping, and the handoff to an artist |
| 25 | [Documentation Generator and Site](25-documentation-generator-and-site.md) | ⚠️ Amends 02 and 12 — DocFX gives way to a Roslyn generator that classifies what the engine actually offers (components, systems, shaders, annotations), and a versioned, searchable site built on xUI and served from a container image. Coverage and examples become build gates |
| 26 | [Virtual Cameras](26-virtual-cameras.md) | ✅ Extends 04 — a Cinemachine-shaped camera system: many authored shots, composable body and aim stages, a director that picks one on priority and blends, noise and impulse |
| 27 | [MMO Framework](27-mmo-framework.md) | ⚠️ Extends 16 and 17 — the substrate: an Orleans orchestrator that places, scales and upgrades map instances; realms as headless heads; the three network planes and why real-time traffic never meets a gateway; seamless transfer by overlapping sessions and a lease |
| 28 | [Gameplay Framework](28-gameplay-framework.md) | ⚠️ Extends 27, 08 and 16 — the opinionated library set on top: tags, definitions and the modifier algebra, then items, combat, quests, guilds, economy, matchmaking and the rest, so a new item is a content build rather than a release |
| 29 | [Players and Possession](29-players-and-possession.md) | ⚠️ Extends 04, 26 and 16 — who the player *is*: a controller that outlives its pawn, possession as a runtime edge, and `MoveIntent` as the one seam between input, physics and the wire |
| 30 | [Post-processing Parity](30-post-processing-parity.md) | ⚠️ Extends 06 — the audit against Unreal 5.8 and HDRP 14: every effect either a node a `.vxcompositor` can name, or a reason below the table. Seventeen of them, plus the motion-vector pass they were waiting on |
| 31 | [Terrain, Grass and Trees](31-terrain-grass-and-trees.md) | ⚠️ Extends 06 and 20 — the outdoor toolset: a heightfield stored as non-destructive edit layers, a quadtree with a vertex morph rather than a clipmap, a splat material generated from the layer list, grass derived on the GPU and never stored, trees stored and culled per instance, and two editor modes. Reopens 20 § Part G |
| 32 | [Post-process Volumes](32-post-process-volumes.md) | ⚠️ Extends 30 — where a look applies rather than which effects exist: an optional-field settings struct, a box with a priority and a blend radius, and a per-frame fold over the camera's position. ⚠ The lens stays on the camera, unlike Unreal, whose volumes can override a cine camera's f-stop |
| 33 | [Character Creator](33-character-creator.md) | ⚠️ Extends 06, 08, 16, 20 and 28 — a MetaHuman-shaped digital-human toolset: a character is a parameter vector over a fitted statistical model, a face rig is a compiled sparse program, wardrobe items conform to the body they are worn on, and the whole thing bakes down to ordinary meshes. Draws 20 § Part G's modelling line more precisely without reopening it, and is honest that the model data — not the code — is what decides whether it is usable |
| 34 | [Move Sets and Pose Constraints](34-move-sets-and-pose-constraints.md) | ⚠️ Extends 04, 26 and 29 — two additions to the animation stack, both because the state machine is the wrong shape: a character's movement vocabulary becomes a flat catalogue of clips carrying facets, picked by a scored query rather than a graph walk, so "injured" costs a facet and not a parallel graph; and a clip carries authored spatio-temporal goals — this hand on that surface, this axis at that point — enforced at runtime against the body, prop and ground the game actually has. Closes the `.vxanim` runtime row it depends on |
| 35 | [Water](35-water.md) | ⚠️ Extends 06 and 31, and closes an owed row in 29 — oceans, lakes and rivers as splines carrying a profile, rasterised into one top-down texture per zone that holds surface height, flow and the ground beneath it. The surface mesh is *the terrain's own quadtree* reading a different height source rather than a second one; carving is a third reserved edit layer on machinery that already exists; underwater costs a non-box volume shape and nothing else. The load-bearing decision is one surface evaluator with two hosts — the shader and the CPU — held together by a seam test at a tolerance of exact, because both references evaluate the surface twice and neither pins them together. Reflections route to 19's traced path rather than waiting on SSR |
| 36 | [An Extensible Editor](36-an-extensible-editor.md) | ⚠️ Extends 11 and 20 — the editor has a plugin system, an inspector registry, a drawer registry and a markup language, and also a 3,601-line `EditorApplication.cs` that hard-references twelve feature assemblies and a hardcoded array of the asset kinds Create ▸ offers. Nine measured findings, the deepest being that there is no single edit path — so a plugin cannot take part in undo, markup has nothing to bind to, and every built-in feature was wired in through the back door rather than proving the front door works. Takes Unity's four mechanisms and its one invariant, and moves the built-ins onto the same API a third party uses |
| 37 | [AI — Behaviour Trees, Utility and GOAP](37-ai-behaviour-trees-utility-and-goap.md) | ⚠️ Amends 28 and extends 04, 11, 13 and 20 — the three planners doc 28 named come off the gameplay spine and become `Vixen.Ai` in Core, because a composite that runs children until one fails has no opinion about what an ability is — and once the spine moves to a `Gameplay/` layer of its own, that is a `CheckArchitecture` line rather than a convention. One event-driven behaviour tree on Unreal's model — an immutable template with a pre-order execution index, per-agent state as a byte range, and an abort that is two integer comparisons — plus utility scoring as an axis of one input, one curve and four parameters, and GOAP as a bounded A\* backward-chained over conditions and effects. The load-bearing decision is that the three are three ways of *choosing* and what gets chosen is one `IAgentAction`, so an encounter mixes them and one debugger serves all three. The node editor is mandatory and it is a behaviour tree's; a utility set is a table with a response curve and a GOAP graph is *derived*, so drawing either by hand would be authoring the same fact twice |
| 38 | [Learned Terrain Generation](38-learned-terrain-generation.md) | ⚠️ Extends 31 and is 36's first real client — an assessment of Terrain Diffusion (SIGGRAPH '26) and the plan if it holds: a diffusion stack that generates planetary heightmaps and climate, run through ONNX Runtime from a **plugin** rather than from the engine, because 31 already put a content generator on the Out side and named a plugin as where it belongs. Heights land through the heightmap import that already exists, so the terrain kernel does not change and the editor's download does not grow. It is also the first plugin to want a native payload, a 2.3 GB downloaded model cache and a two-minute background task, which is the reach 36's P1 says nobody has tested. ⚠ Opens with a 0.25 EM spike whose likeliest single outcome is that the remaining 4.5 EM is never spent — the models contribute nothing below about 8 km of extent, and one training-corpus licence question is unresolved |

| 39 | [The Standard Frame, and render presets](39-standard-frame-and-render-presets.md) | ⚠️ Extends 06 and 32, and answers the 2026-08-04 rendering audit — one engine-owned `!StandardFrame` node that expands at build time into the same graph sample 13 hand-authors, configured by seven semantic knobs; an Unreal-shaped `RenderQuality.vxpreset` tier waterfall whose every entry names an existing engine knob; and a `.vxlook` profile at the base of the volume stack carrying the artistic agreements the audit proved fragile. The preset *is* a node, so the Unity three-pipelines split is excluded by construction, and the escape hatch is explode-to-text rather than eject |
| 40 | [AI-assisted Generation: materials, meshes and retexturing](40-ai-assisted-material-generation.md) | ⚠️ Extends 36, 11 and 08, is 38's sibling — the *second consumer* 38 said would make the inference seam a design question — and takes 39's authoring shape. Two ComfyUI references turn out to be different kinds of thing: TextureAlchemy is Apache-2.0 and contains no model at all, so its whole contribution is a C# image kernel we can have unconditionally; Chord is a single-step SD-2.1 chain, three network evaluations rather than fifty, whose tileability is circular padding and nothing else. Vixen redistributes no weights — a download button fetches from Hugging Face under terms the author accepts, and the panel's job is to state them accurately. The load-bearing decision is that the generation graph is **of intent, not of ComfyUI nodes**: a dozen semantic node types that a provider (ComfyUI · local ONNX · hosted) resolves, so a graph saved in 2026 still means something in 2028 and we are not maintaining a second ComfyUI. Above it sit four task panels — material from text, material from image, mesh, retexture — each a preset node that **explodes** into the real graph, which is 39's `!StandardFrame` layering one level out. Rides `Vixen.Editor.NodeGraph`, which a generation graph fits four rules out of four where a behaviour tree failed two. ⚠ The first cut line is drawn *below* the AI: a texture toolkit and the three sampling material features the renderer never got. Mesh generation is provider-only permanently — TRELLIS is MIT weights and PBR-native, but `spconv` and `flash-attn` mean there is no export to make — and retexturing is where the engine has an unusual advantage, because the conditioning views are a render |
| 41 | [Automatic Retopology: a quad remesher](41-automatic-retopology.md) | ⚠️ Amends 40 and extends 24, 22, 08 and 33 — the thing doc 40 § D6 named as its honest limit and its closing list called out of scope: *there is no retopology and no unwrapper*. That was the right line for a document about **inference** and the wrong one for the pipeline, because retopology is arithmetic — no weights, no licence to accept, no download — so it belongs in `Core/` beside doc 24's mesh kernel and it runs on a build machine. A ZRemesher-class remesher, implemented clean-room from the literature: Instant Meshes' local field solver (BSD-3, the only permissive implementation there is), QuadWild's **feature-lines-as-layout-boundaries** structure (GPL3, read as a design), and — the 2023 unlock that makes the whole shape shippable — Bi-MDF quantization, a min-deviation flow in place of an integer program, at 0.49 % of the ILP's runtime for 11 % lower energy and **no commercial solver**. Seven stages, each one dumpable. ⚠ The four decisions worth the effort are exact feature reproduction (ZRemesher snaps *after* extraction, which is why its 2018 algorithm still ships under a button), **byte-identical determinism as a CI gate** — the content hash, the meshlet pages and the goldens all rest on it, and it is what rules out the randomized initialization the reference implementation uses — an atlas that falls out of the patch layout for nearly free, and attribute transfer including **skin weights**, without which a remeshed character is not a character. ⚠ Not on the GPU, and that is a decision rather than a gap; the learned singularity prior is a plugin that seeds and never decides, and if one is ever trained **the corpus is a script in this repository** — the free 3-D corpora are disqualified twice over, first because Objaverse and Thingi10K hold amateur uploads and printing STLs rather than the artist edge flow that would be the label, and only second because ShapeNet is non-commercial, ShareAlike may travel into weights and a collection's licence grants nothing about the objects in it |
| 42 | [UV Unwrapping: seams, charts and packing](42-uv-unwrapping.md) | ⚠️ Amends 41 § D13 and extends 24, 40 and 08 — the other half of doc 40's gap. 41 derives an atlas from the patch layout *it already computed* and says plainly that this is not a general unwrapper; the meshes that most need UVs are the ones nobody remeshed. Three problems, not one — seams, flattening, packing — and **each is a callable entry point**, so *"just repack these islands"*, which is what UV-Packer does and all it does, is first-class rather than a side effect. ⚠ The licence position inverts 41's: **xatlas and BFF are both MIT**, so this reads permissive reference code rather than inferring from papers. From MeshTailor (arXiv 2603.27309) it takes two things and not the network — the metric set, which is the best-specified in the literature and becomes the gate, and the representational argument that a seam is a **walk on the mesh graph** rather than a curve that gets snapped, which is the lesson 41 § D4 reached from the other direction; the model itself is trained on professional UV layouts, which is exactly the corpus 41 § D19 declined. PartUV supplies the shape: a **distortion threshold drives a recursion**, so chart count is an outcome of a quality target rather than a knob — which is why xatlas gives 51.6 charts where the target is ten. ⚠ The largest unbudgeted item is that the repository has **no sparse linear solver at all**, and the answer is warm-started conjugate gradient rather than a sparse Cholesky, partly because a fixed iteration budget is deterministic and a residual test is not. ⚠ Margin is in **texels**, so the packer takes the resolution — a margin in UV units is a bug with a two-year fuse |

| 43 | [Web Styling and Tailwind Parity](43-web-styling-parity.md) | ⚠️ Extends 09 § Styling and § Layout, and corrects a rationalisation in the utilities README — the requirement is *Tailwind-equivalent utilities, not a basic subset*, and what is built is a subset whose stated justification was a constraint promoted to a principle. A measured three-way cross product, checked in as [`43-web-styling-parity.tsv`](43-web-styling-parity.tsv): every one of **328 Tailwind v4.3.3 utility roots** against what Vixen emits and what a consumer actually *reads*. **51 work, 29 half work, 13 are inert, 223 are absent**, and a fifth state had to be added because `border-t-2` — read by the layout, ignored by the draw list — is neither working nor inert. ⚠ The target is **v4**, whose CSS-first `@theme` makes `vixen.ui.yaml` the wrong shape before tasks #6 and #7 build it more solidly, and whose `color-mix()`-based opacity modifier *dissolves* the `bg-accent/50` token gap rather than requiring the token model to change. Three tracks — properties, layout modes, families — and C depends on A and B, which is exactly how `grid-cols-3` came to be emitted years before there is a grid. ⚠ Two thirds of the 19.3 EM is CSS Grid and inline formatting, each a subsystem; the other six months is about forty independent items. The references are checked firsthand and four of the claims about them were wrong: **Taffy is MIT only, not dual**, and its corpus is the prize — 5 272 Chrome-derived fixtures in language-neutral **XML**, covering the block and grid modes Vixen has no tests for at all, behind a 40-line Ahem measure stub, so `Tools/Vixen.TaffyTestGen` is *smaller* than `Vixen.YogaTestGen` and is the highest-leverage 0.4 EM in the plan; **Parley cannot do `text-overflow: ellipsis`** — the word does not appear in its repository — which was half the reason it was proposed; and WPT's `css/` is 77 % reftests, with only 1 044 renderer-free layout assertions of which 70 % are grid and flexbox, so for inline and block it is not an oracle at all |

| 44 | [Lore Integration](44-lore-integration.md) | ⚠️ Amends 20 § B7's Source control row and 21 § Part A's two rejections, and extends 08, 11 and 36 — version control as a first-class citizen of the editor, on Epic's [Lore](https://lore.org/), open-sourced under MIT in June 2026. Three documents had pushed SCM away and one clause in doc 21 was load-bearing: *"git is better at it than this would be"*, which is true for the `.vxscene` half of a project and false for the `.fbx`, `.png` and `.wav` that are the project by mass. The load-bearing decision is that **the provider interface is the union rather than the intersection** — a seam shaped from git loses the two capabilities Lore is adopted for, a server-recorded lock and a working tree that does not contain the whole repository, so a provider declares what it *lacks* and a verb whose capability is absent is greyed with the provider named. Five invariants, four of which produce a project that works only on the machine that made it — the load-bearing one being that a `.meta` staged apart from its asset is silent GUID loss, which doc 08 nowhere says. ⚠ Two gaps found in Lore itself and named before anyone plans around them: **there is no `rebase` and no `squash` in the CLI** though the glossary defines both, and **the lock informs rather than blocks**, which makes the exact wording in the UI a test rather than a convention. Ships in-tree rather than as a plugin, and owes doc 36 the reason: there is no extension point that decorates a Project Browser row, so this document adds a general `IAssetDecorator` and then declines to bet the first integration on a plugin carrying a per-RID native payload — the reach 38 says nobody has tested |

### Not design documents

| Document | What it is |
|---|---|
| [Implementation Overview](../overview.md) | **The state, not the design.** Every feature and library with a status, a dependency tree over what is left so independent tracks can be scheduled in parallel, and one table of what is owed. Reconciled against the code |
| [RHI Backend Mapping](../rhi-backend-mapping.md) | A reference table: every `Vixen.Graphics` concept against Vulkan, D3D12, GL/GLES/WebGL2, WebGPU and Metal. The fourth of ADR-001's five measures, reviewed whenever the RHI surface changes |
| [spikes/web-webgl2](spikes/web-webgl2/RESULT.md) | ✅ Executed spike: `Silk.NET.OpenGLES` drives real WebGL2 from `browser-wasm`, in ~40 lines of Emscripten bridge, trimming to 0.93 MB Brotli. Retired risk R1 and corrected a size estimate that was an order of magnitude wrong |
| [manual/](../manual/) | Reader-facing: building a game and a server, the diagnostic-code register, the log-event register |

## Four corrections to the original brief

All four are settled. They are kept here because each changed what got built, and the reasoning is in
[15-risks-and-open-questions.md](15-risks-and-open-questions.md).

1. **There is no Silk.NET Metal binding.** Metal on macOS and iOS is delivered through **MoltenVK**
   — a layered implementation of Vulkan 1.4 that converts SPIR-V to MSL with SPIRV-Cross internally
   (ADR-011). A native Metal backend would be a hand-written binding, not a package away, and is
   post-1.0 if ever.
2. **"No Mono" means no Cecil.** The constraint is *no IL weaving, no Mono.Cecil post-processing, no
   embedded Mono scripting host; Roslyn source generators for all metaprogramming* (ADR-002),
   enforced by a `CheckArchitecture` gate. The Mono-based WASM runtime is acceptable, so **Web stays
   in scope** — the engine constrains its compile-time toolchain, not the runtime host the SDK picks.
3. **Yoga has no CSS Grid**, and the Flexbox C# port is not usable as a dependency (.NET Framework
   4.6, class-per-node, allocation-heavy). It is an *algorithm reference* (ADR-006). Grid is a
   separate algorithm, still unbuilt.
4. **Scale.** ~48 engineer-months for the twelve original phases, all of which are now built or
   part-built — [14-roadmap.md](14-roadmap.md) carries the per-phase state, the four publishable
   milestones (M1 and M2 are passed), and a cut list ordered in advance. The amendments in 19–24 carry
   their own budgets. Plan against milestones, not a completion date.
