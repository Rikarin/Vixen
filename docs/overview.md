# Vixen — Implementation Overview

A single-page reconciliation of **what the plan asks for**, **what exists in the repository**, and
**what is holding the rest up**.

Sources: every file under [`docs/plan/`](plan/), [`docs/manual/`](manual/),
[`docs/rhi-backend-mapping.md`](rhi-backend-mapping.md), the per-project `README.md` files, the
`Directory.Packages.props` register, the Nuke targets in `build/`, and `.github/workflows/`.

> **Where the sources disagree, the code wins.** Every row below is marked against the code, and a
> divergence from a document is called out in the note. This file is the *only* place that carries
> per-feature status: [`14-roadmap.md`](plan/14-roadmap.md) keeps the phase boundaries and their exit
> criteria and points here, and the reasoning behind any one subsystem lives in that subsystem's
> `README.md`. Three places recording the same thing is how they come to disagree.

## Legend

| | Meaning |
|---|---|
| ✅ | Built, tested, and gated |
| 🟡 | Partially built — the named half works, the rest is listed under *Owed* |
| ⬜ | Not started — no project, or a project with no implementation |
| ⛔ | Blocked — cannot start until something else exists or a decision is made |
| ✂️ | Deliberately cut or postponed past 1.0 |

---

# Part 1 — Feature inventory

## 1.1 Build, CI and gates

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| Nuke `Clean Restore Compile Test Pack Benchmark` | ✅ | [build/Build.cs](../build/Build.cs) | |
| Nuke `CheckFormat`, `CheckArchitecture` | ✅ | [build/Build.ArchitectureRules.cs](../build/Build.ArchitectureRules.cs) | Enforces ADR-002 (no IL rewriting), ADR-015 (no authoring-format importer in a runtime assembly) and the `Vixen.Ui` ⇸ `Vixen.Engine` boundary |
| Nuke `GoldenImages` | ✅ | build/ | 40 fixtures; generated on MoltenVK, verified on lavapipe. Also run under `Test`, so a wrong picture fails a normal build |
| Nuke `CheckAot` (desktop) | 🟡 | build/ | Publishes `Tools/Vixen.AotProbe` for the **host** RID only — ILC cross-compiles poorly, so three desktop OSes means one CI leg each, and `ci.yml` wires none of them |
| Nuke `CheckAotIos` | ✅ | build/ | `.ipa`, 7 MB native, zero managed assemblies, zero trim/AOT warnings |
| Nuke `CompileMobile`, `CompileWeb`, `RestoreNativeDeps` | ✅ | build/ | ⚠ These projects are outside `Vixen.slnx` — a `net10.0-ios`/`-android` project cannot be *evaluated* without its workload — so `Test`, `CheckFormat`, `CheckArchitecture` and `Pack` never see them |
| Nuke `CheckApi` | ✅ | [build/Build.Api.cs](../build/Build.Api.cs), [Tools/Vixen.ApiCheck](../Tools/Vixen.ApiCheck/README.md) | 59 packable assemblies, both directions gated (an unapproved addition *and* a silent removal). `Shipped` is empty everywhere, because nothing has shipped |
| Nuke `Docs` | ✅ | [build/Build.Docs.cs](../build/Build.Docs.cs), [Tools/Vixen.DocGen](../Tools/Vixen.DocGen/README.md) | ⚠ Read in **Release**: the generators resolve through `ProjectReference` and any other configuration silently drops ~300 types |
| Nuke `CheckDocs` | ✅ | build/Build.Docs.cs | Coverage, the five-heading contract, link and snippet resolution, orphans, compiled examples, baseline agreement. Coverage is seeded by [`docs/DocsExempt.txt`](DocsExempt.txt), which only shrinks |
| Nuke `Release` | ✅ | [build/Build.Release.cs](../build/Build.Release.cs) | Folds `PublicAPI.Unshipped.txt` into `Shipped`, archives the graph under `docs/api-history/<version>/`, and emits the added/removed/deprecated/breaking table — including the engine-specific rows (a component's size, a system's phase, a shader's bindings) whose signatures are unchanged |
| `ci.yml` — 3 desktop runners, test + checks + pack | ✅ | [.github/workflows/ci.yml](../.github/workflows/ci.yml) | Three `test` legs (ubuntu, windows, macos-14 — two architectures), a `checks` leg running `CheckArchitecture CheckApi CheckFormat CheckDocs`, and `pack`. Doubles as the bit-exact-wire-format gate, whose oracle is bytes committed in `Vixen.Net.Tests/Wire` — which is why dropping a leg costs a desync rather than a build error |
| `docs.yml` — graph, site, image | ✅ | [.github/workflows/docs.yml](../.github/workflows/docs.yml) | Publishes vixenengine.org from `master` as `ghcr.io/rikarin/vixen-docs`; a PR gets a `pr-<n>` tag. No budget gate — the file count it enforced was a hosting platform's ceiling; `pnpm check` still measures it |
| `nightly.yml` — long-running fuzz | ✅ | [.github/workflows/nightly.yml](../.github/workflows/nightly.yml) | A `matrix` job per target over all 20, `fail-fast: false`, each with its own budget, timeout and findings artifact from [`nightly-budgets.json`](../Core/Vixen.Fuzz/nightly-budgets.json) — 6 h 10 of fuzzing inside a 2 h wall clock. `raven` was skipped by name until its binder recursion was bounded (`RVN2005`), and is back in |
| lavapipe Vulkan CI leg | ✅ | ci.yml | 155 Vulkan tests, zero skipped, validation-clean. ⚠ `VIXEN_REQUIRE_VULKAN` makes a *skip* a failure here: a runner that lost its ICD would otherwise go green having proved nothing |
| NativeAOT publish leg on every PR | ⬜ | — | Gate exists (`CheckAot`), leg does not — `ci.yml` names neither AOT target |
| CI leg that *runs* a sample (`--frames N`) | ⬜ | — | The mechanism is built — `--vixen-headless --vixen-frames N`, and `--vixen-capture` for a picture — and both sample READMEs describe this as CI's proof. Nothing is wired to it |
| Playwright browser smoke leg | ⬜ | — | ⛔ the only coverage for `Vixen.Platform.Web`'s interop |
| Content-determinism across 3 real runners | 🟡 | — | The three `test` legs each assert determinism *within themselves* (`Tools/Vixen.Cli.Tests`: two builds, and two projects at different paths in opposite creation order). Nothing compares bytes **between** legs — unlike the wire format, there is no committed byte oracle, so that wants an artifact upload and a comparison job |
| 10 k-asset import budget as a *gate* | 🟡 | — | The *scan* half is gated (`AssetDatabaseTests.TenThousandAssetsScanWellInsideTheBudget`, deliberately loose). The import half is a hand-made fixture and a one-off measurement, with no benchmark project behind it |
| `references/` submodules | ✂️ | [references/README.md](../references/README.md) | Deliberately not submodules, against doc 02: a submodule makes every clone pull gigabytes of other people's history, including on runners that never read a line. Clone commands are tracked; cloning is a local decision |

## 1.2 Core foundation

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Core` — identity, `GameTime`, `ServiceRegistry`, pooling, `DisposeBag`, `LeakTracker` | ✅ | Core/Vixen.Core | 86 tests |
| `Vixen.Core.Mathematics` — full ADR-003 type set + `Matrix3x3`, `ColorSpace`, `Oklab` | ✅ | Core/Vixen.Core.Mathematics | 126 tests + CsCheck properties. `Half` omitted (BCL has it) |
| `Vixen.Core.Collections` | ✅ | Core/Vixen.Core.Collections | `RobinHoodDictionary` and `FixedBitSet<N>` deferred with reasons |
| `Vixen.Core.Memory` — `NativeArray`, arena, buddy allocator | ✅ | Core/Vixen.Core.Memory | `GpuUploadRing` still owed — see the [module README](../Core/Vixen.Core.Memory/README.md) and [plan/03](plan/03-core-foundation.md) |
| `Vixen.Core.Threading` — Chase–Lev deques, `JobHandle` DAG, `ScheduleParallel` | ✅ | Core/Vixen.Core.Threading | 45 tests |
| `VIXEN_JOB_SAFETY` access declarations | ⬜ | — | The flag exists and `JobScheduler` compiles checks under it, but only the ones needing no declarations. The declarations themselves need the ECS to supply them |
| Thread pinning / affinity | ✅ | Platform/Vixen.Platform.{Windows,Linux} | The platform half. `SetThreadGroupAffinity` and `sched_setaffinity`, with performance/efficiency core classes; macOS reports `SupportsAffinity = false` and means it. What is owed is the scheduler asking |
| Job priorities / long-running tier | ⬜ | — | No priority concept anywhere in `Vixen.Core.Threading` |
| `Vixen.Core.IO` — `VirtualPath`, mount table, providers, mmap, coalesced watch | ✅ | Core/Vixen.Core.IO | 123 tests. Android `AAssetManager` and Web IndexedDB/fetch providers landed with their platforms |
| `System.IO.Path` analyzer | ✅ | Core/Vixen.Core.IO.Analyzers | `VXIO0001`, 12 tests. Referenced by every `Core/` project from `Directory.Build.props`; off by name in the seven host-filesystem places. The synchronous-IO half of the rule is still review-only |
| `Vixen.Core.Serialization` + generator + `ObjectDatabase` | ✅ | Core/Vixen.Core.Serialization | 53 tests; LZ4/Zstd chunks, CRC-checked bundles |
| `Vixen.Core.Reflection` generator | ✅ | Core/Vixen.Core.Reflection | Generic types still unsupported |
| `Vixen.Core.Syntax` + generator (green/red trees) | ✅ | Core/Vixen.Core.Syntax | Shared by Raven, VXML — the Phase 0 extraction, and it paid off |
| `Vixen.Core.Yaml` — Vixen dialect, `.meta` model, migrations, `vx:` refs | ✅ | Core/Vixen.Core.Yaml | 73 tests incl. byte-identical round trip |
| `Vixen.Core.Imaging` — KTX2, mips, BC1/3/4/5/7/6H, IBL split-sum | ✅ | Core/Vixen.Core.Imaging | See gaps below |
| ASTC / ETC2 encoders | ⛔ | — | The formats are named in `VkFormats`/`DataFormatDescriptor`; no encoder for either, no managed one exists, and none is planned. Needs `astcenc` pinned in `build/native-dependencies.json` |
| BC7 / BC6H multi-mode | 🟡 | Core/Vixen.Core.Imaging | One (single-subset) mode each — valid output, real quality ceiling. `ispc_texcomp` registered in doc 01 for the rest |
| KTX2/BCn verified against an independent implementation | ⬜ | — | Everything is asserted against hand-computed examples; `ktx validate` + a reference decoder are owed — see the [module README](../Core/Vixen.Core.Imaging/README.md) |
| `Vixen.Core.Diagnostics` — `[LoggerMessage]`, ring sink, profiler, Chrome trace | 🟡 | Core/Vixen.Core.Diagnostics | 36 tests; event ids registered in [log-events.md](manual/log-events.md). What is owed is the UTF-8 ring, the remote protocol and Perfetto protobuf — the [module README](../Core/Vixen.Core.Diagnostics/README.md) § What is not here yet is the list |
| Other log sinks (ZLogger file, console, `logcat`/`OSLog`, remote, `EventSource`) | ✅ | Core/Vixen.Core.Diagnostics | All five, over a shared `LogFilter`. The remote one takes an `IRemoteLogTransport`, since the inspector protocol is not written. ⚠ Apple gets `syslog(3)` and not `os_log` proper, so the subsystem/category pairing is lost until a native shim exists |
| Log rate limiting | ✅ | Core/Vixen.Core.Diagnostics | `LogRateLimiter`: a burst per window per (category, event id), the count carried on the line that follows. `Critical` and a novel event are never suppressed |
| UTF-8 record packing in the ring | ⬜ | — | The ring still holds formatted strings, so an *enabled* line allocates. Deferred until a profile asks — see the module README |
| GPU profiling / memory attribution / Perfetto protobuf | 🟡 | Core/Vixen.Graphics · Core/Vixen.Engine.Renderer | **GPU profiling landed and is reached**: `GpuProfiler` sits in `Vixen.Graphics` over `CreateQueryPool`/`WriteTimestamp`/`TryResolveQueries`, `AppGraphics` attaches it under `GraphicsOptions.GpuProfiling`, and `GpuOverlay` draws the frame. ⚠ It deliberately does **not** live in `Vixen.Core.Diagnostics` — a GPU timestamp's zero point is not comparable with `Stopwatch`, so a GPU timeline sits beside a CPU one rather than merging into these rings. Memory attribution still needs the allocators' reporting surface; Perfetto protobuf is deferred until trace size is measured |

## 1.3 ECS, engine loop, scenes

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Ecs` — archetypes, chunks, edge graph, queries + generator, `CommandBuffer`, change versions | ✅ | Core/Vixen.Ecs | 90 tests |
| System scheduler — 9 phases, conflict graph, DAG on jobs, DOT/Mermaid dumps | ✅ | Core/Vixen.Ecs | |
| Read/write **inference** from query bodies | ⬜ | — | Attributes and programmatic declaration exist; the generator does not |
| World serialisation | ✅ | Core/Vixen.Engine | `WorldSerializer` + `WorldContent`, in `Vixen.Engine` because the ECS references no serializer by design and the binders are `SceneComponentRegistry`'s. ⚠ **Entity handles are not stored**: the hierarchy travels as a table of indices rebuilt by a link pass, and a game component holding an `Entity` is the caller's to translate from the lists `Capture`/`Restore` hand back. A component with no `[DataContract]` is named in `Dropped` rather than lost in silence. See [world-serialisation](guide/engine/world-serialisation.md) and [Vixen.Ecs/README](../Core/Vixen.Ecs/README.md) |
| `VIXEN_ECS_EVENTS` hooks | ⬜ | — | Named in [plan/04](plan/04-ecs-and-scripting.md); no flag and no hooks in the tree |
| Entity handle **reservation** (`World.TryRecreate`) | ✅ | Core/Vixen.Ecs | Allowed only when the slot's version is *exactly* one past the requested one — anything else would let one handle name two entities across its life |
| `Hierarchy.SetParentAfter` / `PreviousSiblingOf` | ✅ | Core/Vixen.Engine | Linking prepends, so undo needs a neighbour rather than an index — an index is invalidated by every insertion in front of it |
| Transform hierarchy with dirty propagation | 🟡 | Core/Vixen.Engine | Not depth-split — needs shared components. One visit per moved entity either way |
| `Vixen.Engine` — loop, fixed-step accumulator, `Behavior`, scenes, `SceneTag`, additive load | ✅ | Core/Vixen.Engine | 58 tests |
| Prefabs (capture + instantiate) | ✅ | Core/Vixen.Engine | A prefab is a **world of its own**, so instantiating is one `CreateMany` per archetype and a row copy each |
| Prefab **overrides** and nested prefabs | ⬜ | — | Risk R7. Nothing named `Override` exists under `Core/Vixen.Engine/Scenes` |
| Coroutines (`async Coroutine`, zero-alloc start) | ✅ | Core/Vixen.Engine | `WhenAny` owed; stopping a single launched coroutine refused by design |
| **Virtual cameras** (shots, body/aim stages, director, blending, noise, impulse) | ✅ | Core/Vixen.Engine | [plan/26](plan/26-virtual-cameras.md), and [Vixen.Engine/README](../Core/Vixen.Engine/README.md) § Virtual cameras. A Cinemachine-shaped system: `VirtualCamera` shots picked on priority by a `CameraDirector` beside the real `Camera`, blended with five curves; four bodies and four aims, each a chunk sweep, so which stage a shot uses is an archetype question rather than a branch. Damping is exactly frame-rate independent and a damping time means one thing everywhere. Runs in `LateUpdate` and writes the camera's *local* transform, so a camera never lags its subject by a frame. 50 tests; a hundred shots allocate nothing across five hundred frames |
| Virtual cameras — dolly track, target groups, recentring | ⬜ | — | [plan/26](plan/26-virtual-cameras.md) § What is deliberately not built. The track wants a spline *asset* — authored, serialised, editable — and inventing one in the camera system would make it the second spline in the engine |
| `DebugDraw` accumulator | ✅ | Core/Vixen.Engine | Lines, rays, arrows, boxes (AABB and oriented), spheres, circles, capsules, cones, frustums, crosses, axes, world labels, screen-space lines/rects/fills/text |
| `DebugDraw` **drawing** | ✅ | Core/Vixen.Engine.Renderer | Two line draws — world (billboarded labels included) and screen. Golden-image verified |
| Doc 13 overlays — frame stats, frame graph, log, console, GPU, audio, water | ✅ | Core/Vixen.Engine · Core/Vixen.Engine.Renderer · Core/Vixen.App.Hosting | `IDiagnosticOverlay`, corner-stacked panels drawn out of `DebugDraw`'s screen list, `[ConsoleCommand]` verbs. **A game reaches them**: `GraphicsOptions.Overlays` / `--vixen-overlays` builds six, `--vixen-overlay <names>` switches them on by name for a headless run, and `SceneRenderHost.Load` appends `DebugOverlayRenderer` to every compositor so a reload cannot lose it. `AppGraphics` feeds `gpu`, `water` and `watermesh`; `Samples/13` feeds `audio`; physics draws into the accumulator directly. ⚠ **A panel makes its counter authoritative — check the counter is real before registering it**; registering four found four that were not. The traps (the ANSI/ISO console key, `DebugDrawSystem` deliberately unregistered, `LineRenderer` and a depth-less target, `ConsoleCommands.Contribute` vs the `RequiresUnreferencedCode` reflection overload) are in [diagnostic-overlays](guide/rendering/diagnostic-overlays.md) and [Vixen.Engine/README](../Core/Vixen.Engine/README.md) § Diagnostic overlays |
| Doc 13 overlays — render mode, UI debug, streaming | ⬜ | — | Render mode needs shader debug views in the compositor; the other two need `Vixen.Ui` and `Vixen.Assets` to report, and neither may reference `Vixen.Engine` (doc 02) — so each wants a join assembly or a data seam of its own |
| ImGui debug overlay | ✂️ | — | Cut in Phase 2 rather than built, and Phase 6's "delete it" step struck with it |

## 1.4 Graphics RHI and backends

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Graphics` RHI surface — formats, `synchronization2`-shaped barriers, typed handles, PSO/descriptor descriptions | ✅ | Core/Vixen.Graphics | 46 tests; reversed depth in the defaults |
| `DescriptorBinding` sample type / comparison sampler | ✅ | Core/Vixen.Graphics | `DescriptorSampleType` on the binding and `PixelFormats.SampleTypeOf` beside it. WebGPU declares and enforces it, Vulkan checks it when it is stated, GL reads past it. Shadow maps on the web now wait on Raven having a depth texture type, not on the RHI |
| **Bindless descriptor arrays** — `BindlessTable`, `MaxBindlessDescriptors`, descriptor indexing in the Vulkan backend | ✅ | Core/Vixen.Graphics · Platform/Vixen.Graphics.Vulkan | Reached from `Vixen.Engine.Renderer`'s `WorldRenderer` and `Vixen.Rendering`'s `EffectSetWriter`, not only from tests. `HasBindless` is four opt-in features rather than an extension string, so MoltenVK below argument-buffer tier 2 reports no rather than failing at the first layout. ⚠ `Count == 0` meant two things — a storage buffer's runtime-sized array *and* an unbounded array — and `IsUnbounded()` is where the kind tells them apart |
| Placed resources (true memory aliasing) | ⬜ | — | Two of six planned backends cannot express it |
| `Vixen.Graphics.Null` + recording harness | ✅ | Platform/Vixen.Graphics.Null | Also the shipping dedicated-server backend |
| `Vixen.Graphics.RenderGraph` — culling, aliasing, batched barriers, derived store actions | ✅ | Core/Vixen.Graphics.RenderGraph | 34 tests incl. property tests |
| Async-compute queue scheduling | ⬜ | — | `PassKind` is declared and carried on every pass; every pass still runs on one queue |
| `Vixen.Graphics.Vulkan` — whole device + command list | ✅ | Platform/Vixen.Graphics.Vulkan | 155 tests, validation-clean on MoltenVK and lavapipe |
| Vulkan swapchain acquire/present automated coverage | ⬜ | — | Needs a window; AppKit aborts off the main thread. `Samples/01` exercises it |
| Timeline semaphores, MSAA resolve, query pools | 🟡 | Core/Vixen.Graphics | **Query pools are built and reached** — `CreateQueryPool`/`WriteTimestamp`/`TryResolveQueries` on Vulkan, under `GpuProfiler`; GL throws for them with a stated reason. **MSAA resolve is built at the RHI and unreached**: `ColorAttachment.ResolveView` + `StoreAction.Resolve` are honoured by the Vulkan and WebGPU backends, and no renderer sets either. **Timeline semaphores are a feature flag only** — `HasTimelineSemaphores` is translated and asserted, and nothing consumes it |
| `Vixen.Graphics.OpenGL` — GL 4.5 core / GLES 3.0-3.2 / WebGL2 translation | ✅ | Platform/Vixen.Graphics.OpenGL | 131 tests against a recording `IGlApi`. Reached by `Tools/Vixen.App`, which opens a `GlDevice` when `GraphicsBackend.OpenGl` is in the preference list — but only over the **desktop GL 4.5 core** leg; the GLES and WebGL2 profiles are exercised by the recording tests alone. ⚠ SDL fixes a window's graphics API at creation and the GL and Vulkan flags are mutually exclusive, so `[Vulkan, OpenGl]` cannot fall back from one to the other in a process — `PlatformHost` reads the list to choose the window flag |
| `Silk.NET.OpenGLES` + EGL context | 🟡 | Platform/Vixen.Graphics.OpenGL | `SilkGlesApi` + `EglContext` + `NativeEglApi` are written and tested against a recording `IEglApi` — 19 entry points loaded from `libEGL` through `Vixen.Platform.Native`, since no `Silk.NET.EGL` exists for Silk.NET 2. ⚠ **Nothing outside `EglContextTests` constructs any of them**: the one production GL path takes its context from SDL and uses `SilkGlApi`, and the Android head has no GLES code. Built, not reached |
| `glBindImageTexture` (storage images) | ⬜ | — | `GlDevice.Replay` throws for storage images by name. Every compute path has a fullscreen-fragment variant meanwhile |
| `Vixen.Graphics.WebGPU` (native, Dawn/wgpu) | ✅ | Platform/Vixen.Graphics.WebGPU | Renders against pinned wgpu-native; push constants emulated as a dynamic UBO. `VIXEN_REQUIRE_WEBGPU` makes a skip a failure on the macOS leg |
| `Vixen.Graphics.WebGPU.Browser` (`navigator.gpu`) | ✅ | Platform/Vixen.Graphics.WebGPU.Browser | Tested against a recording fake with no browser |
| WebGPU timestamp queries; Linux CI leg | ⬜ | — | wgpu-native on the lavapipe `ci.yml` already installs would be a second implementation, and is where the interesting bugs are. The leg is written down as owed in the workflow itself: watch one run, then add `ubuntu-latest` to `VIXEN_REQUIRE_WEBGPU` |
| `Vixen.Graphics.Direct3D12` | ✂️ | — | Postponed past 1.0 (ADR-001 / Q4). **The stub project ADR-001 reserves does not exist either.** GL is the abstraction validator |
| `docs/rhi-backend-mapping.md` | ✅ | [rhi-backend-mapping.md](rhi-backend-mapping.md) | |

## 1.5 Platform

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Platform` contracts (window, surface, display, files, clipboard, dialogs, lifecycle, input, IME, power, topology) | ✅ | Platform/Vixen.Platform | 26 tests |
| `Vixen.Platform.Headless` | ✅ | Platform/Vixen.Platform.Headless | 31 tests; drives the dedicated server |
| `Vixen.Platform.Desktop` (SDL **2** via Silk.NET) | ✅ | Platform/Vixen.Platform.Desktop | 58 tests. Doc 01 said SDL 3 and was wrong |
| File pickers, clipboard images/custom formats, thread affinity, thermal state | ✅ | Platform/Vixen.Platform.{Windows,Linux,MacOS} | SDL 2 has none of them; they arrive through `IPlatformSupplement`, chosen by operating system in `DesktopSupplements` |
| `Vixen.Platform.Windows` / `.Linux` / `.MacOS` | ✅ | Platform/Vixen.Platform.{Windows,Linux,MacOS} | 67 tests. `IFileDialog` · `zenity`/`kdialog` · `NSOpenPanel`; `CF_DIBV5` · `image/png` · `NSPasteboard`; affinity on two of the three; thermal on two of the three |
| `Vixen.Platform.Native` — RID chain, `runtimes/` search, `DllImportResolver`, `RestoreNativeDeps` | ✅ | Platform/Vixen.Platform.Native | Retired R11's desktop half with **no suppression** |
| `Vixen.Platform.Ui` — the platform ⇄ document join | ✅ | Platform/Vixen.Platform.Ui | `PlatformInput` (one copy, where `Samples/02` and `Vixen.Editor.App` each had one) and `PlatformWindowHost`, which fills `IUiWindowHost` over `IPlatform`. 7 tests over the headless platform |
| Native-dependency acquisition beyond MoltenVK | 🟡 | [build/native-dependencies.json](../build/native-dependencies.json) | Two entries — MoltenVK (`ios-arm64`, static, link-time only) and wgpu-native. R10 lists five more |
| `Vixen.Platform.iOS` (UIKit, `CAMetalLayer`, `CADisplayLink`, multi-touch, IME) | 🟡 | Platform/Vixen.Platform.iOS | Runs in the **Simulator**. Physical device ⛔ on a provisioning profile (an Apple account, not a build setting) |
| iOS sensors, haptics, Metal-layer HDR, `UIWindowSceneDelegate` | ⬜ | — | None of the four appears in the assembly. File dialogs, clipboard images, gamepads and hardware keyboard refused with reasons |
| `Vixen.Platform.Android` (SurfaceView, lifecycle, Choreographer, `AAssetManager`) | 🟡 | Platform/Vixen.Platform.Android | Runs on the **emulator** (`-gpu swiftshader_indirect` required). No physical device attached |
| Android GLES fallback + device-capability deny-list | ⬜ | — | The binding and the EGL context are written, and nothing reaches them (§1.4). What is left is the head choosing GL over Vulkan and the deny-list that decides when |
| Android key translation, safe-area insets, sensors | ⬜ | — | The insets gap is recorded in `AndroidServices`: `WindowInsets` is the right source and needs a window it does not have |
| Android AOT gate (on its *default* runtime, not NativeAOT) | ⬜ | — | `XA1040` calls NativeAOT experimental on Android |
| `Vixen.Platform.Web` — canvas, all input, IndexedDB, fetch + ranges, single-thread job mode, lazy assemblies | ✅ | Platform/Vixen.Platform.Web | Not in `Vixen.slnx` (needs `wasm-tools` to evaluate) |
| Web: native dialogs, display enumeration, window position, thermal state, clipboard images | ⛔ | — | Absent by platform, not by omission — each documented with why |
| Browser transport for `Vixen.Net` | ⬜ | — | A browser cannot open a UDP socket; the existing `Vixen.Net.Transport.WebSocket` is a server/desktop implementation |
| `AudioWorklet` path (cross-origin-isolated pages) | ⬜ | — | `vixen-audio.js` is a `ScriptProcessorNode` and says so. Would cut WebAudio's 40 ms queue to ~2 ms; needs the page to be cross-origin isolated and threads to be available |

## 1.6 Asset pipeline

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| Asset database — GUID index, reverse refs, duplicate repair, orphan quarantine | ✅ | Editor/Vixen.Editor.Core | 26 tests; 10 000 assets inside budget. Sidecar envelopes only, read in parallel |
| `Vixen.Assets` catalog, `AssetHandle`, ref-counted scopes, label/glob loading | ✅ | Core/Vixen.Assets | 48 + 64 tests |
| Content references (`ContentReference<T>`) | ✅ | Core/Vixen.Core.Serialization | |
| Streamed content — `assets.Open(address)` over `ObjectDatabase.ReadRaw` | ✅ | Core/Vixen.Assets | Claims and caches nothing, so two callers get two independent streams — which is what a video whose picture and sound both seek needs. ⚠ Build such payloads uncompressed |
| `ProjectWorkspace` + `ContentPipeline` (scan → import → plan → pack → write) | ✅ | Editor/Vixen.Editor.Assets | Moved out of `Vixen.Cli` so the editor and `vixen content build` cannot drift; the CLI keeps the console formatting and the worker pool |
| Content build — `.vxgroup`, `ContentBuilder`, content-hash bundle names, deterministic | ✅ | Editor/Vixen.Editor.Assets | 77 tests |
| `BuildPlanner` + sub-asset addressing (`characters/hero#Hero_Mesh`) | ✅ | Editor/Vixen.Editor.Assets | |
| Remote content — HTTP + ranges, `BundleCache`, resume, CRC | ✅ | Core/Vixen.Assets | 31 tests over a hostile transport |
| Unpacked content — a catalog over `Library/`, nothing packed | ✅ | Editor/Vixen.Editor.Assets, Tools/Vixen.App | Doc 17's **Editor variant**, the last of its five. `vixen content loose` writes a catalog whose entries name no bundle beside the artefact store the import wrote; `--vixen-loose-content <project>/Library` boots a player from it, and the same planner decides the same addresses a shipped build would |
| Remote content **reached from the boot path** | ✅ | Tools/Vixen.App | The host builds a `RoutedBundleSource` over a `BundleCache` under `/cache` — but only when the catalog actually names a URL, so a game that ships everything in its package has no cache and no `HttpClient`. `AppBuilder.WithContent` replaces the transport |
| Content updates (hash file → catalog overlay, never throws) | ✅ | Core/Vixen.Assets | |
| `Tools/Vixen.ContentServer` | ✅ | Tools/Vixen.ContentServer | 34 tests, no socket; path traversal asserted 7 ways |
| Importers: Texture, Model (Assimp), Audio, NativeFormat, Raw/Default, NavMesh | ✅ | Editor/Vixen.Editor.Assets | |
| Out-of-process import worker (`Tools/Vixen.AssetCompiler`) | ✅ | Tools/Vixen.AssetCompiler | Crash isolation, not speed |
| **Parallel** import (N workers) | ⬜ | — | The up-to-date *decision* is parallel now (`ImportPipeline.ImportAllAsync` prepares every entry through `Parallel.For`), which is what met the scan budget. The imports themselves stay sequential in path order; the pool runs N workers and is handed one job at a time, and `--isolated` is off by default |
| `AssetDatabase` per-entry persisted index | 🟡 | Editor/Vixen.Editor.Core | `Library/GuidIndex` is written and read — tab-separated text, deliberately, so a person can read it at four in the morning — and `EditorProject` loads it on cold start. ⚠ Freshness is whole-database (a meta count and a newest write time, no file read), so it is a heuristic and a miss costs a full rescan. Per-entry freshness, which would make the miss incremental, is what is owed |
| `.vxscene` **authoring** format (YAML, `SceneFormat`/`SceneSerializer`/`SceneFileWriter`) | ✅ | Editor/Vixen.Editor.SceneView | Entities named by `EntityId` GUID, not by handle |
| `SceneCompiler` — `.vxscene` → runtime chunk, and a runtime scene asset | ✅ | Editor/Vixen.Editor.Assets, Core/Vixen.Engine | **K1, built** — see §3.1. `SceneImporter` claims `.vxscene`/`.vxprefab` and writes a `SceneAsset`/`PrefabAsset` chunk; `SceneComponentRegistry` turns a contract name into a chunk write; 65 tests. ⚠ What a scene may name is `[Component]` **plus** `[DataContract]`, which is what keeps an entity handle out of a file by construction |
| Scenes-in-build → a player that opens one | ✅ | Editor/Vixen.Editor.Assets, Tools/Vixen.App | The content build resolves `PlayerBuildSettings.Scenes` to addresses and writes a `SceneManifest` beside the catalog; `AppConfig.StartupScene` defaults to the first of them and the host loads it before `OnInitialise`. `--vixen-scene` overrides without a rebuild |
| Path-derived default addresses + `excluded:` | ✅ | Editor/Vixen.Editor.Assets | An asset with no `address` ships as `Assets/Textures/Crate.png`, reversing doc 08's "no `addressable` block means not shipped". An explicit address is now for the ones that are *contracts*, because it survives a move. ⚠ Folders and `.vxgroup` files are never defaulted, and a path-derived address that cannot be packed is a warning where an author's would be an error |
| Duplicate sub-asset names survive import | ✅ | Editor/Vixen.Editor.Assets | The second of two meshes called `Cube` in a `.glb` is `Cube_1` with a warning, rather than the whole asset failing with advice nothing in the editor could act on. `ModelImportSettings.SubAssetNames` is the rename |
| A typed load out of a real content build | ✅ | Core/Vixen.Assets, Editor/Vixen.Editor.Assets | Pinned by the first test to put a whole build into an `AssetManager` — which is what found two defects that made every shipped `Load<T>` fail and that the catalog, the determinism gate and `vixen doctor` had all passed. ⚠ A bundle's file name and the chunk's declared type id are the two things only an end-to-end load checks |
| `.vxnetrules` asset (importer + serialised form + per-prefab reference) | ⬜ | — | `NetworkRulesRegistry` is what it loads into; only prose in [plan/16](plan/16-networking.md) and the registry's own doc comment exist |
| Colour-grading `.cube` LUT importer | ✅ | Editor/Vixen.Editor.Assets | `CubeLutImporter` reads what Resolve, Baselight, Nuke and Photoshop export into an `Rgba16Float` volume, and `Tonemap.rvn` samples it with the correct half-texel inset. **It was written, tested, documented and absent from `BuiltInImporters.Create()`**, so every `.cube` fell through to `RawImporter` — registered now, and `CubeLutTests` drives one through the build's own registry rather than only through `CubeLut.Parse` (task #167). ⚠ No mips and no block compression, by design: a mip of a colour transform is a *different* transform |
| **The unregistered-importer gate** | ✅ | Editor/Vixen.Editor.Assets.Tests | Task #168, and the reason #167 was one of **six**: `[Importer]` is a declaration nothing scans for and `BuiltInImporters.Create()` is a hand-written list, so an importer absent from it is silently claimed by `RawImporter` with no diagnostic anywhere. `.vxwaves` was the first and left a warning comment in that file; `.cube`, `.vxbt`, `.vxgoap`, `.vxquery` and `.vxutility` were all added afterwards and all missed it. `EveryAttributedImporterTests` walks the `[Importer]` types in the assembly that defines the list and fails on any the registry does not hold, plus any whose extension the registry hands to something else. ⚠ **Scoped to that assembly on purpose** — a project script's or a plugin's importer *is* meant to register by attribute, through `ImporterContributions`. ⚠ A test over the constructed registry rather than a `CheckArchitecture` rule, because that target parses `.csproj` XML without compiling and could only pattern-match what `Create()` appears to say |
| Server content profile (no textures/audio/shader permutations) | ⬜ | — | `BuildVariant.Server` exists and the content build does not read it. Doc 08's, not networking's |
| `Vixen.Sdk` MSBuild integration | ✅ | Tools/Vixen.Sdk | 7 tests, each a real `dotnet build` |
| SDK ships the `vixen` CLI in the package | ⬜ | — | Consumer still needs the tool restored or installed |
| Platform packaging (APK assets, iOS bundle, `wwwroot`) | ⬜ | — | Waits for those platforms |
| `Vixen.Cli` — `import`, `content build`, `content serve`, `doctor`, `new`, `build`, `run` | ✅ | Tools/Vixen.Cli | 47 tests incl. a byte-for-byte determinism gate |
| Signing, notarisation, DMG/IPA/AAB | ⬜ | — | `Build.Publish.cs` is the first half and says so; `Sign` and `Notarize` do not exist. Doc 17's table is still Nuke's |
| `Tools/Vixen.Templates` — `vixen-game` / `vixen-app` / `vixen-lib` / `vixen-mmo` | ✅ | Tools/Vixen.Templates | 38 tests; one file tree, packed for `dotnet new` and embedded in `vixen new`. Each template's C# is compiled by Roslyn against the assemblies its packages resolve to — and a multi-project template as one library, so its reference graph is asserted by reading the csproj files instead |
| `vixen-plugin` template | ⬜ | — | **Unblocked**: it was written down as waiting on `Vixen.Editor.Plugin`, which landed in the same wave (W0-12). Owed, not blocked |
| `vixen-tool` template; per-platform heads in `vixen-game` | ⬜ | — | Unblocked — `Vixen.Platform.Headless` is built |
| `vixen doctor systems` | ⬜ | — | `doctor` takes a project and a target and runs `DoctorRunner.Examine`; there is no `systems` subcommand. Needs a game assembly to load |
## 1.7 UI framework

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Ui.Reactive` — signals, computeds, effects, collections, async | ✅ | Core/Vixen.Ui.Reactive | 63 tests; diamond evaluated once, zero-alloc steady state |
| `EffectScheduler.Flush()` driven by the frame pass | ✅ | Core/Vixen.Ui | `UiDocument.Update` drains `Effects` once at the top of a pass, under the guard that refuses a nested one. ⚠ Draining is the *document's* job, not the host's — when it was the host's, only `EditorShell` did it and a game on the `vixen-app` template drew an interface whose bindings never ran. `EffectPassTests` asserts on `Update` rather than `Flush` for that reason |
| `Vixen.Ui.Layout` — SoA store + **four algorithms**: flexbox, block, grid, inline | ✅ | Core/Vixen.Ui.Layout | Flexbox and block complete; grid and inline partial and each says which part — see the README's *State* section and [43](plan/43-web-styling-parity.md) § B1–B3. ⚠ **`display` is a seven-member enum** (`Flex`/`None`/`Block`/`Grid`/`Inline`/`InlineBlock`/`InlineFlex`); code that treats an unrecognised member as flex is wrong in a way only a stacked layout with margins shows. Floats are not implemented and **not foreclosed** |
| Yoga conformance suite (534 fixtures) | ✅ | Core/Vixen.Ui.Layout.Tests | 9 skipped (`display: contents`, out of scope) |
| Taffy block corpus (884 + 28 `blockflex`) | ✅ | Core/Vixen.Ui.Layout.Tests | 746 pass, 124 refused, 42 known gaps — all 42 in the shared *absolute* path, none in block formatting. ⚠ 48 of the refusals test that `overflow` blocks a collapse and are refused for `scrollbar-width`: the corpus states Chrome's answer and cannot run it, so `MarginCollapsingTests` holds that rule by hand |
| Layout gates — zero-alloc settled tree, 11 ns unchanged pass, 1.16 ms at 10⁴ | ✅ | Benchmarks/Vixen.Benchmarks.Ui | |
| **CSS Grid** | 🟡 | Core/Vixen.Ui.Layout | Doc 43 § B2. Placement (§8) and most of track sizing (§12) are done — `fr`/`minmax`/`fit-content`/`repeat`, a second arena for track lists, a bump-allocated per-pass scratch — and 1 526 of 2 120 Taffy grid fixtures pass. It reaches the store from a stylesheet (`LayoutStyleBuilder`, `grid-cols-N` utilities) but **nothing authored uses it yet**. Owed: baseline alignment, §9's containing block for abspos children, and `grid-template-areas` — ⚠ the one part with no oracle, since not one corpus fixture sets it. `Taffy/GridKnownGaps.txt` names the rest |
| `Vixen.Ui.Styling` — selectors, cascade, `@layer`, `@media`, inheritance, `var()`, sharing | ✅ | Core/Vixen.Ui.Styling | Oracle gates green, verified by sabotage. `UiDocument` takes size, scale, gamut and colour scheme off its primary surface; `StyleEngine.SetMedia` re-decides on a resize, guarded on whether any condition changed its mind — a reload is a full re-parse and a drag is sixty a second. Owed: per-surface media, since rules are shared across a document's windows |
| Style **invalidation** (`StyleUpdater`, `StyleInvalidator`) | ✅ | Core/Vixen.Ui.Styling | Wired into `UiDocument.Update`. One class toggle: 9.50 ms / 8.87 MB → **0.94 ms / 552 B**, and the allocation no longer scales with the document |
| Transitions, `@keyframes`, `cubic-bezier`/`steps`/`spring()`, Oklab | ✅ | Core/Vixen.Ui.Styling · Core/Vixen.Ui | Springs solved in closed form. On `StyleEngine.Animations`: `StyleUpdater` announces replaced styles, `UiDocument.Tick` advances, `UiDocument.Apply` overlays above `!important` per CSS Cascading 5 § 6.2. `TransitionTests` reads values *between* the endpoints — the only measurement that tells a fade from a jump. ⚠ A transition needs the value it comes *from*, so a property the element did not previously declare does not fade |
| Colour syntax — hex, named, `rgb()`, `oklab()`/`oklch()`, `color-mix()` | ✅ | Core/Vixen.Ui.Styling | Doc 43 A9/A10. Four interpolation spaces, premultiplied alpha, CSS Values 5 percentage normalisation. ⚠ Out of gamut is carried **unclamped** through the cascade on purpose and repaired at draw against the surface's real gamut — see the `OkLch` row |
| Transform decomposition | ⬜ | — | Waits on a transform property existing; nothing in `Vixen.Ui` parses or carries one |
| `Vixen.Ui.Styling.Utilities` — tokens, scanner, variants, arbitrary values, `@apply` | ✅ | Core/Vixen.Ui.Styling.Utilities | 107 tests |
| Text: UAX#29 segmentation / UAX#14 line breaking / UAX#9 bidi | ✅ | Core/Vixen.Ui.Text | **22 048 + 91 707 conformance cases green** |
| Shaping (HarfBuzz) + itemisation | ✅ | Core/Vixen.Ui.Text | 328/413 Consortium cases; the 85 are HarfBuzz's and pinned in both directions |
| Cluster reconciliation, shaping cache, font fallback chain | ✅ | Core/Vixen.Ui.Text, Core/Vixen.Ui | |
| Glyph outlines (`glyf` + `CFF`), variable fonts (`fvar`/`avar`/`gvar`) | ✅ | Core/Vixen.Ui.Text | 100 Consortium variable-font cases green; `gvar` deltas applied, packed point numbers included |
| `CVAR`, `CFF2` variation, direct `HVAR` | ⬜ | — | 6 cases excluded with the reason recorded; `GlyphOutlineSource` names all three as what it declines |
| Rasteriser, MSDF, atlas, `GlyphFieldCache` | ✅ | Core/Vixen.Ui.Text | Gated by Green's theorem and by field reconstruction. ⚠ The atlas carries a `Version` (coordinates moved) *and* a `Revision` (bytes changed); an uploader watching the wrong one sends the texture once and never again |
| Line wrapping (`LineWrapper`) | ✅ | Core/Vixen.Ui.Text | Greedy first-fit, deliberately not Knuth–Plass |
| **Wrapping wired through** — `TextLayout` over `TextLine` over `TextRun`, `white-space`/`overflow-wrap` from the cascade | ✅ | Core/Vixen.Ui | Wrapping lives in `Vixen.Ui` because the widths do: a paragraph in two faces has no single design-unit scale. ⚠ Each wrapped line is re-shaped on its own — a ligature does not cross a break |
| `CodeEditor` wrap; the *editing* half of `TextArea` (caret between lines, Enter starting one) | 🟡 | Core/Vixen.Ui.Controls | `TextArea` is done — `AcceptsNewlines` makes Enter insert a break, Up/Down move the caret between lines, Home/End are line-relative. ⚠ Ctrl-Enter still submits, so a form's default button stays reachable from inside the field that claimed the plain key. `CodeEditor` still does not wrap |
| `TextEditor` model — IME, caret affinity | ⬜ | — | Affinity is what makes bidi hit-testing answerable; nothing in the tree carries either |
| Rich-text runs from markup (which stretch is bold) | ⬜ | — | The run list already carries face/size/tracking/leading |
| Geometry builder, path tessellation + antialiasing fringe, batching | ✅ | Core/Vixen.Ui | Trapezoid sweep, not ear clipping |
| `Vixen.Ui.Renderer` — box/text/solid pipelines, atlas upload, scissor clips | ✅ | Core/Vixen.Ui.Renderer | `ui-interface` and `ui-clipped` goldens; 13 sabotages |
| Element tree, `[UiProperty]` system, hit test, routed events, draw list, focus, tab + arrow nav, gestures | ✅ | Core/Vixen.Ui | |
| Element removal, style-slot compaction, `Move`, reparenting | ✅ | Core/Vixen.Ui | Tombstone + compaction, not slot reuse — the ordering invariant is why |
| Pinch and rotate gestures | ✅ | Core/Vixen.Ui, Core/Vixen.Ui.Testing | `GestureRecognizer` tracks a second pointer and raises `TransformEvent` with cumulative *and* incremental scale and rotation, each past its own slop; `UiSubject.Pinch(scale, rotation)` drives it and `TransformTests` reads it. ⚠ A transform claims two pointers that had already begun drags, and cancels those drags rather than continuing them underneath |
| `Vixen.Ui.Composition` — `Component`, `@if`/`@switch`, keyed `@for` | ✅ | Core/Vixen.Ui | |
| `scoped` scoping + a component stylesheet loaded once per **type** | ✅ | Core/Vixen.Ui | `StyleIsScoped` was parsed, carried, and then read by nothing |
| Named slot projection; LIS reorder pass | 🟡 | Core/Vixen.Ui, Core/Vixen.Ui.Markup | Projection is built end to end — `<slot name=…>` binds to `BoundSlot`, the emitter calls `BuildContext.Slot`, `Component.Slots` resolves the default — but **no `.vxml` in the tree declares one**, so it is wired and unspent. The LIS pass does not exist and is correctness-neutral: a move that changes nothing returns immediately |
| `VirtualizingPanel` — the virtualisation primitive doc 09 asks for | ✅ | Core/Vixen.Ui.Controls | Realises on `LayoutFinished`. ⚠ Fixed row heights only — variable heights need a running-sum index, which is a different control. `TreeView` is migrated onto it |
| Image / texture draw command (`DrawContext.DrawImage`, `BatchKind.Image`) | ✅ | Core/Vixen.Ui | Unblocked `Image`, `Viewport` drawing a `RenderTarget`, and the node-graph preview layer |
| Multi-window and DPI | ✅ | Core/Vixen.Ui, Platform/Vixen.Platform.Ui | `UiSurface` — one document, N windows, each with its own size, DPI scale, draw list and `vw`/`vh`. A window is a *surface* rather than a second document, which is what makes moving a panel between windows a `Reparent` and not a rebuild. `IUiWindowHost` is the seam `Vixen.Platform.Ui` fills |
| `Vixen.Ui.Markup` — VXML lexer/parser/binder/emitter, `#line` spans, incremental reparse | ✅ | Core/Vixen.Ui.Markup | 118 tests; byte-exact round trip over every *prefix* of a real file. `VxmlParser` takes the shared `Blender` and reuses green nodes |
| `bind:` update events | ⬜ | — | Two-way `bind:value` works; `bind:value:oninput` does not — the modifier list (`stop`/`prevent`/`capture`/`once`/`self`) is `on:`-only, so which event commits the write is not selectable |
| `Vixen.Ui.Markup.Generators` — `IIncrementalGenerator` | ✅ | Core/Vixen.Ui.Markup.Generators | 19 tests, two of them real `dotnet build`s |
| `vixen` CLI path emitting generated C# to disk | 🟡 | Tools/Vixen.Cli, Tools/Vixen.Sdk | The `CoreCompile` hook carries something now: `VixenImport` runs `vixen import --addresses`, writes `Addresses.g.cs` and adds it to `Compile` *inside* the target, because the SDK's glob was evaluated before it ran. ⚠ It runs before the game assembly exists, so it is `--advisory` and is not the authority on content errors. It does not yet carry the VXML components or shader parameter keys doc [08](plan/08-asset-pipeline-and-addressables.md) names; the source generator covers VXML in-process instead |
| `Vixen.Ui.HotReload` — styles / markup / component replacement | ✅ | Core/Vixen.Ui.HotReload | 27 tests |
| Hot reload driven against a **running window** | ✅ | Editor/Vixen.Editor.App, Editor/Vixen.Editor.Host | Doc 36 § P4. `EditorApplication` builds a `HotReloadHost` over the shell's document, registers it with `MetadataUpdate` (weakly, so a closed window is not a leak) and publishes it as a service, so a module's markup panel follows a `.vxml` edit once `dotnet watch` has recompiled it; `--hot-reload <dir>` adds a `HotReloadWatcher` over `.vcss`. ⚠ A watched sheet loads at `Author` origin over the shipped `UserAgent` copy, so a **deleted** rule does not disappear — replacing in place wants `UiDocument.Replace`, which nothing calls |
| `Vixen.Ui.Controls` — 40-odd standard controls, `ControlTheme` as `UserAgent` origin | ✅ | Core/Vixen.Ui.Controls | 259 tests over a real theme and font |
| `Vixen.Ui.Controls.Advanced` — Docking, TreeView, PropertyGrid, NodeCanvas, CodeEditor, DataGrid, Viewport, ColorPicker, CurveEditor, GradientEditor, Timeline | ✅ | Core/Vixen.Ui.Controls.Advanced | 313 tests |
| **Floating dock groups in real OS windows** | ✅ | Core/Vixen.Ui.Controls.Advanced, Platform/Vixen.Platform.Ui | A tab dragged off the window tears out; a drop inside docks. Windows keyed on the group object rather than its index so a rebuild does not blink them, drags tracked in desktop space so one can cross a boundary, and closing a window brings the panels home. Falls back to a rectangle inside the host where the platform has one window |
| `UiDocument` "layout finished" callback | ✅ | Core/Vixen.Ui | All six controls on it. `Control.WhenResized` gates on the box changing; `Update` refuses a nested call, which is what lets a `Refresh` that runs its own pass hang on the event |
| Undo inside controls | ⬜ | — | Deliberate rather than merely missing: `CodeBuffer` says undo belongs to the application, because it has to interleave with everything else the application did. The four `Changed` events are the seams a real stack subscribes to |
| `Canvas2D` | ⬜ | — | Doc [09](plan/09-ui-framework.md)'s P2, no editor consumer. `Samples/06-CanvasStress`, the intended proof, does not exist either |
| `OkLch.ToSrgb` real gamut mapping | ✅ | Core/Vixen.Core.Mathematics | `GamutMap` does CSS Color 4's binary search on chroma in OkLch, holding hue where a per-channel clamp shifted it. `StyleValueParser` brings wide-gamut authored colours in; `UiGeometryBuilder` maps every emitted colour into the surface's gamut. `GamutMapTests` + `UiGamutTests` |
| `StyleTree.AppendChild` O(children) | ✅ | Core/Vixen.Ui.Styling | Amortised constant now: a child run reserves capacity beyond its count and doubles on overflow, so copies fall off geometrically. ⚠ The reserved slots hold stale ids and nothing may read them — every reader is bounded by `ChildCount`, not by the arena's length |
| `Vixen.Ui.Testing` harness + software rasteriser | ✅ | Core/Vixen.Ui.Testing | Selector queries, interactions (taps, drags, `Pinch`), box and visual assertions. Group opacity, a third finger and the remaining box assertions are owed — see the README's *Owed* |
| `Samples/02-HelloUi` | ✅ | Samples/02-HelloUi | 8 001 elements at 0.436 ms, 0 B — exit criterion met with margin |

## 1.8 Raven and shaders

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| Hand-written lexer + recursive-descent parser (Phase 5b migration) | ✅ | Raven/Vixen.Raven | ANTLR `.g4` kept as a differential oracle |
| Green/red tree from `Syntax.xml` (79 + 13 node types), trivia, spans | ✅ | Raven/Vixen.Raven | |
| Semantic phase, target-independent IR | ✅ | Raven/Vixen.Raven | |
| GLSL + SPIR-V emitters, `spirv-val` on every module | ✅ | Raven/Vixen.Raven | Golden `spirv-dis` snapshots |
| Incremental reparse with green-node reuse | ✅ | Core/Vixen.Core.Syntax | |
| `protocol`, shader inheritance, compile-time generics, `compose` | ✅ | Raven/Vixen.Raven | |
| Atomics (8 on `int`/`uint`) | ✅ | Raven/Vixen.Raven | Landed for the VFX compaction path |
| `.rvnlib` / `.rvnfx` artefact formats | ✅ | Raven/Vixen.Raven | |
| `Raven/Library` — Core, Shading, Geometry, Material, Pipeline, PostFx, Ui, Vfx | ✅ | Raven/Library | Every shader reaches both backends under `glslc` and `spirv-val` |
| **Bindless texture arrays** (`Texture2D[]`) | ✅ | Raven/Vixen.Raven · Raven/Library | The only unsized array outside a storage block, and the one that is descriptors rather than memory: `OpTypeRuntimeArray` with no stride under `RuntimeDescriptorArray`, `uniform texture2D t[]` under `GL_EXT_nonuniform_qualifier`, `Count == 0` in the reflection. Spent by `MaterialSurface.rvn` and `Terrain.rvn`, reached from the host by `WorldRenderer` and `EffectLoader`. ⚠ Every subscript is decorated `NonUniform` on **both** the index and the pointer — one without the other validates and then reads one descriptor for a whole subgroup |
| **String interpolation** | ⬜ | — | Needs lexer modes; nothing shipped uses it |
| **Workgroup-shared memory** | ✅ | Raven/Vixen.Raven · Raven/Library | `groupshared` is a parser modifier, a flag on `SourceFieldSymbol`, and a per-entry-point reachability pass in the `Lowerer` — ⚠ a stage declares only the shared variables its own reachable code touches, because workgroup memory is a budget rather than a namespace. Emits as an `OpVariable` in SPIR-V's `Workgroup` class and a GLSL `shared`, with two barrier intrinsics carrying `AcquireRelease` over `WorkgroupMemory`. Held by `GroupSharedTests`, and **spent**: `Culling.rvn`'s traversal queue, `VisibilityTiles.rvn` and `WaterTiles.rvn` need it. **Nothing is blocked on this**, GPU sorting included |
| `Vixen.Raven.Transpile` (SPIRV-Cross → ESSL/HLSL/MSL/WGSL) | ⬜ | — | ADR-012 says SPIRV-Cross owns these targets. **No SPIRV-Cross package in `Directory.Packages.props`**, and no such project under `Raven/` |
| Cross-compilation test pass | ⬜ | — | Blocked on the row above |
| Nuke `CompileShaderLibrary`; SPDX enforcement in `CheckFormat` | 🟡 | build/Build.Shaders.cs · build/Build.cs | `CheckShaders` is the half that matters and exists: it builds `Vixen.Raven.Cli`, recompiles each editor library shader from its whole import closure, and fails if a committed `.spv` has drifted. ⚠ A shader's import closure is one compilation (`raven compile --source`) — the alternative is a chain of `.rvnlib` in dependency order, a second build graph that has to agree with the `import` lines. **SPDX enforcement is still absent from `CheckFormat`**, which runs `dotnet format style` and `analyzers` only |
| Numeric BRDF gate (GPU compute readback vs. C# port) | ✅ | Platform/Vixen.Raven.Gpu.Tests | The shipped `Brdf.rvn` — not a copy — evaluated on a device over 256 (angle, roughness) samples against arithmetic *derived* from Walter 2007 and Schlick 1994, because an oracle sharing an implementation with its subject is not one. Plus a **white-furnace** integral needing no reference: directional albedo ≤ 1, monotonically decreasing, 0.885 → 0.438, with a lower bound asserted beside the upper one. ⚠ `ClampAlpha`'s 0.002 floor is part of what the BRDF *is* — a reference without it measures a different function below 0.045 roughness |
| Per-backend layout gate (reflection offsets vs. GPU readback) | ✅ | Platform/Vixen.Raven.Gpu.Tests | The host writes bytes at the offsets the reflection reports and the shader reads members by name, so a member the two disagree about arrives holding its neighbour's value. Nothing asserts what the offsets *should* be — that is std140's business, and a hard-coded table would be a second implementation of the rules. ⚠ The block's hazards are asserted *present*, because a gate is only a gate for what it can observe: `beta@28` packs into `direction`'s tail, the array's stride is 16 and the matrix's is 16 |
| Negative-diagnostic fixture pairs | 🟡 | Raven/Vixen.Raven.Tests | 127 diagnostic ids against 34 `AssertNoDiagnostics` sites: most ids have a trigger, few have the negative that proves the rule does not over-fire |
| Stream interpolation control; per-module flat IR namespace | ⬜ | — | Recorded in doc [07](plan/07-raven-shader-pipeline.md) §Streams and §D. `flat` is applied automatically where an integer varying requires it (`StageInterface.MustBeFlat`), but an author cannot ask for `noperspective` or decline interpolation; IR struct and function names stay one flat namespace, collision-resolved in the `Lowerer` |
| `Vixen.Shaders` — typed parameter/permutation keys, std140 writers | ✅ | Core/Vixen.Shaders | Generated from Raven reflection |
| Effect system + 3 cache tiers (`EffectStore`, disk, remote) | ✅ | Core/Vixen.Shaders | |
| `Tools/Vixen.ShaderCompiler` (`PermutationClosure`, `EffectBundleBuilder`) | ✅ | Tools/Vixen.ShaderCompiler | Zero-runtime-compilation criterion asserted by test |
| `Tools/Vixen.ShaderCompilerService` | ✅ | Tools/Vixen.ShaderCompilerService | |
## 1.9 Rendering, video and XR

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `RenderSystem`, `RenderObject`/`RenderNode`, features, views, stages, sort modes | ✅ | Core/Vixen.Rendering | |
| `VisibilityGroup` (job-parallel) + `GpuVisibilityGroup` (Hi-Z, indirect args) | ✅ | Core/Vixen.Rendering | Falls back where it cannot run |
| Mesh, transform, skinning, instancing, material, lighting, shadow-caster features | ✅ | Core/Vixen.Rendering | |
| **Two-phase occlusion culling** (`GpuVisibilityGroup.TwoPhase`, `Culling.rvn`'s `Late` permutation) | ✅ | Core/Vixen.Rendering | Removes the frame of staleness. ⚠ The late pass writes a **difference**, not an answer — the union would draw every visible object twice. Needs the readback off; `LatePhaseRan` says which happened |
| **Sprites, sprite sheets, 9-slice** | ✅ | Core/Vixen.Rendering · Core/Vixen.Ui · Core/Vixen.Core.Mathematics | `SpriteRenderFeature` + `SpriteGeometry` on the renderer side, `DrawContext.DrawNineSlice` on the interface side, sharing `NineSlice` because the two assemblies cannot reference each other. A nine-sliced image is the same `Image` command and the same batch as a stretched one. ⚠ Tiling is the renderer's only: a repeat count needs the texture's pixel size, which a draw list does not have |
| Compacted draws | ✅ | Core/Vixen.Rendering | `GpuDrawArguments.Compact` — one command per batch, count read from a buffer the host never sees. A clustered `ForwardPlus` frame merges; ⚠ the uniform-light-list path binds a block per object at a dynamic offset and cannot |
| `GraphicsCompositor` as an asset, resolvable by address | ✅ | Core/Vixen.Rendering | Asserted in `Vixen.Assets.Tests` |
| **`WorldRenderer` in the boot path** | ✅ | Tools/Vixen.App | `AppGraphics` is the host's half — device (`GraphicsHost`: Vulkan where there is a surface, Null where there is not), swapchain, `EffectSystem` fed from the content build's `shaders.effects`, the compositor (a project's by address, or a built-in one-pass frame), and the per-frame order: lend the image under a name, draw the world, hand the open list to `Game.OnRender`, submit, present. `--vixen-frames N` therefore exercises the whole renderer on a machine with no GPU |
| **Camera component → `RenderView`** | ✅ | Core/Vixen.Rendering | `CameraExtractionSystem`: lowest `Order` wins, `PreRender` after the transforms. ⚠ A world with no camera leaves the view alone and says so — a zeroed matrix is a black frame that reads as a broken renderer |
| Materials — composable feature tree, 2 workflows, 7 shading models, both layering forms | ✅ | Core/Vixen.Rendering | Every combination through `glslc` + `spirv-val` |
| **A material feature that samples** (`TexturedMetalRoughnessSurface`) | ✅ | Raven/Library · Core/Vixen.Rendering | Needed four things that did not exist: `Texture2D[]` as a type, `[Shared]` so every sampling feature names one table, `uv` on `MaterialData`, and a table slot arriving as a value. ⚠ The authored record carries a *name* and no handle, so a material still serialises on a machine with no device |
| Transmission / refraction | 🟡 | Core/Vixen.Rendering.Water · `Raven/Library/Water` | [35](plan/35-water.md) § D8 closed the *pass*: `!Water` reads a `!Copy` of the scene colour plus depth and integrates absorption and scattering over the path behind the surface, with `SingleLayerWaterShading` as the lobe beside it. ⚠ **What is closed is the pass, not the general case** — the volume integration is homogeneous and the geometry is a surface with something behind it, so water and thick tinted glass are answered; a refracted *ray* through a solid with two interfaces is not, and `Transmission.Refract`/`ScreenOffset` remain the thin-surface approximation. Owed: the general transmissive material, which takes the same seam — a copy, and a pass that reads it |
| Bindless material textures (a feature that samples needs a binding index) | ✅ | Core/Vixen.Rendering | `MaterialRenderFeature.Textures` + `TextureIndices`: the shader declares a `uint`, the texture takes a table slot, and the slot goes into the material's own block. ⚠ Registered per material and **idempotently** — this runs every frame, and a table asked for the same view sixty times a second raises a count nothing lowers |
| Lighting — directional/point/spot/tube/rect, clustered binning, IBL, reflection probes | ✅ | Core/Vixen.Rendering | `EnvironmentBaker` + `SphericalHarmonics` on the CPU. ⚠ Intensities are **photometric**: `LightComponents.Default` gives a new directional light 100 000 lux (daylight), not 1 — see [physical lighting](guide/rendering/physical-lighting.md). A pass lit by an authored 0–1 tint in a cd/m² frame is pixel-identical to a pass that never ran |
| **Light probes** (tetrahedral interpolation) | ✂️ | Core/Vixen.Core.Mathematics · Core/Vixen.Rendering | **Withdrawn as an approach, not deleted as code.** `ExactPredicates` + `DelaunayTetrahedralization` + `LightProbeVolume` are built and correct, and nothing outside their own tests constructs them; the GPU half will not be built, because [19](plan/19-lighting-and-global-illumination.md) § 4 retires tetrahedral interpolation in favour of § L2's brick-and-indirection field, which places probes on a lattice and needs no predicates and no triangulation to sample. The predicates stay because other geometry wants them |
| **Distance fields** (bake, clipmap, tracer, traced pass) | ✅ | `Vixen.Rendering.DistanceFields` | [19](plan/19-lighting-and-global-illumination.md) § L1. Exact bake with a voted sign, camera-snapped scrolling clipmap, CPU tracer, volume textures, compositor node, Raven module, `DistanceFieldAo` — and `DistanceFieldAoImageTests` draws with it on a device |
| **Irradiance field** (bricks, indirection, L1 payload, leak mitigation) | 🟡 | `Vixen.Rendering.IrradianceFields` · Core/Vixen.Rendering | [19](plan/19-lighting-and-global-illumination.md) § L2, and see the [README](../Core/Vixen.Rendering.IrradianceFields/README.md). 4³ probes in a 5³ footprint with refinement, borders, dilation and both biases (§ G3's four leak mitigations); a CPU reference filler and a compute filler that agree probe by probe on a device; `IndirectDiffuse` draws the result and `ForwardPlus` composes it into its ambient term. Filler B renders its own cubes and shades from the field, so pass *n+1* bounces off pass *n*; coarsening is `Split`'s exact inverse with hysteresis. Owed, no known defects: `Deferred` (blocked on the Phase 10 pass), the bake-time step that would size a baked field, and two unmeasured optimisations (narrowing the repair to dirty bricks, batching filler B's one-submit-per-probe) |
| **Screen probe gather** (octahedral map, lattice, reference, traced atlas) | 🟡 | `Vixen.Rendering.ScreenProbes` · Core/Vixen.Rendering | [19](plan/19-lighting-and-global-illumination.md) § L3, and see the [README](../Core/Vixen.Rendering.ScreenProbes/README.md) for every claim below. The whole chain runs on both processors and the two agree: octahedral map with exact per-texel solid angles, 16-px lattice with per-probe validity, placement from the frame's own depth and normals, trace (screen march first, then the irradiance field), resolve to L1, temporal accumulation, spatial filter and bilateral upsample — scheduled by `ScreenProbeGatherRenderer` as one node. § L3's exit criteria are asserted in `ExitCriteriaTests` against a 4096-sample Hammersley reference (1% unshadowed, 5% RMS / 10% worst under occlusion), with a camera cut and a fast pan as the temporal cases; a resize is a deliberate `Reset`. `ScreenTraces` is off by default — probe origins are a frame older than the depth the rays march. Owed, quality rather than criteria: the adaptive probes' device half, importance sampling, bilinear history taps |
| **Surface cache** (cards, atlas, radiosity) | ✅ | `Vixen.Rendering.SurfaceCache` | [19](plan/19-lighting-and-global-illumination.md) § L4. Cards fitted by dominant-axis vote, shelf atlas, traced capture, direct sun behind a shadow ray, double-buffered radiosity — the Cornell box converges against a five-bounce Halton path tracer within a stated 5% (measured 1.2). `SurfaceCardIndex` narrows sampling, `SurfaceCacheLight`/`SurfaceCacheGather` match the CPU pair at zero drift, `ISurfaceCacheSource` makes "what a hit sees" a compose slot so the screen probes inherit multi-bounce light, and `SurfaceCacheRenderer` keeps it live (capture one card a frame, one frame late, because a readback follows its submit). One-pass MRT capture landed. Nothing in § L4 is owed |
| **Traced reflections** (mirror, rough, fallback) | ✅ | `Vixen.Rendering.Reflections` | [19](plan/19-lighting-and-global-illumination.md) § L5, and see the [README](../Core/Vixen.Rendering.Reflections/README.md). The layer that reuses everything below it: a mirror ray marches L1's tracer, a hit answers through L4's `SurfaceCacheRadiance`, a miss asks `IReflectionFallback` — the seat doc 06's probes take through `ReflectionProbeMissSource`, which retires the "blended against the sky" caveat. Rough surfaces above a threshold read L2's field instead of tracing, with a blend band closed-formed on both processors; the screen answers first, through the shared HZB march. `ReflectionTrace` is the kernel and `ReflectionRenderer` the node, held against the CPU reference within a stated hundredth (measured four thousandths, all of it the field read's hardware trilinear). Nothing in § L5 is owed |
| **Ray tracing** (RHI, query, tracer, referee) | ✅ | `Vixen.Rendering.RayTracing` · Core/Vixen.Graphics | [19](plan/19-lighting-and-global-illumination.md) § L6. A median-split triangle BVH as the reference — deterministic builds, front-to-back traversal, two-sided Möller–Trumbore, held against brute force at four-times-fewer visits. The RHI concept is `HasRayTracing` (build/refit + queries + buffer device addresses, all three or none), the two-level build, the acceleration-structure descriptor kind and the 64-byte shared instance record; Vulkan 1.2+ implements it, everything else refuses honestly, the Null device answers coherently. Raven gained the query as one intrinsic — `AccelerationStructure.Trace` expands to the whole query sequence in both emitters — and `RayQueryField` puts it behind L1's interface so every kernel composes it unchanged. ⚠ The golden comparison skips on MoltenVK, which exposes neither extension — stated, not discovered. Owed, quality: the hit's true normal off the vertex buffer, SAH, refit |
| **Doc 19's lit path as compositor nodes** — `!GlobalDistanceField`, `!IrradianceField`, `!DistanceFieldAo`, `!IndirectDiffuse` | ✅ | Core/Vixen.Rendering · Core/Vixen.Rendering.PostFx | The GI renderers all existed with no asset naming them, so a game could reach doc 19 only by assembling its compositor in C#. `CompositorBuilder` now carries the host slots the fields need (`DistanceField`, `IrradianceField`, its filler and refinement policy), on the same terms as `Clusters`/`Pages`: a document names placement, a host supplies what outlives a frame. ⚠ **A node built without one does nothing rather than throwing**, so one document serves a project that has no field — and the empty-`source` defaults are deliberate: "nothing is near, fully lit" and "no indirect light, sun unshadowed" are two different right answers, not one convenient zero |
| **Screen probes, reflections and the surface cache as nodes** — `!ScreenProbeGather`, `!Reflections`, `!SurfaceCache` | ✅ | Core/Vixen.Rendering · Core/Vixen.Rendering.PostFx | The same seam extended: the document owns placement and the numbers, the host supplies `ScreenProbeTracer`/`Resolver`/`Accumulator`/`Filter`, `SurfaceCache`, `Effects` and `TracePyramid`. ⚠ `TracePyramid` is **taken, not read** — `TakeTracePyramid` counts takers per build, so one nearest chain under both marching nodes gets a descriptor ring two deep without the host knowing `BuildsPerFrame` exists. The ambient split landed with it: `ForwardPlus.SplitOutputs` writes direct+emissive, albedo+material-AO, normal+roughness and `f0` to four targets (off compiles to the single-target pass byte for byte), `VisibilityResolve` splits under the same key, and `!AmbientCombine` puts the frame back together. ⚠ **The fourth plane carries `f0`, not metalness, and that decides the arithmetic** — metalness cannot be reconstructed from `SceneAlbedo`, so the combine *adds* the specular term and `ClusteredShading.ambientSpecularScale` stops the shading pass writing what the addition replaces; one condition in `AmbientCombineRenderer.Build` decides both, and a document naming only one of `reflections:`/`specular:` moves nothing. The reasoning is in `AmbientCombineRenderer`'s own remarks. Samples/13 runs the split frame end to end; `!IndirectDiffuse` and the hardware tracer are off there, each with its reason in the document |
| Per-object reflection probe selection | ✅ | Core/Vixen.Rendering · `Raven/Library/Pipeline` | Built and wired. `ForwardLightingRenderFeature` picks a probe per object and writes `probeIndex`/`probeWeight` into an object record; `ClusteredShading.ProbeIndex` reads it back through `SV_InstanceID`; `SceneLighting` fills the array in the selector's own order. ⚠ **`UseObjectRecords` requires `UseTransformRecords`** — what addresses a record is the instance index, and it only holds the object's slot because the transform path put it there; `CompositorBuilder` enables it only where transforms turned on, in a deliberate second pass. ⚠ `ProbeCount` is a compile-time array size (default 4): a scene with more probes binds the first N and `SceneLighting.Dropped` counts the rest, which reflect the wrong room rather than nothing. Raising that ceiling is what [23](plan/23-bindless-materials.md) would buy |
| Shadows — CSM, cube, spot, atlas, static caching, PCF/PCSS | ✅ | Core/Vixen.Rendering | |
| **Virtual shadow maps** — clipmap, page marking, page residency, per-page draws, composed lookup | 🟡 | Core/Vixen.Rendering · `Raven/Library/VirtualShadows` | [22](plan/22-virtualized-geometry.md) phase 7. A directional clipmap and a map per spot: the level is chosen **per pixel** from that pixel's own world footprint, only marked pages are allocated, only newly allocated or invalidated ones are drawn. ⚠ The snap is to a whole **page**, not a texel — that is what makes a page's world footprint bit-identical between frames and therefore cacheable at all. A page enters the table when it is *drawn*, not when it is allocated, and the lookup answers a *sample* (a visibility and whether it knew), so `ClusteredShading.Shadow` falls through to the cascades where the map has nothing. Improvement 6's second consumer: `PageResidency` serves it unchanged. **Owed:** clusters do not cast through it — the traversal appends every view's cut to one untagged list, so per-page culling needs a list per view (a change to phase 3's output); virtualized meshes cast through phase 1's fallback mesh meanwhile. Also owed: point lights, and per-caster invalidation (a moved light invalidates all its pages, a moved *object* invalidates none) |
| Punctual shadow caching | ⬜ | — | Only the directional cascades are cached; `PunctualShadowRenderer` redraws every lamp every frame |
| Motion vectors — a stage with a shader override, as the shadow pass is | ✅ | Core/Vixen.Rendering | `MotionVectorRenderFeature` + `Pipeline/MotionVectors.rvn`, drawn as its own velocity pass — `Taa.rvn` had declared a `motionVectors` input since it was written and nothing produced one. ⚠ **A host must add `"Motion"` to `GraphicsOptions.CasterStages`**, or the pass draws nothing and motion blur is a copy. Why it is a pass rather than an extra target on the shading pass, and why a skinned mesh reports its root's motion: [post-processing § Motion vectors](guide/rendering/post-processing.md) |
| Blend shapes | ⬜ | — | No morph-target code anywhere in the tree |
| `Vixen.Rendering.PostFx` — TAA, FXAA, sharpen, AO, fog, outline, vignette, lens distortion, chromatic aberration, grain, bloom, depth of field, motion blur, local exposure, lens flare, tonemap + CDL grading | ✅ | Core/Vixen.Rendering.PostFx | Seventeen node kinds a `.vxcompositor` names, each with its shader in `Raven/Library/PostFx` and its factory arm in `PostEffectAssets`; `ISceneRendererFactory` makes a game's own effect a first-class one too, and `CubeLutImporter` reads a `.cube` grading LUT — registered as of task #167, having been written and unregistered until then. [30](plan/30-post-processing-parity.md) is the audit against UE 5.8 and HDRP 14, and every row of it is now either built or has a reason below the table |
| **`!StandardFrame` + `RenderQuality.vxpreset`** — the seven-knob frame, the tier waterfall, `vixen frame explode` | 🟡 | Core/Vixen.Rendering.PostFx · Tools/Vixen.Cli | [39](plan/39-standard-frame-and-render-presets.md) and the [README](../Core/Vixen.Rendering.PostFx/README.md). One node that expands at build time into the graph a hand-authored document would contain; `quality:` folds engine→project→document per parameter; `vixen frame explode` writes the expansion out with its comments as the one-way eject. The default path ships end to end: the `vixen-game` template stamps the seven-line document and the preset stub, `Samples/03` runs it, five device goldens cover the tiers and the split frame, and the editor opens a `.vxcompositor` as its knobs with both resolved stacks — the quality table naming which of engine/`.vxpreset`/document stated each knob, and the volume panel reading `PostProcessVolumeSystem`'s fold per camera. `look:` is live (`LookAsset` → `CompositorBuilder.Look` → `PostProcessVolumeSystem.Look`); ⚠ the expansion never reads it, so editing a look relights the same document with nothing rebuilt. **Owed:** `gi: Probes` still needs its host halves — the field objects, surface cache and tracer a builder slot supplies — so the emitted nodes do nothing without them; `look:` as an asset *reference* rather than inline; and the caster stages stay the host's by design (`GraphicsOptions.CasterStages`), because a frame document cannot decide what an object is extracted as |
| SMAA, MSAA resolve, full GTAO, SSR | ⬜ | — | Each needs a shader that does not exist yet. SSR's arithmetic is not among them — `ScreenSpaceTrace` under `!Reflections` is SSR reduced to its march |
| **The water kernel and the evaluator** — bodies, the spectrum, the field, one surface definition | ✅ | Core/Vixen.Water | [35](plan/35-water.md)'s **W1**: one closed-form surface definition every consumer asks, and it opens no device, because § D1's kernel is what a dedicated server runs. ⚠ **`WaterMath.SinCos` is a stated polynomial rather than `OpSin`**, which Vulkan licenses at 8192 ULP — so nothing in that file may be contracted into an FMA, and π is split into four pieces to stay exact at the hundred thousand radians a long session reaches. ⚠ **Depth is computed and never stored**, because a third number can disagree with the two it came from. Bodies rasterise to the same field in either order, and the window snaps to the *coarsest* consumer's grid. The rest — the spectrum, the seed's determinism, the snap — is [`Core/Vixen.Water/README.md`](../Core/Vixen.Water/README.md) |
| **The water pass and the shading model** — `!Water`, `SingleLayerWaterShading`, the volume integration, § D8's tile classification | ✅ | Core/Vixen.Rendering.Water · `Raven/Library/Water` | [35](plan/35-water.md)'s **W2**. `WaterVolume` integrates absorption and scattering over the *view* path in closed form, `!Water` composites it once over W0's `!Copy`, and the shading model is **specular only** because the volume above already counted those photons. ⚠ **All three nodes — `!WaterSurface`, `!Copy`, `!Water` — in that order or there is no wet pixel**, and naming the output in `behind:` is refused by name at build time. **Two stated divergences from § D8**: the surface plane carries a coverage mask rather than a shading-model id (there is no deferred path here), and the tile list is a flag per tile, so the draw is **instanced rather than indirect** — `ICommandList` has no non-indexed `DrawIndirect`, and bringing the count back to the host is a frame-long stall to avoid a pass over mostly-empty tiles. `WaterPassImageTests` asserts each term on a device rather than against a bitmap, which is how the missing isotropic sky in-scatter was found. Traps in [`Core/Vixen.Rendering.Water/README.md`](../Core/Vixen.Rendering.Water/README.md) |
| **The water zone and its sliding window** — the field, when it moves, and what it costs | 🟡 | Core/Vixen.Water · Core/Vixen.Rendering.Water | [35](plan/35-water.md)'s **W3**, and both halves are built and reached: `WaterZone`/`WaterZoneState` hold and move the field, `WaterInfoTexture` uploads its four channels, `WaterZoneSystem` folds the two components out of a world (`AppGraphics` in a game, `WaterPresenter` in the editor), and `.vxwaves` is § D6's one new asset kind. Both exit criteria are measured — a millimetre across two overlapping bodies, 10⁻⁴ m over a two-kilometre scroll. ⚠ **The field is filled by the same code the surface query reads** rather than by § D3's top-down render, so the shoreline the material shades and the depth the boat reads are one number by construction; `WaterInfo.rvn` therefore does not exist and § D3 has not been updated to say so. ⚠ **Bodies are cached by identity and the cache stores the success, never the failure** — every asset source a game has answers null for its first frames. *Owed*: § D3's own headline correction, **the terrain as a first-class ground producer** — `WaterZoneSystem.Ground` defaults to `FlatWaterGround(0)` and only sample 13 and the editor's presenter ever set it, which is the opt-in-per-game shape § D3 rejected; `WaterOccluder` exists nowhere; and the **velocity blur finalise** named in W3's scope line has no implementation at all |
| **The water surface mesh** — the quadtree, the coverage predicate, the far skirt, the two planes | ✅ | Core/Vixen.Water · Core/Vixen.Rendering.Water · `Raven/Library/Water` | [35](plan/35-water.md)'s **W4**, and the node without which no wet pixel can exist. **One quadtree, not two**: `PatchSelector` is `TerrainLodTree`'s descent with the terrain taken out of it, so the morph, the no-crack property and the continuity property are one implementation rather than the reference's two of each, and the finest node's vertex spacing is the field's texel spacing by construction. ⚠ **A node's box is grown by the sea state's maximum amplitude**, or a crest is culled while it is still in front of the camera, and ⚠ **the coverage predicate rounds outward**, because pruning happens before the children are visited. ⚠ **Depth is tested and never written** — a surface that wrote it would be integrated against itself — ⚠ **the far skirt is drawn first**, and ⚠ **`EdgeFade` lives in the mesh and never in the evaluator**, or a raft rides differently depending on where somebody is looking. The horizon fixture asserts the geometry rather than a bitmap; it found the pass had never drawn a pixel on any device (descriptor sets bound before any pipeline) and pinned the `Greater`-with-`LoadAction.Load` silent no-draw — [reading the frame](guide/rendering/reading-the-frame.md) |
| **Water carving** — the reserved layer, the bed, the bank, and what survives a body moving | ✅ | Core/Vixen.Water | [35](plan/35-water.md)'s **W5**, on [31 § D4](plan/31-terrain-grass-and-trees.md)'s contract and **with no change to it** — which is the evidence doc 35 claims it is. The bed a body carves is the bed the field rasterises, because both read `WaterBody.Sample`. ⚠ **A carve only ever cuts**, or a lake whose surface sits above a valley floor fills the valley in; ⚠ **`Regenerate` resolves every body at each sample and combines by min and max** rather than carving them in turn, which is order-dependent twice over and was found by the two-overlapping-lakes test. The profile has three numbers where the reference's brush has twenty, and the missing seventeen are terrain brushes an author runs on a layer *above* — the test that a hand-sculpted shoreline survives a body being moved twice is what says leaving them out was allowed |
| **Swimming and underwater** — the fourth move mode, the immersion, the hysteresis, the volume shape, the waterline | ✅ | Core/Vixen.Physics · Core/Vixen.Water.Physics · Core/Vixen.Rendering.Water | [35](plan/35-water.md)'s **W6**, closing [29 § Where this stops](plan/29-players-and-possession.md) on that row's own condition. `CharacterMoveMode` gains a fourth member and `CharacterState` gains one number, the immersion; every rule is a threshold on it, with **two thresholds and a gap**, because a character in chest-deep water with a 30 cm swell crosses any single one twice a second. `WaterImmersionSystem` is the writer and lives in **`Core/Vixen.Water.Physics`** — not in `Samples/13` where it spent its first life — and finds the water through `IWaterSurface`, never through `WaterZoneSystem`, so nothing drags a device into the path a dedicated server runs. ⚠ **A game that does not reference that package gets no error at all**: the immersion stays zero and a character walks along the bed of a lake that draws perfectly. Underwater is doc 32's volume with `UnderwaterShape` — per zone, rebuilt per frame, testing the *drawn* surface — and `!Underwater` is § D9's waterline half, solved per pixel against the local surface **plane** rather than the wave sum. Traps in [`Core/Vixen.Water.Physics/README.md`](../Core/Vixen.Water.Physics/README.md) and [`Core/Vixen.Rendering.Water/README.md`](../Core/Vixen.Rendering.Water/README.md) |
| **Buoyancy** — pontoons, the spherical cap, the flow drag, and a convergence test with a control | ✅ | Core/Vixen.Water · Core/Vixen.Water.Physics | [35](plan/35-water.md)'s **W7**, kernel and join. `Buoyancy.Solve` asks the same evaluator the surface is drawn from and produces a force at each pontoon's own world position, so a body pitches rather than bobbing flat; the submerged fraction is the exact spherical cap and both damping terms are physical rather than unit-free. ⚠ **`BuoyancySystem` runs in `FixedUpdate` between `PhysicsSyncSystem` and `PhysicsStepSystem`** — either side of that pair fails silently — and ⚠ **`WaterClockSystem` in `EarlyUpdate` is the one writer of the water clock**, because the zone fold is `PreRender` and a solver reading the clock there sits exactly one frame of swell behind the water. No replication of its own: the force is a pure function of things both peers already have, which is also why no ripple reaches the solver. ⚠ **Nothing but a fixture has ever assembled it**: `WaterSceneRunsTests` folds a real pond, `.vxwaves` and dinghy through Jolt — no sample floats a boat, and `Vixen.Water.Physics` is opted into by a game and linked unconditionally by the editor so that one can be authored first. [`Core/Vixen.Water.Physics/README.md`](../Core/Vixen.Water.Physics/README.md) |
| **Ripples** — the sliding-window height field, the budget, and the boundary that does not mirror | ✅ | Core/Vixen.Water · Core/Vixen.Rendering.Water | [35](plan/35-water.md)'s **W8**, both halves: the CPU reference, and `Ripples.rvn` as § D12's step in one dispatch over W0's `PingPongTextures` — its first consumer — with `WaterRippleSimulation` owning the pair. § Risks calls this "an unbounded feature wearing a bounded name", so nearly every number in it is a bound: ⚠ **the Courant limit is refused by `Validate` rather than discovered**, ⚠ **an injection goes into the velocity and not the height**, ⚠ **the edge is damped to nothing rather than clamped**, and ⚠ **the window scrolls by *shifting* its contents** where `WaterField` forgets them, by whole texels, because repeated resampling is a low-pass filter. The seam test states a divergence bound rather than exact agreement, because a height field's state **is** its history. Wakes and splashes are one `WaterDisturbance` event with two consumers and ⚠ **draining does not empty it**. *Owed*: `glBindImageTexture` on the GL backend, and the `Vixen.Vfx` spray the buoyancy solver already produces the events for |
| **The water toolset** — one mode, three verbs, the zone panel's derived numbers, and the wiring | 🟡 | Editor/Vixen.Editor.Water · Core/Vixen.App.Hosting · Core/Vixen.Rendering.Water | [35](plan/35-water.md)'s **W9**, the phase that turns the stack from built into reachable — and most of it is. **One mode where doc 31 needed two**, because placing a lake is placing an entity and editing its shape is editing a spline, and the editor already does both. **Draw is wired end to end**: `WaterMode` → `WaterModule` writes the `.vxspline` and creates the entity, ⚠ **clicking the first point again is how a closed body is finished** (the UI layer has no double click), and ⚠ **an unsaved scene refuses the draw** rather than making an entity whose curve has nowhere to live. **The zone panel is its derived numbers** — metres per texel, megabytes, vertices, height quantum, maximum amplitude — computed by the *kernel*, so it cannot be right about a configuration the renderer refuses. **And the wiring closes the island**: `AppGraphics` owns the `WaterZoneSystem` and the one `WaterClockSystem`, hands the zones to `!WaterSurface` on the terrain factory's terms and supplies them as `PostProcessVolumeSystem.Shapes` before the volume fold; `WaterPresenter` draws the same fold in an editor pane; the `overlay water` / `overlay watermesh` panels are doc 13's registry's first consumers; and the six `water.show*` verbs are registered explicitly by `WaterDebug.Register` and again as editor commands, because `ConsoleCommands.RegisterFrom(Assembly)` has no callers in the tree. *Owed*: the other two verbs are declared and unreachable — **Profile**'s handles (`WaterEdit.Grab`/`Drag`/`HandlesOf`) have no caller but their own tests and `WaterMode.Pointer` returns `false` for any tool but Draw, **Preview**'s `CarvePreview` is a flag nothing reads, and `WaterProfileCommand`/`WaterCarveCommand` are never constructed |
| **The water seam** — `Water/Surface.rvn` against the C# evaluator, on a device | ✅ | `Raven/Library/Water` · Platform/Vixen.Graphics.Golden.Tests | [35](plan/35-water.md)'s **§ D2**, written before there was a renderer to see it fail in, which is the point: `WaterSurfaceProbe.rvn` dispatches the library's `BufferedWaterWaves.Displace` over four thousand positions and times and the readback is compared against `WaterEvaluator` per component. ⚠ **The measured tolerance is 2 × 10⁻⁴ m where the doc asked for exact** — § Risks' own stated fallback, taken: the residue is a device contracting a multiply and an add into one FMA, it lands in the *phase*, and it is therefore linear in it (7 × 10⁻⁷ m under a hundred radians, 4.9 × 10⁻⁵ m at five thousand). Vixen's emitters do not contract; the Metal translation layer below the only Vulkan device in reach does, and no parenthesisation changes that. The structural half still buys most of the distance, since `OpSin`'s licensed 8192 ULP at those phases is a different wave rather than a rounding difference. ⚠ **The wave buffer is a `compose` slot and not a function parameter** — an unsized array has no type SPIR-V can express away from its binding (RVN4001) — so whoever holds the buffer holds the loop, which is the seat an FFT model takes when it lands |
| **Water's unblockers** — a volume shape interface, a third reserved terrain layer, a ping-pong pair, the scene-colour copy | ✅ | Core/Vixen.Rendering · Core/Vixen.Terrain · Core/Vixen.Graphics.RenderGraph | [35](plan/35-water.md)'s **W0**, and every one of the four is written for a consumer outside water, which is the phase's whole condition. **B2**: `IPostProcessShape` replaces [32](plan/32-post-process-volumes.md)'s axis-aligned test with a box *and a sphere*, because an extension point whose only implementation is the special case ends up shaped like it; `Box` is the enum's zero so every volume authored before this loads unchanged, and `Custom` resolves through `PostProcessVolumeSystem.Shapes` — ⚠ **reaching nothing when nothing answers**, since falling back to the box would grade a rectangle around the lake while the inspector looked correct. **B4**: `TerrainLayerKind.Water` on [31 § D4](plan/31-terrain-grass-and-trees.md)'s contract with no change to it, plus `Terrain.ReservedLayer(kind)`. **B5**: `PingPongTextures` imports both halves every step and tracks the state each was left in — ⚠ **`Undefined` is not a neutral placeholder**, it licenses the driver to discard the previous contents, and `HasHistory` exists because the graph *cannot* catch a first-frame read. **B1**: `!Copy` is the resource, not the pass — the compositor could already say "read the scene colour, write the scene colour" and could not say the second target that makes it legal; it refuses a format or size mismatch by name rather than rescaling, and a copy nothing reads is culled with its target, so a document carrying a water node costs nothing without water |
| Grading LUT as an **asset** | ✅ | Core/Vixen.Rendering.PostFx · Editor/Vixen.Editor.Assets | Both halves. The consuming one: `TonemapRenderer.Lut` names a graph resource, `TonemapKeys.UseLut` folds the sample out when it is unset, and `PostEffectAssets` carries the knob. The authoring one: `CubeLutImporter` reads Resolve/Baselight/Nuke/Photoshop `.cube` files, and **is now in `BuiltInImporters.Create()`** — it was not, so no `.cube` became an asset and the failure was silent (§ 1.6's row, task #167). ⚠ No mips and no block compression, deliberately — the reasoning is in `CubeLutImporter`'s remarks |
| Auto exposure — the log-average chain **and** a 64-bin histogram | ✅ | Core/Vixen.Rendering.PostFx | Both of UE's meters. The histogram is **off by default** because it is a different question rather than a better chain — a geometric mean and a percentile disagree about a bright window in a dark room, and a document should not change meters by accident. See `AutoExposureRenderer.UseHistogram` |
| `AutoExposure.rvn` wiring | ✅ | Core/Vixen.Rendering.PostFx | `AutoExposureRenderer`: a chain of `ComputeRenderer` dispatches halving a 512-wide luminance image to 1×1, then easing the stored exposure toward it. `Tonemap.rvn`'s `UseExposureBuffer` permutation keeps the value off the bus and the change additive. ⚠ **The exposure buffer is imported into each frame, and into the *frame* rather than onto the compositor** — a re-declared buffer eases from zero and never converges, and a late import fails only the first frame. Both traps and the launch-seeding bug they hid are in [the PostFx README](../Core/Vixen.Rendering.PostFx/README.md) § Auto-exposure |
| **Terrain** — the heightfield kernel, the LOD quadtree with no cracks, the splat material's 4/8/12/16 slots, tiles and holes | ✅ | Core/Vixen.Terrain · Core/Vixen.Rendering.Terrain · `Raven/Library/Terrain` | [31](plan/31-terrain-grass-and-trees.md) T0–T4, T7, T8. The kernel opens no device — sculpt, paint, erosion, layer composition and the LOD descent are pure functions over arrays, which is why the no-crack and morph-continuity properties are unit tests rather than screenshots. Fully game-wired: node, factory, extraction system, atlas upload and mips, shadow casters and velocity, all spliced into a document by `TerrainFactory`. Proved by `Samples/03-PbrShowcase` — a committed multi-tile `.vxterrain` through a content build rather than editor machinery — and used by `Samples/13`. ⚠ **The placement rides the view matrix**, because `TerrainConstants` has no world transform: cull in the terrain's own space, or a terrain away from the origin culls the wrong nodes. Owed list in [the renderer's README](../Core/Vixen.Rendering.Terrain/README.md) |
| **Terrain collision** — the heightfield as a collider | 🟡 | Core/Vixen.Terrain.Physics · Editor/Vixen.Editor.Terrain.Physics | [31 § B1, § D10](plan/31-terrain-grass-and-trees.md). **The runtime half is done and reached**: `TerrainColliderSystem` is one assembly with two references, opted into by a game — `Samples/13` builds it, so a character stands on the ground instead of falling through. ⚠ **Nothing in any project produced a heightfield shape until 2026-08-12, and falling through terrain is not an error anywhere.** **The editor half is built and unfed**: `TerrainColliders` adapts `ITerrainColliders` and `TerrainModuleSession.BindColliders` takes one from `PluginServices`, but ⚠ `EditorApplication` publishes none and holds no `PhysicsScene` — play mode is a `WorldSnapshot` capture and restore, not a system graph — so the only caller in the tree is a test. ⚠ Both `Rebuild` overloads return `bool` where the interface returns `void`, and `false` means "never heard of that terrain": a wrapper that discards it succeeds at rebuilding nothing |
| **Foliage and grass** — instance cells, the two-stage GPU cull, the scatter ring, impostors | ✅ | Core/Vixen.Foliage · Core/Vixen.Rendering.Terrain · `Raven/Library/Terrain` | [31](plan/31-terrain-grass-and-trees.md) T5–T7. Two paths that look alike and are not: foliage is *placed* instances persisted beside the scene as a `.vxfol` and culled per instance by `FoliageCull.rvn`, while grass is *scattered* by `GrassScatter.rvn` into a ring of device buffers as cells enter range and re-culled every frame. Both were built CPU-first on purpose, because a cull and a scatter fail silently in both directions, and the seam test compares the CPU reference against the shader as sets at exact tolerance. ⚠ **Owed, narrow and named in [the renderer's README](../Core/Vixen.Rendering.Terrain/README.md)**: only the ground casts a shadow, `ImpostorCapturePass` has no caller outside its tests, and a volume's palette is session state |
| **Texture streaming** — mip tails through the page service | ✅ | Core/Vixen.Engine.Renderer · Core/Vixen.Rendering | [22](plan/22-virtualized-geometry.md) improvement 6. `TexturePagePool`'s pages are fixed byte-size slices of a KTX2 file's level data, and it brought no budget of its own: the queue, the ceiling, the LRU order, the pinning and `Rejections` are `PageResidency`'s, unchanged — which is the claim `IPageStore` was making. ⚠ **Five page kinds now share it** — geometry, virtual shadows, textures, terrain tiles and foliage cells — and the last two are consumers doc 22 never anticipated. See [Streaming texture mip tails](guide/rendering/texture-streaming.md) |
| **Virtualized geometry** — cluster DAG, pages, hierarchical traversal, visibility buffer, material resolve, software raster | ✅ | `Vixen.Rendering.VirtualGeometry` · Core/Vixen.Rendering | [22](plan/22-virtualized-geometry.md) phases 0–6. Import → pages → residency → traversal → hardware raster → bin → shade runs on a device; phase 6's compute raster is the accelerator beside it, with clusters routed by projected screen size, the two rasters filling opposite ends of one visible list, and a 64-bit `atomicMax` resolving depth without an attachment. **Off unless a host sets a threshold** — the crossover is hardware's to say — gated on `HasInt64Atomics`, which forces the threshold to zero rather than branching anywhere else. ⚠ **The device half of phase 6's exit criterion has not run**: MoltenVK reports `shaderBufferInt64Atomics = false`, so the software-vs-hardware raster golden skips on the only Vulkan device in reach, and only the host half is asserted. Phase 7 (virtual shadow maps) is its own project |
| Deferred pipeline — GBuffer, shading-model dispatch, forward routing, decals | 🟡 | `Raven/Library/Pipeline` | Phase 10; cut-list #6. **The shader half exists and is gated**: `GBufferPass.rvn` (the same shader as `ForwardPlus` down to `surface.Compute`, diverging into three targets), `Deferred.rvn` (a fullscreen lighting pass with no `compose` slot, by design) and `GBuffer.rvn` holding the layout exactly once — all three in the library tree test, on both backends. The compositor already knows the stage shape: `RenderStage.ShaderComposes` and `PassAsset.ComposeFromMaterial` exist for it. **Nothing renders through it** — no node, no shading-model-ID dispatch, no automatic forward routing for non-representable materials, and no decals |
| Volumetric fog (froxel) | ✅ | Raven/Library/PostFx · Core/Vixen.Rendering.PostFx | [plan/06 § P2](plan/06-rendering-pipeline.md), which corrects its own "shares the clustering grid" premise: what is shared is the Z distribution and `ClusterGrid.Of`, not the grid. `VolumetricFog.rvn` is three compute passes behind one `Pass` permutation — inject the medium, light it, march a column front to back — each dispatch sized from what the *document* declared; `Fog.rvn`'s `Volumetric` permutation composites the result so the analytic falloff carries on beyond the volume's far plane rather than being deleted. The passes sit between `Lamps` and `Main`, the composite takes the `Air` seat after TAA, and the march is restated on the CPU against Beer–Lambert at four slice counts so the slicing neither adds nor loses energy. **All three items this row listed as owed have since landed** — `Shadowed` (sun in-scatter through the cascade atlas), `UseClusteredLights` (the per-froxel punctual loop) and `Reproject` (temporal). Remaining gap: the punctual in-scatter is **unshadowed**, because the punctual shadow atlas is a composed feature of the scene pass. ⚠ The far plane is the volume's own and never the camera's, and ⚠ in a local `PostProcessSettings` override `null` is "no opinion" while `0` is "no fog *here*" — both recorded in doc 06 § P2 |
| Contact shadows, light shafts, SSS blur, upscaler + FSR1 | ⬜ | — | Phase 10. Nothing in the tree names a contact shadow, a light shaft, an `IUpscaler` or FSR. Subsurface is the partial exception and is **not** this row: `SubsurfaceShading` and `Shading/Subsurface.rvn` ship the forward per-light half (wrapped diffuse, back-lit transmission) *and* the Burley profile a separable screen-space blur would sample — what is missing is the blur pass |
| Mesh shaders / meshlet culling behind capability flags | ⬜ | — | Phase 10; [22](plan/22-virtualized-geometry.md) is now the plan. `GraphicsDeviceFeatures.HasMeshShaders` is detected on Vulkan (`VK_EXT_mesh_shader` / `VK_NV_mesh_shader`) and refused elsewhere, but nothing outside the backends' own tests reads it, and `VirtualGeometry`'s `Meshlet*` types are the cluster DAG's rather than a mesh-shader stage |
| Golden-image fixture suite | ✅ | Platform/Vixen.Graphics.Golden.Tests | Six kinds — the backend at its simplest, one fixture per state bit a backend can silently ignore, the compositor, the UI, debug draw, and a whole frame per quality tier — plus device fixtures for clustered shading, GI, screen probes, surface cache, water, ray tracing and virtual geometry: 49 committed references across ~55 test classes. It caught two engine bugs **nothing but a picture could see** (a composed material parameter's qualified name depending on lowering order; one Raven struct used in both a uniform block and a storage buffer collapsing to one MSL type), and a third — an empty frame since the bindless-material merges — that **closed itself**, recorded rather than deleted because a defect that fixed itself can come back. ⚠ **The Linux leg is the only one that runs these**: macOS and Windows runners have no Vulkan driver and skip every fixture, so a tolerance impossible to meet cross-driver looks green to everyone but lavapipe. [The README](../Platform/Vixen.Graphics.Golden.Tests/README.md) carries the container recipe |
| `Samples/03-PbrShowcase` | ✅ | Samples/03-PbrShowcase | Converted to the standard frame: seven knobs stand where the raw-RHI passes were, the grid casts through the cascades, a baked Preetham sky is the image-based ambient, and the occlusion under each sphere is a real march over exact analytic distances. Also the terrain stack's proving ground — a multi-tile `.vxterrain` and a `.vxgrass` rule reaching the frame through the `extensions: afterOpaque:` splice, from a scene component and a content build. That run found the `.vxgrass` chunk unreachable at runtime, the terrain pipeline layouts placing set 2 at index 0, and the Raven fragment-stage `SV_Position` gap |
| **`Vixen.Video`** — managed WebM demuxer, codec seam, player with an audio-driven clock, YUV planes + conversion coefficients | ✅ | Core/Vixen.Video | 144 tests, and `Samples/11-VideoPlayback` drives it. Doc 06 § Other renderables. Landed far ahead of its Phase 10 slot |
| **`Vixen.Video.Codecs`** (Opus over loose WebM packets) | ✅ | Core/Vixen.Video.Codecs | Split so a game with an uncompressed logo sting links no Concentus |
| **`Vixen.Video.Rendering`** — one pipeline, three plane bindings, `VideoRenderTarget` | ✅ | Core/Vixen.Video.Rendering | `VideoRenderer` converts the planes to an ordinary colour texture, and `Samples/11` uses it. ⚠ `VideoRenderTarget` and `VideoRenderFeature` — the "video into the UI" and ECS halves — have no caller outside this project's own tests |
| Video: MP4; a **material** (video lit on a mesh); frame-accurate seek; audio-track choice; subtitles; 10-bit / BT.2020 | ⬜ | — | MP4 is additive behind `IVideoStreamDecoder`, and `VideoImporter` claims-and-refuses it rather than half-reading it; the material is `MaterialRenderFeature`'s and Raven's. `VideoPlayer.Seek` exists but is the demuxer's keyframe seek, and `VideoPixelLayout` stops at 8 bits by construction |
| **`Vixen.Xr`** — session state machine, per-eye poses, asymmetric projections, runtime-owned swapchains, action input, ECS bridge, simulated headset | ✅ | Core/Vixen.Xr | All of it runs on a machine with no headset. ⚠ No sample or engine host drives it — the row below is why |
| **`Vixen.Xr.OpenXR`** — the three desktops and Android | ✅ | Platform/Vixen.Xr.OpenXR | The Vulkan instance/device/GPU are the *runtime's* choice, and the API shape makes that order impossible to get wrong |
| XR: a render feature; single-pass multiview; hand/eye tracking, passthrough, anchors | ⬜ | — | Two passes work today. Multiview's hook is `XrSwapchainDescription.ArrayLayers`, honoured by both backends; the `VK_KHR_multiview` half is `Vixen.Graphics`'s and is missing. Passthrough is the partial exception — `XrSessionOptions.PreferPassthrough` picks an alpha-blend or additive environment blend mode when the runtime offers one, and `OpenXrSession.IsPassthrough` reports it. Nothing names hand tracking, eye tracking or an anchor |
| Shader reflection: vertex input locations + push-constant stage coverage | ✅ | Core/Vixen.Shaders | A binding index is declaration order, so a literal in C# survives a renumbering and a generated constant does not — and the failure is silent CPU fallback, every frame |
## 1.10 Gameplay subsystems

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Physics` (Jolt 2.22.0) — bodies, shapes, constraints, character, queries, triggers, layers, CCD, ECS sync, debug draw, determinism gate | ✅ | Core/Vixen.Physics | Two binding bugs pinned by tests — [README](../Core/Vixen.Physics/README.md) § Two bugs in the binding |
| Physics on **iOS** | ⛔ | — | `JoltPhysics.Native` carries win/linux/osx/Android and **no iOS slice**; iOS is NativeAOT-only where a dylib would not load anyway. Needs a static `libjoltc.a` pinned the way MoltenVK is in `../build/native-dependencies.json`. Reason is recorded beside the `JoltPhysicsSharp` pin in `../Directory.Packages.props` |
| Per-pair collision suppression; vehicles, ragdolls, soft bodies; double precision | ⬜ | — | Out of Phase 8 scope. Layer-pair filtering exists (`PhysicsLayers`); `Foundation.Init(doublePrecision: false)` is the pin |
| `Vixen.Audio` — software mixer, buses, sends, sidechains, 14 effects, streaming, ECS, events, parameters, interactive music, capture, HRTF, loudness | ✅ | Core/Vixen.Audio | Far beyond the line the roadmap asked for; nothing structural is owed |
| `Vixen.Audio.Codecs` (Vorbis, Opus, ADPCM) | ✅ | Core/Vixen.Audio.Codecs | Pure managed, rooted in the AOT probe |
| `Vixen.Audio.Physics` (Jolt occlusion), reverb zones | ✅ | Core/Vixen.Audio.Physics | Zones need no physics; occlusion does |
| Backends: OpenAL ✅, WebAudio ✅ | ✅ | Platform/Vixen.Audio.Backend.* | |
| Measured HRTF sets | ⬜ | — | `HrtfPanner` is a synthetic model and says so; measured impulse-response sets are content |
| `Vixen.Animation` — skeletal playback, 1D/2D blend trees, layers + masks, state machine, two-bone/look-at/foot IK, root motion, events, GPU skinning, key reduction | ✅ | Core/Vixen.Animation | Benchmarked; `ParallelThreshold` = 32 from measurement |
| A runtime path for `.vxanim` | ✅ | Editor/Vixen.Editor.Assets | `AnimationClipImporter` compiles an authored clip to `AnimationClipContent` and a game loads one by address. `AnimationClipCache.Get` pairs it with a skeleton once per (clip, rig); `TrySample` reads a named target with no rig at all, which is what `Samples/13`'s box character uses. The sidecar's `extensions` block round-trips markup this build does not understand |
| Move sets — flat catalogue, scored query, bake-time overlay | ✅ | Core/Vixen.Animation | Doc [34](plan/34-move-sets-and-pose-constraints.md) P1. `MoveSet` is a flat array with a contiguous scan table; `QueryMoveSelector` filters on required facets, scores preferences and proximity, breaks ties on a stable `MoveKey` hash and retimes within the rate range. `MoveSetMotion` is a `Motion`, so layers, masks and events are unchanged. ⚠ **Owed: 5.9 µs for a 500-entry query with no required facet misses the plan's 5 µs by 18 %** (3.4 µs with one; zero-allocation either way, `MoveSelectionBenchmarks` records both) |
| Move transitions, phase sync and partial-body sets | ✅ | Core/Vixen.Animation | Doc [34](plan/34-move-sets-and-pose-constraints.md) P2. `ITransitionPolicy` fronts an ordered rule list, first match wins, and `RuleFor` says which rule decided a pair. `MoveSetMotion` crossfades with easing and an optional bone mask and carries the phase across — `SyncMode.ClosestFoot` aligns **contacts**, not fractions; `PhaseSource`/`IPhaseSource` lets a masked upper-body set hang off the layer below. Both exit criteria assert continuity of the *contact* clock over a walk↔run↔sprint ladder |
| Pose constraints — the stage, the four goal kinds, arbitration, and the frame's pre-evaluation pass | ✅ | Core/Vixen.Animation | Doc [34](plan/34-move-sets-and-pose-constraints.md) P3. `ConstraintStack` is an `IPoseProcessor`: position, orientation, aim and distance goals, each with a region and an **additive** form, resolved through `IConstraintFrame` against named `ConstraintBindings`, arbitrated by `IConstraintArbiter` and applied through `IChainSolver` onto `TwoBoneIk`. Residuals live on the *instance* — a clip's goal object is shared by every character playing it. D19's `IConstraintScheduler.PlanPreEvaluation`/`PlanPose` stage measures indistinguishable from a build without it. Traps and limits in [README](../Core/Vixen.Animation/README.md) § Traps. **Owed:** the arbiter has no skeleton-authored joint limits below the ones added later, and a chain longer than two bones is solved over its last two with the shortfall reported |
| Proxy shapes, surface coordinates and adapted sockets — one authored contact on any body | ✅ | Core/Vixen.Animation · Editor/Vixen.Editor.Assets | Doc [34](plan/34-move-sets-and-pose-constraints.md) P4, and its central claim demonstrated: one authored contact, three bodies of different proportions, the hand on the same spot on all three — asserted by projecting where the hand landed back onto the body. Seven primitives with a normalised parameterisation per kind, `ShapeGeometry.Project`/`Evaluate` exact inverses, `SurfaceFrame` as the sixth `IConstraintFrame`, `IProxyShapePoser` with lazy posing, and `AttachmentSocket` — a socket whose bone offset is itself solved, so one prop is gripped correctly by three hands. `.vxshapevocab`/`.vxproxyshapes` are real files whose importer fails naming the set and the missing shape. Measured: 8 shapes vs 120 on a body costs 902 vs 909 µs for 100 characters. ⚠ Ellipsoid parameterisation and basis order: [README](../Core/Vixen.Animation/README.md) § Traps |
| Trajectories — a goal that moves through a clip | ✅ | Core/Vixen.Animation | Doc [34](plan/34-move-sets-and-pose-constraints.md) P5. `TrajectoryFrame` wraps any other frame, so "this goal moves" is orthogonal to "this goal is on a socket". Two decimated polylines plus an optional surface path and rotation curve, RDP against an authored tolerance reported through `TrajectoryReport`. Exit met: a sliding-hand contact reproduces on a rail of twice the radius and nearly twice the length, checked at all 121 captured samples rather than at the surviving keys. ⚠ The surface path is what makes it portable; `U` unwrapping, the live frame resolve and per-goal phase are in [README](../Core/Vixen.Animation/README.md) § Traps |
| Root placement, camera framing and the LOD governor | ✅ | Core/Vixen.Animation · Core/Vixen.Engine | Doc [34](plan/34-move-sets-and-pose-constraints.md) P6. `RigidBodySolver` is one solve for the two bodies with no skeleton — a character's placement and a camera — so the camera inherits regions, additive goals, weights and priority from the pose arbiter. `root` goals decide where the character should stand, before the pose and excluded from it; the result is a suggestion the controller may refuse, as `LastRootMotion` is. `ScreenFrame` turns a framing constraint into an ordinary position goal. D22's three knobs are complete and `ConstraintGovernor` walks a bounded ladder reporting what it gave up — measured, a 150-of-400 budget left 35.6 % of the constraint time against 37.5 % predicted. ⚠ A satisfied region goal takes no share, and the governor reserves the floor for everyone still waiting: [README](../Core/Vixen.Animation/README.md) |
| Constraint authoring — the track, the generated inspector, templates, the ladder, proposals | ✅ | Core/Vixen.Animation · Editor/Vixen.Editor.Assets · Editor/Vixen.Editor.AssetEditors | Doc [34](plan/34-move-sets-and-pose-constraints.md) P7, which closed P3's authoring gap. A `.vxanim` carries a `constraints:` block that round-trips, is checked at import and bakes against whichever rig plays it. `GoalKindSchema` generates the inspector, and two tests — a field on no panel and not deliberately hidden, a schema field with no accessor — are the whole mechanism against drift. `.vxpriorities` makes a priority a *name*; `.vxconstraints` templates carry a revision and re-apply **diff first**, so hand edits are called out separately. `ConstraintProposals` proposes contacts from proximity, never applied silently, confidence a *product* of near, still and long. ⚠ The overlap audit exempts only adjacent joints: [README](../Core/Vixen.Animation/README.md) § Traps |
| The four authoring views, and a timeline that can hold an interval | ✅ | Core/Vixen.Ui.Controls.Advanced · Editor/Vixen.Editor.AssetEditors | The views P7 first shipped without. **`Timeline` gained spans** — a track is instants or intervals and never both, an interval draws its ease ramps, and dragging an end past the other wraps the span round the loop, which is the only gesture that authors a contact across the loop point. `ConstraintGizmos` draws the last solve — effector, resolved frame, chain, anchor shape, miss graded green to red — into `DebugDraw`, so it is testable with no window. The move set table's filter box is the runtime's own query handed to the same scorer. The proxy shape editor has the mirror, the generated coarse set with per-shape override, and the three audits; the sequencer gained the authoring-context role. ⚠ A mirror swaps the side of the *joint*, and a clip's authoring-context reference is authoring-time only: [README](../Core/Vixen.Animation/README.md) § Traps |
| The variation harness | ✅ | Core/Vixen.Animation · Editor/Vixen.Editor.Assets · Editor/Vixen.Editor.AssetEditors | Doc [34](plan/34-move-sets-and-pose-constraints.md) P8 — the answer to its first risk, that the cost of constraints is knowing when to stop. Bodies, props, ground slope and height as axes that multiply, measured four ways because four things go wrong: a goal misses, a contact sinks in, a hand snaps, a limb runs out of arm. Exit met in one test where **only the small body fails**. The report is a matrix panel whose worst cell hands back the configuration rebuilt. ⚠ Residual grading and snap detection: [README](../Core/Vixen.Animation/README.md) § Knowing when a clip is finished. **Owed: the CI gate is a library entry point and a declared `.vxharness`, not a CLI verb** — `vixen animation check` additionally needs the rig resolved through the model import path |
| Doc 34 wired through the editor and loadable by a game | ✅ | Core/Vixen.Animation · Core/Vixen.Rendering · Editor/Vixen.Editor.AssetEditors | The registration half: `.vxharness` has a document and a factory so the matrix opens and **Run** fills it; `AddAnimation`/`AddConstraintGizmos` are the one-line registration physics has had all along, because `EngineLoop` cannot include the animation passes by default when the dependency runs one way only; the clip editor has **Propose Contacts** with an Add per row and no accept-all. `ProxyShapeCache` and `MoveSetCache` are what `AnimationClipCache` is for a clip — before them a game could import a `.vxmoveset` and had no way to turn one into a `MoveSet`. **Joint limits exist** (swing cone + twist range per joint, relative to bind pose) and the arbiter clamps after the solve, so the harness reports *limits hit* beside *reach exhausted*. The vocabulary has a panel reporting `ShapeVocabularyContent.Problems` — the importer's own rules, so panel and build cannot disagree. A proxy shape drags in a hosted `SceneViewport` through `ProxyShapeGizmoTarget`, with `Records` as the one new seam so whoever supplies the targets owns the undo entry. ⚠ The clamp cuts rather than redistributes, and `Limited` is a flag: [README](../Core/Vixen.Animation/README.md) § Traps |
| Doc 34's seams, implemented twice | ✅ | Core/Vixen.Animation · Editor/Vixen.Editor.Assets | Doc [34](plan/34-move-sets-and-pose-constraints.md) P9, less the sample. Every interface in its Part 4 has a second implementation differing in *shape* — a table-driven selector that never calls the scorer, an alphabetical scorer, a wheeled gait model with signed speed, a delegate transition policy — and `SeamTests` asks the assemblies rather than trusting review. `IClipMetadataExtension` was the seam the plan named and never built: an unrecognised `extensions` block used to be carried silently, so a typo survived import and did nothing; a registered kind is now checked and an unread one reported as information. ⚠ **The sample is not built and it is blocked on art, not engineering** — both halves need a skeleton and `Samples/13`'s character is seven boxes with none, which is why its `CharacterAnimation` samples clips by target name |
| The animation documents actually connected to a project | ✅ | Core/Vixen.Animation · Editor/Vixen.Editor.App | The last mile: until it was walked the panels were inert in the shipped editor — the shape viewport drew nothing, **Run** had no project, **Propose Contacts** had no scene. `EditorAnimation` is the one place that turns an asset path into a rig, a shape set, a vocabulary, a ladder, a clip or a move set, and `EditorApplication.Bound` sets the hooks per *document* — the document's twin of `Joined`, separate because a view is rebuilt on every dock while the document behind it is built once. ⚠ A `.vxproxyshapes` now names its rig (authoring-time only), the rig is read from the model's *source* with its own import settings, effectors are derived from the body's own shapes, and rigs are cached against write time — all four with their reasons in [README](../Core/Vixen.Animation/README.md) § Traps |
| `Vixen.Editor.AnimationGraph` | ✅ | Editor/Vixen.Editor.AnimationGraph | Cut-list #7 built rather than cut. An authored state machine as a serialisable document plus the compiler that turns it into `Vixen.Animation`'s `AnimationStateMachine` and `AnimationLayer`s; registered through `StandardEditors`. Deliberately **not** on `Vixen.Editor.NodeGraph` — its README says why |
| Ragdoll integration | ⬜ | — | No implementation anywhere; lands with the animation/physics join. `Vixen.Net.Animation` already assumes one exists |
| `Vixen.Input` — devices, `InputControlPath`, actions, maps, processors, interactions, `.vxinput`, generated accessors, rebinding | ✅ | Core/Vixen.Input | |
| **Players and possession** — `PlayerController`, `ControlRotation`, `MoveIntent`, the `Possessing`/`PossessedBy` pair, `Player`, `PlayerInputSystem`, `PossessionSystem`, `ActionPlayerInput` | ✅ | Core/Vixen.Engine | [29](plan/29-players-and-possession.md) **P0**, and reached by `Samples/13-ThirdPersonShooter`'s `PlayerRig`. Unreal's decomposition: the controller is the player and outlives the pawn, so a respawn is one `Possess` call and the aim survives it. `MoveIntent` is the seam that lets the movement half live in `Vixen.Physics` and the networked half in `Vixen.Net.Engine` without either referencing the other. The camera half needed no new code — possession writes the bound shot's `CameraTargets` and doc 26's director blends. 33 tests. ⚠ `ActionPlayerInput` binds actions **by name** and throws at construction naming the map: [README](../Core/Vixen.Engine/README.md) § Players and possession |
| **Character movement** — `CharacterMovement`, `CharacterState`, `CharacterMotion`, `CharacterMovementSystem`, the `PhysicsScene` character bridge | ✅ | Core/Vixen.Physics | [29](plan/29-players-and-possession.md) **P1**. Walking, falling and flying; coyote time, jump buffering and a variable-height **clamp**; crouch through `TrySetShape`, so a low ceiling is a refusal needing no special case. `CharacterMotion` is a pure function of settings, state, intent and ground, so the whole rule set is tested with **no Jolt at all** — which is what makes P3's determinism gate cheap enough to run everywhere. 24 tests. ⚠ `Position` is the shape's *centre*, and the achieved velocity is measured from the displacement because `CharacterVirtual` does not report it: [README](../Core/Vixen.Physics/README.md) § Character movement |
| Per-character slope and step limits | ⬜ | — | `CharacterControllerSettings` has both and Jolt fixes them at creation, so a component field means detecting the edit and recreating the controller. Deferred rather than half-done — [README](../Core/Vixen.Physics/README.md) § Character movement |
| **Player camera rigs** — `PlayerCameras.FirstPerson` / `.ThirdPerson`, `PlayerCamera`, the aim → `PovAim` / `OrbitBody` wire | ✅ | Core/Vixen.Engine | [29](plan/29-players-and-possession.md) **P2**. Each builds a `Camera` + `CameraDirector` on the player's channel and a shot on the same one; `PossessionSystem` points it and feeds it the aim, so a death, a respawn and a pawn swap need no camera code. 12 tests. ⚠ Both rigs are steered by `ControlRotation`, so neither can be assembled from outside the assembly — doc 29's P-R1 originally said the opposite — and the orbit's pitch is **negated** against the aim's: [README](../Core/Vixen.Engine/README.md) § Player camera rigs |
| Split-screen **rendering** | ⛔ | — | Simulates and does not draw. Two players get two directors, two sets of shots and two independent cameras, but `CameraExtractionSystem` fills exactly one `RenderView` from the lowest `Camera.Order`, and `RenderView` still carries no viewport rectangle. Needs a view per player, a rect on each and a compositor node per view — [06](plan/06-rendering-pipeline.md)'s work. `PlayerCameras` sets each camera's order from its channel meanwhile, so seat zero is on screen and switching who is watched is a component write |
| **Networked players** — `PlayerMoveInput`, `PlayerPawn`, `PlayerSpawner`, `LocalPlayerSystem`, `PredictedPlayerMovement` | ✅ | Core/Vixen.Net.Engine · Core/Vixen.Net.Physics | [29](plan/29-players-and-possession.md) **P3**. `PlayerSpawner` is `AGameModeBase` minus the god object: join, respawn, leave, with the pawn spawned **owned**, which is what makes the client's copy an autonomous proxy. A client is never told which entity is its pawn — a `PlayerPawn` tag rides on the prefab, so "owned by me, possessed by nothing" is a query over already-replicated state with none of a message's ordering cases. 34 tests, the load-bearing one being `MispredictionCountIsZeroOverALosslessRun`: a client and a server stepping the same decoded inputs over 180 ticks of real Jolt character movement, zero rollbacks |
| **Rollback smoothing, wired** — `PredictionSmoothingSystem`, `PredictionSmoothing`, `PlayerInputQuantizeSystem` | ✅ | Core/Vixen.Net.Engine | `PredictionSmoother` had been built and tested in `Vixen.Net` and **nothing used it**, so a corrected character teleported and took the camera with it. ⚠ The offset goes on a visual child, never the body — writing the body would be adopted by `PhysicsScene` as a teleport and the hidden error would become a real one. `PlayerInputQuantizeSystem` turns "remember to call `Round`" from a sentence in three documents into a registered system |
| Zero-allocation cover for the player path | ✅ | Core/Vixen.Engine.Tests · Core/Vixen.Physics.Tests · Core/Vixen.Net.Physics.Tests | Input sampling, the possession pass, aim, intent, the character bridge, the motion rule and a **whole predicted tick including the native world step** all measure zero bytes per frame under `Measured` |
| Players: the end-to-end project (P4) | ✅ | Samples/13-ThirdPersonShooter | [29](plan/29-players-and-possession.md) **P4** landed as `Samples/13-ThirdPersonShooter` — a real project rather than a sample (`.vxproj`, `Assets/`, `VixenApp.Run<T>`) with a headless run asserting the player is `Walking` on collision the level authored. Nine engine failures came out of it, seven of which produced a working program with a wrong answer; three were a `[ModuleInitializer]` that never ran, because "referenced" and "touched" are different facts. The *tuned* rigs still belong in `Samples/05-PlatformerGame` (below) |
| ⚠ Three ways to write a prediction that reports agreement while the ends drift | ✅ | Core/Vixen.Net.Physics | A lesson rather than a feature, recorded in full in [README](../Core/Vixen.Net.Physics/README.md) § the prediction table: the tick must call `World.AdvanceVersion()`, must publish `NetworkTransform` inside itself, and the rollback must reach into Jolt. All three produce a `MispredictionCount` of zero |
| Action-map editor + input debug panel | 🟡 | Editor/Vixen.Editor.AssetEditors | **The action-map editor is built and registered**: `InputActionsDocument` + `InputActionsView` open a `.vxinput` through `StandardEditors`, with maps, actions, bindings, control schemes and press-the-key-you-want listening as a mode rather than a modal. ⚠ It records a *control*, not a chord — a modifier is a composite part, and a chord written into a binding path is one `InputControlPath.TryParse` refuses. **Owed: the input debug panel** — nothing shows live device state |
| Sensors, pen/stylus, MIDI, custom HID | ⛔ | — | `Vixen.Platform` reports none of the four |
| `Vixen.Navigation` — voxel bake, tiled mesh, A\* + funnel, crowd + RVO, off-mesh links, watershed, height detail, dynamic obstacles, sliced/jobbed queries | ✅ | Core/Vixen.Navigation | Managed, no Recast/Detour binding. 40 tests, zero steady-state allocation; consumed by `Vixen.Engine`, `Vixen.Ai.Nodes` and `Vixen.Xr` |
| Navmesh baked from a **compiled scene** | ⛔ | — | ⚠ **Blocked on a decision, not on the compiled scene format** (which exists — see §1.6). `NavMeshImporter` wants `(path, transform)` pairs; a scene gives an `AssetReference` GUID, and `ImportContext` can only *declare* a GUID dependency — resolving one to a path is the asset database's job and an importer runs beside it rather than inside it. Needs either a resolution service on `ImportContext` or a collision-source component that names a path |
| `Samples/05-PlatformerGame` | ⬜ | — | Phase 8's exit criterion. Does not exist; where the *tuned* player rigs belong |
| **AI: the substrate** — the compiled blackboard, the action surface, per-agent memory, the governor, the debug record | ✅ | Core/Vixen.Ai | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P0, and the layer move that document exists to argue for: `Vixen.Ai` references `Vixen.Core`, `Vixen.Core.Mathematics`, `Vixen.Core.Threading` and `Vixen.Ecs` and nothing else, so a tree can run a shopkeeper without linking a combat package — `AiLayeringTests` asserts both halves. `BlackboardLayout` compiles `(name, type)` rows into indices and aligned byte ranges, so a key reference is a `ushort` and a read is a span slice. Ten thousand agents step in a frame that allocates zero bytes; the round-robin schedule is a pure function of tick and index. 71 tests. ⚠ The four traps — a version bumps only when the value actually changed, an action never owns its state, the memory pool is paged rather than one growable arena, and a governed agent gets the time since *it* last ticked — are in [README](../Core/Vixen.Ai/README.md) with their symptoms |
| **AI: behaviour trees, runtime** — the compiler, the stepper, the node library, subtrees | ✅ | Core/Vixen.Ai | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P1. A `.vxbt` compiles to a flat array of nodes in depth-first pre-order, so **an index is a priority**, a subtree is a contiguous range, and the abort test is two integer comparisons. Measured both ways in one test: a thousand agents on a ten-node tree visit zero nodes across sixty settled frames, against 60 000 for a per-frame traversal of the one-node tree beside it, and the same population allocates zero bytes a frame. 144 tests. ⚠ **A decorator reaches the siblings under its own parent composite and no further** — Unity's scope rule, not Unreal's "lowest-index runnable node", and the property test over 200 generated trees requires every disagreement with a from-scratch walk to be explained by it. A static subtree is spliced at compile time; a step descends until something is running. [README](../Core/Vixen.Ai/README.md), doc 37 § P1 |
| **AI: the node editor** — the `.vxbt` document, the model, the canvas, the importer | ✅ | Core/Vixen.Ai · Editor/Vixen.Editor.Ai · Editor/Vixen.Editor.AssetEditors · Editor/Vixen.Editor.Assets | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P2, and its exit criterion as one test: **thirty nodes put there by thirty gestures — saved, reopened, and asserted equal both as text and as a walk.** A `.vxbt` is a type name and a bag of named strings rather than a polymorphic tree, because binding one needs reflection at load and ADR-002 rules that out; `BehaviorNodeSchema` lives in `Vixen.Ai` rather than the editor, because a game loading a tree at run time needs the same table, and it carries the label, category, tooltips and defaults the inspector is generated from. `BehaviorTreeEditorFactory` is registered in `StandardEditors`, so the editor really opens one. ⚠ **A node with no schema entry is a node nobody can use** — `Composite`, `ConditionalLoop` and `RunUtilitySet` each had a class, tests and a ✅ and no entry, so the compiler refused them and the search popup never offered them; `AuthorableNodeTests` now asserts the plan's list against the shipped table. ⚠ Undo is a snapshot and the first `Do` must install nothing. 23 tests. [Editor/Vixen.Editor.Ai](../Editor/Vixen.Editor.Ai/README.md) |
| **AI: perception** — the five senses, the perceived list, the three bounds | ✅ | Core/Vixen.Ai.Perception | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P3, and both exit criteria as numbers. **Five hundred listeners against five hundred sources on one tick: 7 960 sources examined in 2.10 ms with the broad phase, against 250 000 in 5.71 ms without it.** A second assembly rather than a folder in `Vixen.Ai`, because perception needs `Vixen.Engine` and `Vixen.Physics` and a game that wants trees without a solver should link `Vixen.Ai` and stop. 38 tests. ⚠ Four traps, all recorded in [README](../Core/Vixen.Ai.Perception/README.md): the lose-sight radius is a second, larger radius and leaving it out makes targets flicker (five changes of mind against one); an event is consumed by *sequence number* and never by clock; the broad phase is over the stimuli sources rather than Jolt's bodies, and is two-dimensional; the team relay copies **one target per ally**, since whole lists measured 6.3 ms of an 8.8 ms frame |
| **AI: nodes over the world** — movement, patrol, rotation, path existence, animation, sound | ✅ | Core/Vixen.Ai.Nodes | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P4, and its exit criterion as one test: **a guard patrols a baked navmesh, notices the player, chases and gives up — no window, asserting positions** — with every part authored rather than driven, so a failure is a failure of the whole chain, sense to key to abort to node to crowd. The widest reference list in `Core/` — navigation, animation and audio at once — and `NodeLayeringTests` asserts the other half: **nothing depends on it.** 30 tests. ⚠ In [README](../Core/Vixen.Ai.Nodes/README.md): a move task keeps its issued destination (rewriting `NavigationDestination` every tick is a full path search per agent per frame), aborting stops the agent where it stands, and there is no hierarchical path query — a node-budgeted search stands in for one |
| **AI: utility** — considerations, six curves, four selectors, inertia, the `.vxutility` asset and its editor | ✅ | Core/Vixen.Ai · Editor/Vixen.Editor.Ai · Editor/Vixen.Editor.AssetEditors | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P5, both exit criteria measured: **two actions within 2 % of each other over sixty seconds — 0 switches with the defaults, 120 with inertia disabled**, which is why the shipped defaults carry a commitment bonus and a 0.2 s decision interval. A utility action names its task out of `BehaviorNodeSchema` and resolves through the same registry, so two files that both say `Wait(2)` share one action whether one is a tree and the other a set; `AiAgent.Tree` became `AiAgent.Asset` accordingly. `UtilitySetEditorFactory` is registered in `StandardEditors`; the editor is a two-pane table over `CurveEditor` rather than a canvas, because a utility set has no edges. 27 tests here and 8 over the editor. ⚠ In [README](../Core/Vixen.Ai/README.md): compensation means the **count is irrelevant** rather than "a neutral consideration changes nothing", one zero is a veto, inertia lives on the set and not the selector, and the bucketed selector is a *rank* |
| **AI: GOAP** — world keys, conditions and effects, the bounded search, the queue, the derived viewer | ✅ | Core/Vixen.Ai · Core/Vixen.Ai.Nodes · Editor/Vixen.Editor.Ai · Editor/Vixen.Editor.AssetEditors | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P6, all three exit criteria measured: the **pear test** plans `pick-up-pear` then `eat-pear` backwards from the goal; a 24-action tangle fails with **`BudgetExhausted` naming the goal** after exactly its 200 nodes; **64 agents on a 40-action domain are answered four per step, sixteen steps**. A search reads a snapshot and never the world — keys projected, targets sensed and costs computed at *submit*, on the agent's own thread — which is what makes a resolve a job. `GoapDomainEditorFactory` is registered in `StandardEditors`; the editor is three tables beside a graph nobody authors, because a GOAP graph's edges are *computed* from which effects satisfy which conditions. 23 tests here and 8 over the editor. ⚠ In [README](../Core/Vixen.Ai/README.md) and doc 37 § P6: the matching rule is a direction and nothing else, a plan is a chain with only its head committed, an action may not appear twice in one chain, and **a planner that has chosen nothing must run nothing** (`AiAgent.Action` is zero until something sets it) |
| **AI: the debugger** — the keyed overlay, breakpoints, the diagnosis over the log, the remote channel, the editor panel | 🟡 | Core/Vixen.Ai · Core/Vixen.Ai.Diagnostics · Core/Vixen.Ai.Perception · Editor/Vixen.Editor.Ai · Editor/Vixen.Editor.AssetEditors | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P7 and its live half. Both exit criteria measured: an agent with its inertia off is reported as `Flapping` from a recorder the world has already gone out of scope around, and **the overlay's whole surface is asserted against a bare `DebugDraw` with no device anywhere in the file**. The three planners agree on one shape — a name, a reading, and whether it is the live one — so the overlay draws one kind of line, the panel builds one kind of table and the wire writes one kind of record; `BehaviorTreeProjection.Live` tints the canvas in four accents (an **aborted** node records as failed), the utility bars follow an agent's own scores, and the GOAP viewer gives every condition a verdict in **three** states, because "nobody is running this domain" drawn as false would say every condition was failing. All of it is off until a panel asks. 14 tests here, 9 over the overlay, 6 over the panel. **Reached, in task #162.** `AssetEditorsModule` registers the panel as `ai-debugger` through `context.AddPanel`, holding the model as a module field because a panel's factory runs again on every reopen; both of its buttons were built by `OnCreated` and wired to nothing, and now are. `AiOverlaySystem` is constructed by `Samples/15-AiVillage`, which is also the first `Samples/` project to reference `Vixen.Ai` at all. ⚠ **Docking the view for the first time found that it had no stylesheet rules whatever** — its body measured 740 px inside a 313 px panel and drew its own toolbar over the scene viewport — which is P7's own `Button.Text` finding one layer out: a control nobody laid out is a control nobody styled. ⚠ **And reading a *tree* agent through the surface found that `AiSnapshots.Take` filled `Snapshot.Action` from `AiAgent.Action` for every planner**, which `AiSystem.Advance` never writes for a behaviour tree — so the overlay's and the panel's headline "what is it doing" was `NameOf(0)` for every tree agent alive. Doc 37 § P6's trap in reporting form; fixed to read the live leaf and to say `Symbol.None` between two tasks, with a regression that registers a decoy first (the existing tree-snapshot test registers *one* action, so index zero was right by accident, and P7's overlay test reads its readout off a utility agent). 6 tests over the panel through `EditorSession`. **Owed: `BehaviorTreeView.Follow` / `UtilitySetView.Follow` / `GoapDomainView.Follow` still have no non-test caller** — the live canvas tinting wants the debugger's model handed to a freshly-opened asset editor, and `EditorApplication.Joined` is the seam; and the editor still owns no `AiSystem`, so the panel is live only for a host that publishes one into `PluginServices`. ⚠ A debugger must not move the bug: a capture goes through `Score` (`ref readonly`) rather than `Choose`, and reads a plan rather than re-resolving one — [README](../Core/Vixen.Ai/README.md) |
| **AI: environment queries** — generators, tests with three purposes, the shared scorer, the `.vxquery` asset and its list editor | ✅ | Core/Vixen.Ai · Core/Vixen.Ai.Nodes · Core/Vixen.Ai.Diagnostics · Editor/Vixen.Editor.AssetEditors · Editor/Vixen.Editor.Assets | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P8, exit criterion measured on both halves. **"The best cover point with line of sight to the target" is one authored query** — an eight-metre grid, a sight filter, a reach filter and two scoring tests. And **the same scorer really is the same object**: `UtilitySet` and `EnvironmentQuery` both implement `IScoredCandidateSet<T>`, one `IResponseCurve` *instance* scores an action and a point in the same test with `Assert.Same` on both sides, and `UtilityScoring.Combine` forwards to `CandidateScoring.Combine` — made true rather than asserted about, by moving the combining code and rewriting the streaming loop as a generic over a `ref struct` reader. `QueryEditorFactory` is registered in `StandardEditors`. 16 tests here, 6 over the world-facing half, 4 over the preview, 9 over the editor. ⚠ In doc 37 § P8: a test's purpose is Unreal's three and a filter is not a zero score; a `NaN` reading filters rather than scoring zero; test order is the file's and the runtime does not reorder it; a weight pulls a factor toward one rather than multiplying the score |
| **AI: the seams twice, the sensor taxonomy and the sample** — doc 37 finished | ✅ | Core/Vixen.Ai · Core/Vixen.Ai.Nodes · Core/Vixen.Ai.Perception | [37](plan/37-ai-behaviour-trees-utility-and-goap.md)'s P9, and **the whole of doc 37 with it — all ten phases**. **`SeamTests` is a theory over twenty-one interfaces** asking the assemblies rather than trusting review, and it found three gaps on its first run: two "second implementations" that were delegates wrapping the first, and § D13's sensor taxonomy, of which one member of four had shipped. P9 finished it — `ITargetSensor`, `IGlobalWorldSensor` and `IGlobalTargetSensor` beside `IWorldSensor`, with a `SensorSet` running the globals **once a pass, cached**, so two agents standing together cannot see different weather. The sample is one level, three agent kinds, one of each planner, all sharing one `AiSystem`, one `AgentActionRegistry`, one `PerceptionSystem`, one `SensorSet` and one navmesh. 65 tests. **⚠ P9's sample is a test rather than a `Samples/` project, stated as a deviation** — and until task #162 no `Samples/` project referenced `Vixen.Ai` at all, so the runtime half of this block had no consumer outside its own test projects. **`Samples/15-AiVillage` is that consumer**: the same three agents, but on a real `EngineLoop`, with `AiOverlaySystem` registered and a deterministic decision log as the evidence — the guard picks up the chase at 13.7 m, inside its 14 m sight radius, and drops it at 17.6 m, outside the 16 m lose-sight radius. ⚠ **Running it rather than reading it found two defects**: the tree-agent snapshot reporting `NameOf(0)` (see the debugger row), and **a zeroed `NavigationDestination` being the world origin** — it carries no "has one" flag, so an agent whose planner has not issued a destination walks to (0, 0, 0), which made the villager leave for the corner on frame one and be out of range by the time the intruder arrived. ⚠ **And `PerceptionSystem`'s `[UpdateBefore(typeof(AiSystem))]` had never been honoured by a scheduler**, because every caller in the tree called `Step` by hand in the order it wanted; the sample's suite asserts the graph. 10 tests. ⚠ P9 also deleted `AiSymptom.NeverFinishes`: the log has no notion of *progress*, so it could not tell a guard walking its beat from one stuck against a wall |
| `Symbol` lifted to `Vixen.Core` | ✅ | Core/Vixen.Core | Doc 37's P0 and its A-R8. It was in `Vixen.Animation.Moves` because move sets needed an interned name first; a blackboard key, a gameplay tag and a GOAP world key each want the same four bytes, and two interned-name types in one engine is the duplication this repository avoids everywhere else. Unshipped API, so a baseline rewrite and a using sweep rather than a type-forward |
| `Vixen.Vfx` — SoA storage, compiled graph, deterministic RNG, CPU jobs, `ParticleRenderFeature`, compute-shader emitter | ✅ | Core/Vixen.Vfx | 136 tests, zero-alloc frame. Three kernels emitted — initialize, update, reap — each reaching both backends under `glslc` and `spirv-val`. [README](../Core/Vixen.Vfx/README.md) |
| A `.vxvfx` is **an asset a scene names** | ✅ | Editor/Vixen.Editor.Assets · Core/Vixen.Rendering | `VfxImporter` compiles the node graph into `VfxEffectContent` at build time, and `VfxEmitter` is a `[Component] [DataContract]` with an `[AssetType]` reference — the Add Component entry, the type-filtered picker, the drag-and-drop target and the `.vxscene` line, with no editor code. `VfxExtractionSystem` resolves, creates, places, **steps** and retires, on `MeshExtractionSystem`'s terms; nothing else in the engine steps a `VfxSystem`, deliberately, because a renderer that simulated would simulate once per view. `Samples/13` drifts embers off eighteen lamps with no per-lamp code. ⚠ The enabling change is `VfxSystem.Origin`: every opcode writes a *world-space* position, so without an emitter transform one asset could serve exactly one entity. It is read at spawn, so a moving emitter leaves its live particles behind — see [rendering/particles](guide/rendering/particles.md) |
| The CPU expansion **reaches a screen** | ✅ | Raven/Library/Vfx · Core/Vixen.Engine.Renderer | `ParticleRenderFeature` had only ever been asserted about draw calls against the Null backend — no shader in the library took its three attributes. `ParticleSprite.rvn` is the missing half — a procedural soft disc, set 1 and set 2 and no set 0, on `ShadowCaster`'s terms — with `ParticleVertices.Schema` naming the attributes and `WorldRenderer` registering it at layout index 1. `ParticleSpriteDeviceTests` renders one and reads the colour back; `Samples/13` drifts embers off eighteen lamps. ⚠ A particle material goes through `WorldRenderer.ParticleMaterials` and not `Materials` — a sub-feature has exactly one owner, and a material assigned through the mesh path leaves an effect that expands its quads every frame and draws none of them. See [rendering/particles](guide/rendering/particles.md) |
| VFX **GPU dispatch** (upload, readback, reaping, indirect draw) | 🟡 | Core/Vixen.Rendering · Platform/Vixen.Vfx.Gpu.Tests | `VfxGpuSimulation` — storage, descriptors, the dispatch pair and both transfers — with the CPU/GPU agreement criterion asserted on a real device, validation-clean. **Reaping runs on the device**, as a third emitted kernel: every survivor claims the next slot with `atomicAdd` and copies itself there, so a reaping effect holds two full sets of the attribute buffers and the reap swaps which is live. `WriteDrawArguments` copies the counter's four bytes into a `DrawIndexedIndirect` command, so a draw reads its instance count from a buffer the host never sees. **Owed: nothing dispatches it.** `VfxGpuSimulation` is constructed only by `Platform/Vixen.Vfx.Gpu.Tests`; `VfxExtractionSystem` and `ParticleRenderFeature` step the CPU backend, and no emitter, component or option selects the GPU one. ⚠ The survivors come out in an order the two backends do not share and neither promises — a particle's randomness follows its **identifier**, not its slot, which is why `VfxReapTests` compares the two as sets keyed by identifier. [README](../Core/Vixen.Vfx/README.md) |
| VFX **GPU sort** | 🟡 | Core/Vixen.Rendering · Raven/Library/Vfx | Built and device-tested, **not blocked** — the roadmap's "blocked on Raven workgroup-shared memory" is stale: `ParticleSort.rvn` uses no shared memory at all. `VfxGpuSort` is a bitonic network, because a GPU comparison sort has to *be* a network — every invocation runs the same instructions, so a data-dependent partition is a serialisation. log²(n) passes with a barrier between each; 78 dispatches at the 4096 default, and `Passes` says so rather than making a caller count. **Owed: the same reach gap as GPU dispatch** — `VfxGpuSort` is constructed only by `Platform/Vixen.Vfx.Gpu.Tests/VfxSortTests`, and the CPU path sorts in `VfxGeometryBuilder` instead. ⚠ The keys are sortable **integers** through the monotone flip, so the device order and `Array.Sort`'s are the *same* order and the test compares permutations position for position; the buffers are the capacity rounded to a power of two and the tail is **padded**, not the network truncated. [README](../Core/Vixen.Vfx/README.md) |
| Mesh/ribbon/light renderers, custom attributes, force-field/curl-noise/collision/sub-emitter/trail updaters | 🟡 | Core/Vixen.Vfx · Core/Vixen.Rendering · Editor/Vixen.Editor.VfxGraph | **Most of this landed piecemeal and the row had not been re-read.** Built and tested: `VfxRendererKind.Mesh`/`Ribbon`/`Light`; custom attributes end to end (`ParticleBuffer` slots, `SetCustom`/`RandomCustom`/`CustomOverLife`, `VfxCustomAttributeRow` in the asset); `Attract` and `Vortex` force fields; `Turbulence` (the curl-noise field); `CollidePlane` and `CollideSphere`; `VfxSubEmitter` with burst-on-death, burst-on-birth and trail. Mesh and ribbon are drawn by `ParticleRenderFeature` — `Instanced` and `Ribbon` beside `Quads`. **Owed, and each is a reach gap rather than a missing algorithm:** `ParticleLights.Collect` has no caller, so a `Light` effect emits no light and falls through to `Quads`; `ParticleRenderFeature.SetMesh` has no caller, so an authored mesh effect draws nothing; `VfxSubEmitter` has no asset form (`VfxEffectContent` carries no sub-emitters), no node and no runtime that steps it; and the graph editor's node library offers no Mesh or Ribbon output, no custom-attribute node, no `Vortex` and no `CollideSphere`. [README](../Core/Vixen.Vfx/README.md) |
| Second view of one effect (shadow/reflection passes) | ⬜ | — | Expansion is CPU and once per view; the GPU path is the fix |
| **The gameplay kernel** — tags, `DefId`, definitions and their catalog, the attribute algebra, effects, requirements, the RNG, the module seam | ✅ | Gameplay/Vixen.Gameplay | [28](plan/28-gameplay-framework.md)'s **G0**, the milestone that document says is not optional. Every other `Gameplay/` library, both editor gameplay projects, `Live/Vixen.Live.Gameplay` and `Live/Vixen.Live.Matchmaking` are built on it. ⚠ **A tag id is a bake and a `DefId` is a hash, and the asymmetry is the whole tag design** — tags are numbered by a pre-order walk so a prefix test is two integer comparisons, which means adding a tag renumbers and durable state must store the name's `Symbol`, never an index. ⚠ **Reach ceiling for the whole G0–G8 block, and it applies to every row below:** nothing outside a test process runs these libraries. `Samples/14-Mmo` composes and content-compiles all twenty, but `MmoLibraries.Load` and `MmoRealm.Compose` are called only from its tests — a running shard (`RealmApp.Run<MmoRealm>`) leaves `MmoRealm.Libraries` null. Only the economy ledger and instance lockouts reach a runnable program, through `Mmo.Soak`. Doc 28's exit criterion is that sample and it is 🟡. 117 tests; the traps live in [README](../Gameplay/Vixen.Gameplay/README.md) |
| **The definition load path** — a build's labelled addresses out of `Vixen.Assets`, one catalog with its tag table baked | 🟡 | Gameplay/Vixen.Gameplay.Content | [28](plan/28-gameplay-framework.md) § Definitions, the last of **G0**. `DefinitionContent.LoadAsync` finds definitions by label, reads them through `AssetManager` and bakes one catalog; several labels bake one table, because two catalogs number their tags separately. ⚠ **A bad label is a reported problem and a bad address is an exception** — content being wrong versus the caller being wrong. **Owed: a caller.** Nothing outside its own 12 tests and [guide/gameplay/definitions.md](guide/gameplay/definitions.md) calls it: `Samples/14-Mmo` builds its catalog by hand from disk, and `MmoRealm.OnRealmInitialise` records the wiring as owed. [README](../Gameplay/Vixen.Gameplay.Content/README.md) |
| **Address constants** — every address a build shipped, as C# a compiler checks | ✅ | Editor/Vixen.Editor.Assets · Tools/Vixen.Cli | The last thing [28](plan/28-gameplay-framework.md)'s **G0** owed, and what doc 28 called `Vixen.Gameplay.Generators`. It never became a project: **a source generator sees the compilation and an address list is a property of the *content* build**. `vixen import --addresses` writes it and `Vixen.Sdk`'s import target adds it as a `Compile` item *inside* a `BeforeTargets=CoreCompile` target, because the SDK's glob ran long before; the `dotnet new vixen-mmo` template turns it on via `VixenAddressConstants`. ⚠ **The id is computed from the string and never emitted as a literal** — a second implementation of the hash in generated code nobody reads goes wrong silently and takes every id with it. 32 tests, two of which compile what it wrote; the remaining traps are in `AddressConstants.cs` and `AddressRunner.cs` |
| **The `.vxdef` importer** — one importer, six extensions, the YAML type tag deciding | ✅ | Editor/Vixen.Editor.Assets | [28](plan/28-gameplay-framework.md) G-Q1 settled in favour of one, and `OneImporterClaimsEveryDefinitionExtension` is what stops the next definition kind quietly arriving with an importer of its own. ⚠ **The artefact is self-describing, which no other artefact in the engine is** — a catalog reads a directory of files whose only statement of type is the tag a designer wrote, so the payload leads with the `[DataContract]` alias and resolves it through `SerializerRegistry`; renaming an alias is a content break on the same terms as renaming an address. 5 tests, plus `Samples/14-Mmo`'s content test which runs it over 981 authored files |
| Definitions through `Vixen.Assets`' ref-counted handles | ✂️ | — | **Refused by design rather than owed**, and [28](plan/28-gameplay-framework.md) § Definitions carries the correction: ref-counting the definitions themselves puts a load call on the damage path and admits a sword in somebody's bag naming a definition that has been unloaded — **a `DefId` that sometimes resolves is worse than one that never does**. `DefinitionContent.LoadAsync` reads `.vxdef` artefacts through `AssetManager` and bakes the catalog whole; `DefinitionRegistry.Reload` replaces it wholesale. What *is* ref-counted is what a definition points at — its mesh, its icon, its sound |
| **Items** — definitions, the sixteen-byte instance, rarity, durability, binding, affix rolling, the equip-time stat block | ✅ | Gameplay/Vixen.Gameplay.Items | [28](plan/28-gameplay-framework.md) **G1**. ⚠ **An instance is sixteen bytes and a test asserts the number** — everything an item *is* is recomputed from `(definition, seed)`, which is what lets the wire carry those two and have a client's tooltip, a trade window and a realm's damage calculation agree. ⚠ **The affix pool is sorted by address rather than by authoring order**, because the pool's order is part of what a seed means and tidying a YAML file must not re-roll every sword in the game. ⚠ **Rarity is a definition, not an enum or a tag**, or a bag sorted by rarity puts Common above Legendary for ever. 27 tests; [README](../Gameplay/Vixen.Gameplay.Items/README.md) |
| **The container algebra** — one container type with policies, five operations, atomic transactions | ✅ | Gameplay/Vixen.Gameplay.Inventory | [28](plan/28-gameplay-framework.md) **G1**, and the part that document says "has to be exactly right because it is where duplication bugs live". Bags, equipment, banks, mail attachments, trade windows, vendor buyback and loot windows are one type with different policies; `ContainerSet.Apply` is the only writer and snapshots every container a transaction touches before the first step. ⚠ **The randomised conservation oracle found a real duplication bug on its first run** that reading the code had not — and the oracle asserts a useful fraction both applied *and* were refused, so a change that made everything fail cannot pass it quietly. ⚠ **`Add` is all-or-nothing**, and binding is a trigger rather than a flag. 27 tests; [README](../Gameplay/Vixen.Gameplay.Inventory/README.md) |
| **Loot tables** — weighted rows with conditions, nested tables, independent rows, pity, four distributions, salvage | ✅ | Gameplay/Vixen.Gameplay.Loot | [28](plan/28-gameplay-framework.md) **G1**. A roll is seeded from `(eventId, player)` and nothing else, so a drop is recomputable a year later. ⚠ **The evaluation order is therefore a contract** — reordering any of it silently changes what every recorded event id produces. ⚠ **A row whose conditions fail is *absent*, not skipped**: the remaining weights renormalise. ⚠ **Pity is behind an `IPityStore`, because durable means a grain and `Gameplay/` may not reference `Live/`** — the shape every durable thing in these libraries takes. 23 tests; [README](../Gameplay/Vixen.Gameplay.Loot/README.md) |
| **The loot table editor** — the editable model, the flattened outline, the drop simulator | ✅ | Editor/Vixen.Editor.Gameplay.Loot | [28](plan/28-gameplay-framework.md) **G1**'s last line, whose real requirement is that the simulator runs `LootEvaluator` rather than an approximation — a second set of odds is the one a designer balances against and the one that is wrong. `TheSimulationAgreesWithTheEvaluatorEventForEvent` compares both over two thousand events exactly. ⚠ **The two only agree when both are given a pity store**, since a pity row that drops consumes an extra draw. 21 tests. **Owed: the view** — this is a model and an outline with no panel, and `Vixen.Editor.App` does not reference the project. [README](../Editor/Vixen.Editor.Gameplay.Loot/README.md) |
| **Combat** — abilities, casting, channels, the global cooldown, charges, the six-stage damage pipeline, threat and taunt | ✅ | Gameplay/Vixen.Gameplay.Combat | [28](plan/28-gameplay-framework.md) **G2**'s first half. The pipeline is `Compute → Crit → Mitigate → Absorb → Apply → React`, extensible at named stages and never replaceable (G-Q4); the six shipped rules are ordinary `IDamageRule`s and a test asserts nothing is privileged in any stage. ⚠ **`DamageEvent`'s random stream has to be a *field*, and that is a silent bug rather than a style question** — through a property getter every rule draws the same number for ever and nothing reports it; `.editorconfig` carries the scoped CA1051 exemption with that reason. ⚠ **A taunt forces the target for a duration rather than being a large threat number**, because a margin is something damage can out-run. Death is reported and not enacted. 44 tests; [README](../Gameplay/Vixen.Gameplay.Combat/README.md) |
| **Shooting** — weapons, spread, recoil, ammo and reload, falloff, penetration, the hit-claim validator, the rewind budget | ✅ | Gameplay/Vixen.Gameplay.Shooting | [28](plan/28-gameplay-framework.md) **G2**'s second half, and **G2** with it. Nothing in the hit path is new networking — prediction, the claim RPC and `ColliderRollback` are doc 16's and all built; what is new is the weapon model and the claim's validation rules. ⚠ **The cone is a pure function of `(shot, pellet, spread)`**, which is the whole basis of a checkable claim: the server recomputes where the pellets could have gone rather than believing a direction it was sent. ⚠ **The pellet-uniqueness rule is the cheapest and most valuable check here** — without it one client hits one target and reports the same pellet against forty. ⚠ Doc 16's owed rewind cost budget exists as a type, but **nothing charges it**: `RewindBudget` is referenced only by `HitClaim` and the library's own tests, and `RpcRouter` does not name it. Projectile flight is owed. 39 tests; [README](../Gameplay/Vixen.Gameplay.Shooting/README.md) |
| **Progression** — XP curves, levels, talent trees, specialisations, professions, reputation | ✅ | Gameplay/Vixen.Gameplay.Progression | [28](plan/28-gameplay-framework.md) **G3**'s first half. `ProgressionState` is an `IRequirementContext`, which is what makes doc 28's own `requires: [ Level >= 80, HasTag(Profession.Smithing), … ]` resolve through the same algebra a vendor and an ability use. ⚠ **A talent allocation is validated whole rather than click by click**, because a client-built tree is a client-chosen power level — and that forces every rule to be a property of the allocation: "A before B" is not expressible and is not missed, since it is unverifiable after a respec. ⚠ **A row gate counts the rows above it**, or the point spent *on* a row is the point that opens it. 34 tests; [README](../Gameplay/Vixen.Gameplay.Progression/README.md) |
| **Quests and dynamic events** — the kernel event bus, stages, ten objective types, contribution tiers, participant scaling, chains | ✅ | Gameplay/Vixen.Gameplay.Quests · Editor/Vixen.Editor.Gameplay.Quests | [28](plan/28-gameplay-framework.md) **G3**'s second half, and **G3** with it. ⚠ **The event bus went into the kernel**, closing a gap doc 28 named without listing: an objective counts kills, pickups, crafts and purchases — four other libraries' verbs — so a quest library referencing any of them is the horizontal edge the spine forbids. ⚠ **A subscription made during a dispatch must not see the event being dispatched**, and this is the main path: the last kill of a stage completes it, cancels its subscriptions and takes out the next stage's, all inside the handler the bus is calling. ⚠ **Completion is latched and progress is not**, and ⚠ **participant scaling is monotone by construction** — an event that got easier when a tenth player arrived is a mechanic for griefing it. Nothing here pays a reward or resolves an address. 76 tests. **Owed: the chain editor's view** — `Vixen.Editor.Gameplay.Quests` is a model and a canvas projection with no panel, unreferenced by `Vixen.Editor.App`. READMEs: [Quests](../Gameplay/Vixen.Gameplay.Quests/README.md) · [editor](../Editor/Vixen.Editor.Gameplay.Quests/README.md) |
| **Social and chat** — parties, squads and teams as one group with a policy; guilds with a tag-query permission matrix; friends, blocks, presence; channels, routing and moderation | ✅ | Gameplay/Vixen.Gameplay.Social · Gameplay/Vixen.Gameplay.Chat | [28](plan/28-gameplay-framework.md) **G4**. ⚠ **`PlayerId` went into the kernel and this is what forced it**: the spine forbids `Chat → Social` — rightly, since a game with no parties still has chat — but both name a player, and a type both need with no edge between them can only live below both. ⚠ **Chat resolves no audiences**: a channel declares its scope and an `IChatAudience` the game supplies answers it. ⚠ **A guild always has one member at rank zero, and the top rank carries every permission whatever the charter said**, or a charter that forgot `Guild.Permission.Invite` bricks every guild founded on it. ⚠ **Stored rosters and graphs come back in through unchecked `Seat`/`Assign` doors** — the authority's, never a player's. 81 tests; READMEs: [Social](../Gameplay/Vixen.Gameplay.Social/README.md) · [Chat](../Gameplay/Vixen.Gameplay.Chat/README.md) |
| **Economy** — the ledger seam, currencies, vendors, the trade confirm-lock, mail, the auction house, the price model | ✅ | Gameplay/Vixen.Gameplay.Economy | [28](plan/28-gameplay-framework.md) **G5**, and one of the two doc-28 libraries that reach a runnable program: `Mmo.Soak` drives it through `Live/Vixen.Live.Gameplay`'s `LedgerBridge`. ⚠ **The confirm-lock needs a revision and doc 28 does not say so** — "any change re-opens both confirmations" loses the race where a change and a confirmation cross in flight, so a confirmation quotes the revision it saw. ⚠ **The library never holds or moves anything**: a trade produces one intent and the realm applies it. ⚠ **Everything is escrowed when it is offered rather than when it is taken**, or a sender attaches a sword, posts it, sells it, and the recipient claims a second one. ⚠ **A player may not go negative and a world account may**, which is what makes a world account a source or a sink. 70 tests; [README](../Gameplay/Vixen.Gameplay.Economy/README.md) |
| **Instances, PvP and matchmaking** — difficulty tiers, encounters, lockouts; four composable objective types; tickets, pools and two rating models | ✅ | Gameplay/Vixen.Gameplay.Instances · Gameplay/Vixen.Gameplay.Pvp · Live/Vixen.Live.Matchmaking | [28](plan/28-gameplay-framework.md) **G6**. ⚠ **A lockout reset is an absolute boundary, not a timer from whenever somebody entered**, it is issued on the first *defeat*, and one locked-out member refuses the whole group. ⚠ **A capture meter is one signed number rather than two per-team ones**, and a contested objective freezes the *capture* and not the *scoring*. Neither library owns a scene query — `PvpMatch.Occupy` is told who is standing on a point. ⚠ **A matchmaking ticket is a *party*, never a player**, which is how "a party is never split" becomes a property of the types. ⚠ **`default(MatchPool)` admits nobody** — a positional record struct's parameter defaults belong to its constructor and not to `default`. TrueSkill's published reference figures come out at 29.396 ± 7.171. 80 tests; READMEs: [Instances](../Gameplay/Vixen.Gameplay.Instances/README.md) · [Pvp](../Gameplay/Vixen.Gameplay.Pvp/README.md) · [Matchmaking](../Live/Vixen.Live.Matchmaking/README.md) |
| **The world** — interaction and gathering, crafting, exploration, travel, vehicles with seats, leashing and spawning | ✅ | Gameplay/Vixen.Gameplay.Interaction · .Crafting · .Exploration · .Travel · .Movement · .Ai | [28](plan/28-gameplay-framework.md) **G7**, all six, and **none of them moves anything**. ⚠ **Movement's *transform* half is blocked on doc 16's owed parent-relative replication** — item 69 is still ⬜ — so what is built is the seat model. ⚠ **The AI half shrank twice**: threat and aggro went to Combat at G2 and the planners to `Core/Vixen.Ai`, leaving leashing and spawn tables. ⚠ **A shared node is *claimed* for the duration of a channel**, which is this section's duplication bug, and interruption consumes nothing. ⚠ **Crafting does not reference Interaction and `Travel` does not reference `Exploration`** — what each borrows is a *tag query*, which is what keeps the spine's edge list honest; travel's whole output is a `TransferOrder` and it does not take the fare. ⚠ **A leash needs two radii**, or a mob on the boundary flickers between chasing and resetting once a frame. 102 tests; a README per library under `Gameplay/` |
| **Owning** — plots, decoration, permission tiers, visitor access; collectibles, achievements, transmog, titles | ✅ | Gameplay/Vixen.Gameplay.Housing · .Collections | [28](plan/28-gameplay-framework.md) **G8**, both. ⚠ **Doc 28's "ten thousand houses are ten thousand rows, not ten thousand processes" is a claim about the API and is now enforceable by reading one: no method in either library takes a `now`** — no timer, no decay, no tick, and a plot's change signal is a `Revision` counter rather than an event, because a hibernating plot has nowhere to keep a subscription. ⚠ **A ban is a separate set that beats the permission ladder rather than its bottom rung**, or a house opened to the public admits the banned at the moment it matters most. ⚠ **G-Q3 was right about two things out of three**: a tag query is a standing test and cannot count, so an achievement has *both* `Requires` in the requirement algebra and `Criteria` on the kernel's event bus. ⚠ **A misspelt criterion verb cannot be caught by the build** — `Event.Kil` resolves to a real tag nothing ever posts; only a criterion with *no* verb is catchable. 72 tests; READMEs: [Housing](../Gameplay/Vixen.Gameplay.Housing/README.md) · [Collections](../Gameplay/Vixen.Gameplay.Collections/README.md) |
## 1.11 Editor

> **Phase 6's exit sentence is met.** The editor opens a project, imports assets, builds content,
> edits a scene, saves, and runs the game — entirely in `Vixen.Ui`, with no other toolkit anywhere in
> the dependency graph. The asset editors, the profiler, the debugger, the plugin host and the
> animation graph are all projects. What is left of that phase is **`PublishEditor`** with signing and
> notarisation, **golden screenshots** for editor layouts, and the **editor-shell performance bar**,
> which is unmeasured.
>
> ✅ **The viewport composes a real frame.** There are two presenters over the same three calls:
> `ScenePresenter` draws the editor's own instanced shapes lit by one key direction, and
> `FramePresenter` draws the scene through a real `GraphicsCompositor`. Both are reached; see the
> composed-viewport row below.

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Editor.Core` — `IEditorCommand`, `CommandStack` with merging/transactions/clean-marking, document model, `EditorProject`, `Selection<T>`, settings assets | ✅ | Editor/Vixen.Editor.Core | 48 tests incl. randomised do/undo/redo. [Editor.Core README](../Editor/Vixen.Editor.Core/README.md) |
| **One editing pipeline** — `EditTarget`, `EditProperty`, `EditValue`, `IEditMember`, `IEditProvider`, `SetValuesCommand` | 🟡 | Editor/Vixen.Editor.Core | [36](plan/36-an-extensible-editor.md) § P1. The inspector is on it (`InspectorField` *is* an `EditProperty`) and so is the gizmo, through `IGizmoTarget.Record`. ⚠ **`InspectorEditProvider` is still the only `IEditProvider` in the tree**: terrain, foliage, node graphs and blockout order correctly on the document's stack but have no provider, which is what stops a panel binding their members by name |
| **Built-in features register like plugins** — `PluginHost.Activate(id, name, module)`, `PluginContext.FindMenu`/`AddSubmenu` | 🟡 | Editor/Vixen.Editor.Plugin · Editor/Vixen.Editor.Blockout · Editor/Vixen.Editor.Terrain · Editor/Vixen.Editor.Water · Editor/Vixen.Editor.Scripts · Editor/Vixen.Editor.Diagnostics | [36](plan/36-an-extensible-editor.md) § P3. `Editor/Vixen.Editor.Host` is the composition root; `Vixen.Editor.App` is a library taking its modules as a constructor argument, and **Blockout, Terrain, Water and Scripts are gone from its project file** — each registering its modes, menus and panels through the door a third party uses. Terrain needed `PluginContext.OnUpdate` and `EditorDocument.Saved`, which did not exist. ⚠ **`Assets`, `AssetEditors` and the diagnostics trio remain project references of `App`**, each for a measured reason — the import pipeline is not a feature, and the diagnostics *report* aggregates the project, the scene and the log ring as well as the profiler. Owed: moving `AssetEditorRegistry` somewhere both ends can see, which is what the last dereference waits on |
| **The contribution registry** — `EditorRegistry`, `NewAssetKind`, `CustomInspector`, `SceneTool`, `PluginContext.Owns`/`With` | ✅ | Editor/Vixen.Editor.Core · Editor/Vixen.Editor.Plugin · Editor/Vixen.Editor.App | [36](plan/36-an-extensible-editor.md) § P2 and § D3. `OutOfTreePluginTests` compiles a plugin **at run time**, drops it in a folder, and an ordinary start-up loads it, its inspector element, its drawer and its tool. D3's attributes are built and reached: `DeclaredContributions` is an `IContributionScanner` registered by `EditorApplication`, so `[EditorMenu]`, `[CustomInspector]`, `[CustomDrawer]`, `[EditorTool]`, asset kinds, overlays, gizmos and icons mean the same thing in a packaged plugin and in a project's `Editor/` scripts. ⚠ **The scan is bounded and ADR-002 permits it only because of that** — one assembly just loaded or just compiled, never the installed set |
| `Vixen.Editor.Ui` — shell, docking, command registry, menus/toolbars/context/palette as views, theming, notifications, background tasks, localisation | ✅ | Editor/Vixen.Editor.Ui | [Editor.Ui README](../Editor/Vixen.Editor.Ui/README.md) |
| Editor panels written in VXML rather than in C# | 🟡 | Editor/Vixen.Editor.Ui · Editor/Vixen.Editor.App · Editor/Vixen.Editor.AssetEditors · Editor/Vixen.Editor.Profiler · Editor/Vixen.Editor.Terrain | [36](plan/36-an-extensible-editor.md) § P4 ✅ — the path is walked end to end: `binding-path`, `<PropertyField>`, `MarkupInspector.Of<T>` over a `HotReloadHost`, and eleven `.vxml` files, every one of them mounted (task centre, undo history, three profiler panels, the frame and look inspectors, two import views, the prefab view, the terrain brush inspector). 🟡 because most of the editor is still hand-written C#; the ratio is what is owed, not the mechanism. ⚠ **The editor had never created a `HotReloadHost`** — the whole reload mechanism was built, tested and unreferenced until P4 |
| Keybinding editor UI; notification panel; `Strings.Resource` generation | 🟡 | Editor/Vixen.Editor.Ui · Editor/Vixen.Editor.App | Two of three built. `KeyBindingsView` and `MessageLogView` are the **only two panels the shell registers itself** — everything else is a view over something the application owns — so any host of the shell gets both; `EditorSettingsPanels` adds keymap import/export. Owed: `Strings.Resource` generation, so an id and its source text stop being written twice |
| `Vixen.Editor.Inspector` — generated drawers, attribute set, multi-object editing, `ref` accessors | ✅ | Editor/Vixen.Editor.Inspector | [Inspector README](../Editor/Vixen.Editor.Inspector/README.md) |
| Nested-object drawer / nested struct editing | ✅ | Core/Vixen.Ui.Controls.Advanced | ⚠ Set the leaf, then write each *owner* into its own owner innermost-first; `PropertyRow` carries a path where it used to carry a member |
| Multi-edit of a curve | ⬜ | — | `CurveDrawer` answers for one `AnimationCurve`. Merging twenty curves has no answer that is not a guess |
| **Asset fields — picker, type filter, drag-and-drop from the browser** | ✅ | Editor/Vixen.Editor.Inspector · Editor/Vixen.Editor.App | The drawer answers for `AssetReference` as well as `AssetId` — the type a scene actually stores. `Vixen.Core`'s `[AssetType]` is how a *runtime* component says what a member takes, since `[AssetPicker]` is the editor's and a component carrying it would invert the layering. ⚠ **A drag is not a click**: `FollowSelection` is suspended while a pointer is down in the browser, or pressing a row hands the inspector away and destroys the field the drag was aimed at ([20](plan/20-editor-parity.md)). Still a list rather than a thumbnail grid |
| `Vixen.Editor.SceneView` — viewport, gizmos, picking, camera nav, grid, outline, debug view modes, `SceneDocument`, `.vxscene` | ✅ | Editor/Vixen.Editor.SceneView | [SceneView README](../Editor/Vixen.Editor.SceneView/README.md) |
| **The composed viewport** — `FramePresenter`, `EditorWorldRenderer`, `EditorEffects`, per-pane `RenderView`, the View Mode menu | ✅ | Editor/Vixen.Editor.App · Editor/Vixen.Editor.Host | The editor compiles its own Raven variants, extracts the scene per frame and draws it through a real `GraphicsCompositor`; `EditorFrames` is the calling of three tiers that were each built and never constructed. **Every** pane of a quad layout composes, each with its own `RenderView` and lending the frame its own colour and depth targets (`ComposedPaneTests`; `ComposedPaneCaptureTests` writes the four panes out as a picture under `VIXEN_PANE_CAPTURE`). A mode is a compositor tree (`ViewModes`), switched per pane from View ▸ View Mode; `ViewShading` is the tool renderer's near answer for a pane that is not composed. ⚠ **The frame is built into the *window's* graph, not one of its own**, or the interface's pass has nothing to declare a read against; the tools are a node appended to the built tree, and it is the depth link that makes them a viewport rather than a decal. ⚠ **A composed pane does not draw the block-out shapes, terrain, vegetation or water** — those reach it through extraction, and the tool presenter's device-geometry preview of them would composite one surface twice. `SelectionCage` and `TerrainCursor` reach both presenters |
| Undoable entity **create / delete / rename**, handle surviving a delete-and-undo | ✅ | Editor/Vixen.Editor.SceneView · Core/Vixen.Ecs | Five things come back: the handle (`World.TryRecreate`), the components (a scratch world — the only thing that can hold an arbitrary unconstrained struct is a chunk), the name, the stable id, and its place among its siblings. A delete takes the whole subtree. ⚠ **A handle is only recreatable while nothing else has taken it**, so `SubtreeSnapshot` checks the whole subtree before changing anything |
| Undoable **reparenting** + hierarchy drag-and-drop | ✅ | Editor/Vixen.Editor.SceneView · Editor/Vixen.Editor.App | `ReparentCommand`, and the outliner's drop wired to it through `TreeView.Moved`. ⚠ **The position is recorded as a neighbour, not an index** — an index is invalidated by every insertion in front of it and meaningless once a sibling is deleted. World position is kept, so dragging a crate onto a shelf does not teleport it. ⛔ Owed: a root has no neighbour and comes back in creation order; making that exact is a change to the scene format ([App README](../Editor/Vixen.Editor.App/README.md) § Known gaps) |
| Clicking in the viewport to select | ✅ | Editor/Vixen.Editor.SceneView · Editor/Vixen.Editor.App | `EditorApplication.Configure` sets `pane.Picker` on every pane, and `SceneViewport.EndSelect` routes a band too small to be a band to `Pick` — a click is the picker's question, not a one-pixel marquee. Rubber-band selection is `IScenePicker.Within`. ⚠ **The GPU id readback is still not driven**: `PickingRenderer` wants a `RenderStage` in the composed frame, which is the one place picking would beat a ray test — geometry a shader moved |
| `ISceneWriter` / `SceneFileWriter` / scene save | ✅ | Editor/Vixen.Editor.SceneView | |
| Play-in-editor, **in-process** (`WorldSnapshot` + `PlayModeController`, leak detection) | ✅ | Editor/Vixen.Editor.SceneView | Restore clears first; the selection is translated through the handle table. ⚠ A capture-and-restore rather than a system graph, so there is no `PhysicsScene` in the editor |
| Play-in-editor, **out-of-process** (`PlayerSessions`) | ✅ | Editor/Vixen.Editor.SceneView | Ports assigned by the set; a hung player is killed. **Supersedes the roadmap's "genuinely blocked" note in Phase 9** |
| `Vixen.Editor.App` / `.Host` — platform, window**s**, device, frame loop, `--frames N` | ✅ | Editor/Vixen.Editor.Host · Editor/Vixen.Editor.App | The loop, the swapchains, the windows and `--frames N` are `Vixen.Editor.Host`'s since the executable split off; `Vixen.Editor.App` is the library it composes. One `EditorPane` per window: its own swapchain, its own `UiRenderer`, its own extent and DPI. ⚠ **Two windows may not share a `UiRenderer`** — it rings its buffers per `Upload`, so a shared one draws over geometry still in flight. ⚠ **A pane exists without a device**, which is what makes `--frames N` mean something on a machine with no Vulkan. `Vixen.Editor.App.Tests` covers the panels; `Vixen.Editor.Host` has no test project, so `EditorHost.Record`'s order is reproduced in `ComposedPaneTests` rather than called |
| Panels torn out into OS windows from the editor | ✅ | Editor/Vixen.Editor.App · Editor/Vixen.Editor.Ui | Drag a tab off the window, or **View ▸ Panels ▸ Float Panel**. Proved by `--run view.float-panel`, which opens the second window, gives it a swapchain and presents to it validation-clean |
| **Project browser** (`AssetTree` + `ProjectBrowser`) — the asset database as a searchable tree over the real `Assets/` | ✅ | Editor/Vixen.Editor.Core · Editor/Vixen.Editor.App | Watched. `EditorApplication.FollowDisk` drains an `IFileWatcher` on the frame and rescans; the debounce, the atomic-save fold and the self-write suppression are `FileChangeCoalescer`'s. `Ctrl+R` still forces one |
| **An edit made outside the editor reaches the document open on it** (`ExternalEdits`) | ✅ | Editor/Vixen.Editor.Core · Editor/Vixen.Editor.App | The last few metres of the same watcher: everything else on it read the drained list for a *count*. A `.vxcompositor` or a `.rvn` saved beside the running editor re-reads the document, and `IFileWatcher.Suppress` — built with the coalescer and until then called by nothing — keeps the editor's own saves from round-tripping. ⚠ **A document with unsaved edits is not reloaded over**: it goes `IsStale` and says so ([Editor.Core README](../Editor/Vixen.Editor.Core/README.md)). Implemented by `StandardFrameDocument` and `CodeDocument`; every other document answers `CanReload` false, honestly. Ctrl+S keeps this copy, **File ▸ Revert to Saved** takes the file's. ⛔ Owed: the offer as a banner rather than a toast, and re-pointing a document whose asset is renamed on disk |
| **Import Assets / Build Content from the editor** (`ContentPipeline` on the background task manager) | ✅ | Editor/Vixen.Editor.App | The same call the CLI makes, so the two cannot produce different output for one project |
| Redraw-on-change (it redraws every frame today) | ⬜ | — | Every animation, toast expiry and task progress would have to say so, and one that forgets freezes a progress bar |
| Plugin loading (`Vixen.Editor.Plugin`, `AssemblyLoadContext`) | ✅ | Editor/Vixen.Editor.Plugin · Editor/Vixen.Editor.App | Collectible per plugin, so `Reload Plugins` picks up a rebuild without closing the project. The reason `Vixen.Editor.App` is not NativeAOT. ⚠ **Importers and build steps are the two extension points still unreachable** — `ContentPipeline` builds its registry per run |
| **Project `Editor/` scripts** (`EditorScripts`, `ScriptModule`) | ✅ | Editor/Vixen.Editor.Scripts | [36](plan/36-an-extensible-editor.md) § P5. Roslyn over every `Editor/` folder in a project into one editor-only assembly, loaded through `PluginHost` and rebuilt on save. `EditorScriptWorkflowTests` writes a `.cs` into `Assets/Editor/` in a running editor and finds the command it declared — then writes a broken one and finds the editor still running. ⚠ **The one place that enumerates an assembly's types, and ADR-002 permits it because it is bounded**: an assembly compiled seconds ago, in a folder being watched, in a process with no publish. ⚠ **A hand-written registration beats an attribute in the same folder**, matching the packaged-plugin order |
| "Open project…" file dialog | ✅ | Editor/Vixen.Editor.App | `EditorProjects.PickProjectDirectory` over `platform.Dialogs` — the OS's own picker on all three desktops — behind `file.open-project`, in the menu and the palette, on Ctrl+Shift+O |
| Asset editors: texture, model, material, prefab, shader, UI, addressable groups, compositor | ✅ | Editor/Vixen.Editor.AssetEditors | Texture, model, material, sprite sheet, addressable groups, compositor, animation clip, animation graph, sequencer, audio mixer, input actions and font each have a document and a view; the shader one is `Shading/` over the graph; the UI one is `Code/MarkupDocument`, which lexes, parses and binds a `.vxml` and previews the bound tree. ⚠ **What is owed is not an editor but the *liveness* of two previews**: a `.vxml` becomes a C# partial class, so a running preview means compiling and loading the generated type, and the pane draws static structure and says so. VCSS is genuinely live, through `StyleEngine.Replace` |
| **Sprite editor** (slice a texture into a sheet) | ✅ | Editor/Vixen.Editor.AssetEditors · Editor/Vixen.Editor.Assets | `SpriteSheetView`, a second **tab** over `TextureImportDocument` rather than a second document — a slice is rects written into the texture's own import settings, so it shares that undo stack. Grid by size, grid by count, and automatic (connected components over the alpha). `SpriteSlicer` is a pure function of pixels and options, checked against images built in a test. The importer produces one sub-asset per sprite keyed by name, so a re-slice keeps references |
| Scene and prefab editors (as asset editors) | ✅ | Editor/Vixen.Editor.AssetEditors | Authoring, save and both play-mode topologies, plus the **Compiled** tab — `CompiledSceneView` shows the archetype blocks, their entity counts, their column bytes and every complaint `SceneCompiler` makes. ⚠ **It compiles the *open document* rather than reading the last import's artefact**, so it cannot show a stale artefact and cannot be wrong about an unsaved edit — the opposite trade from the shader graph's *show generated code*. A prefab is compiled as a prefab, so its one-root rule is checked here as a build checks it |
| `Vixen.Editor.Profiler` — CPU flame chart, GPU timeline, memory view, per-scene statistics | ✅ | Editor/Vixen.Editor.Profiler | [20](plan/20-editor-parity.md)'s B4. Over the sample rings the engine already keeps and the timestamp queries the RHI already has |
| `Vixen.Editor.Debugger` — frame debugger, remote inspector, device manager | ✅ | Editor/Vixen.Editor.Debugger | The other half of B4: a captured command stream, an attach to a running build, and whatever can be deployed to |
| `Vixen.Editor.AssetEditors` — the asset editors | ✅ | Editor/Vixen.Editor.AssetEditors | 84 files. [AssetEditors README](../Editor/Vixen.Editor.AssetEditors/README.md) |
| `Vixen.Editor.Testing` | ✅ | Editor/Vixen.Editor.Testing | The editor's own harness, above `Vixen.Ui.Testing` |
| **Editor modes** — `IEditorMode`, `EditorModes`, the mode bar, `SelectMode`, `PluginContext.AddMode` | ✅ | Editor/Vixen.Editor.Ui · Editor/Vixen.Editor.SceneView | [20](plan/20-editor-parity.md)'s A1 and [24](plan/24-blockout-tools.md)'s B2. A mode is "what the viewport's input means right now": an id, a title, an icon, a context, an optional toolbar and panel, and first refusal on the pane's input through `IViewportInput` — the pane's own interface, because `Vixen.Editor.SceneView` does not reference the shell. ⚠ **The key claim needed no new mechanism**: `EditorCommand.Context` and `KeyMap`'s per-context chord table already let two panels share Delete, so `2` binds under the mode's context while `scene.bookmark-go-2` keeps it globally. ⚠ **First refusal is over what a press *starts*** — a mode cannot take the release of a drag it did not begin — but keys are offered mid-drag, because typed numeric entry only means anything while one is in flight |
| **Sub-object picking** — `SubObjectPicker`, `MeshElements`, `ISubObjectPicker`, `SceneViewport.PickSubObject` | ✅ | Editor/Vixen.Editor.SceneView | [24](plan/24-blockout-tools.md)'s B4: the ray test with a different payload rather than a new subsystem. A face is answered by a ray; a vertex and an edge by projecting positions, because neither has area. Innermost wins. ⚠ **A drawing vertex is not a vertex**: `MeshData` splits a corner per normal and per UV, so a cube's eight corners are twenty-four entries and its twelve edges are not in it at all — `MeshElements` welds by tolerance, not equality, because a sphere's seam is `cos 0` against `cos 2π`. Two limits are asserted rather than approximated: a face is a triangle until the kernel has n-gons, and nothing is occluded |
| **Snapping as one service** — `SnapContext` (element / base / modifiers), `ISceneProbe.TrySnap`, eleven commands and a Snap dropdown | ✅ | Editor/Vixen.Editor.SceneView · Editor/Vixen.Editor.App | [24](plan/24-blockout-tools.md)'s D4 and B5: an element set adding **edge** and **edge centre**, a **base** so `SnapBase.Pointer` puts the corner you grabbed on the target rather than the object's origin, three modifiers, and one context per editor shared by every gizmo and every `ScenePlacement`. The four old booleans are views over the element set rather than second state. ⚠ **`SnapToVertex`/`SnapToSurface` were honoured, tested and unreachable** because nothing turned them on — grep for who *sets* a flag ([SceneView README](../Editor/Vixen.Editor.SceneView/README.md) § Snapping) |
| **`Vixen.Geometry`** — `EditMesh`: faces over shared positions, an edge table that reports rather than refuses, face groups, corner layers | ✅ | Core/Vixen.Geometry | [24](plan/24-blockout-tools.md)'s D2 and P1–P7, all built: `MeshTopology`, `MeshSelection`, `MeshOperations` (fourteen verbs, ear clipping), `MeshShapes` (twelve parametric shapes incl. stairs, ramp, arch, pipe, door frame), `MeshSurfaces` (UV projection, smoothing groups), `MeshBoolean`, `MeshCollision`, `Stitch`. Under `Core/` referencing only the maths; the copies into `MeshData` and into the scene file live in the editor. ⚠ **Deliberately not a half-edge** — blockout geometry is non-manifold constantly, and a kernel that refuses that refuses the ordinary case; `MeshReport` says where instead. ⚠ **The boolean's classification is exact and that is the design**: a face's plane is three corners of the *original* operand, never recomputed. Every other trap — the two graphs, the relative area epsilon, faces renumbering, partial subdivision splitting neighbours, smoothing carried per verb, a shape sized in geometry rather than transform, `Stitch` after every cut — is in the [Vixen.Geometry README](../Core/Vixen.Geometry/README.md) |
| **An entity carries an editable mesh** — `SceneDocument.MeshOf`, a `mesh:` key in `.vxscene`, `EditMeshCommand` | ✅ | Editor/Vixen.Editor.SceneView · Editor/Vixen.Editor.Core | P1's exit: a cube in a scene is an `EditMesh`, it saves/reloads/re-saves to identical bytes, and moving one vertex is undoable. ⚠ **The record is flat lists with corner *counts* rather than start offsets**, so a bad merge is a short mesh rather than one whose last face reads off the end. ⚠ **Undo is two granularities in one type**: a position change merges across a drag, a topology change records the whole mesh because a boolean has no inverse. ⚠ **`parameters:` is written *instead* of the mesh** for a parametric shape — the geometry is a function of the numbers, so a file carrying both would carry two answers to one question. Materials per face group, smoothing groups and UVs joined the record with P5 |
| **Work plane** — `WorkPlane`, `SceneGrid` as a view of it, set-to-face / to-selection / to-world, offset along the normal, `]` and `[` | ✅ | Editor/Vixen.Editor.SceneView · Editor/Vixen.Editor.App | [24](plan/24-blockout-tools.md)'s D5. The floor grid is a plane you can put on a wall and build in, and every pane draws the editor's one plane. ⚠ **The adaptive 1-2-5 spacing stays and a chosen step overrides it**: a grid that only adapted could never be the four metres a level is blocked out at, and one that only obeyed would be a grey haze from two hundred metres up. `SnapContext.GridStep` reads the plane on demand, so the second number that could disagree does not exist |
| **Numeric entry during a drag** — `NumericEntry`, `TransformGizmo.Typed` | ✅ | Editor/Vixen.Editor.SceneView | Blender's `G X 5 ⏎`, which neither reference editor has. Every frame of a drag was already the pose at the grab plus a magnitude, so typing substitutes the magnitude. ⚠ **A typed number beats a snap**, being the most specific thing anybody has said about where the object lands. Axis letters override the grabbed handle, `Tab` moves between components, `-` toggles the sign, and backspacing the last character out puts the drag back on the pointer |
| **Dimensions during a drag, the tape measure, and scale references** | ✅ | Editor/Vixen.Editor.SceneView · Editor/Vixen.Editor.App | The readout is above the middle of the pane rather than in a corner, and shows the typed text, the drag's magnitude, or a measurement. `SceneMeasure` is two points a distance and three an angle, snapped through the same `SnapContext` a drag consults. ⚠ **`ReferenceVolumes` is drawn and not shipped** — lines in the pane rather than entities, so there is nothing to select, save, or leave in a level by accident |
| **`Vixen.Editor.Blockout`** — the blockout mode, its element modes, its selection verbs and its geometry verbs | ✅ | Editor/Vixen.Editor.Blockout | [24](plan/24-blockout-tools.md)'s P0–P7: `BlockoutSelection` (loop, ring, grow, shrink, group, coplanar, linked, all, none, invert), `BlockoutGeometry` (fourteen verbs, one undo entry each), `BlockoutCreate`, `BlockoutCubeGrid`, `ShapeDrag`, `BlockoutSurfaces`, `BlockoutBoolean` and `BlockoutHandoff`. The mode implements `IViewportInput` as well as `IEditorMode`, so it takes hover, the `Ctrl`+drag extrude and the shape tool's two-stage drag, and leaves the rubber-band to the pane. ⚠ **Demotion of a parametric shape happens at the first edit, not on entering a mode**, so pressing `4` on a staircase costs nothing and extruding a tread costs everything; the badge is `IsPlainMesh`, derived. Every other trap — the `1`–`4` context split against view bookmarks, a boolean's operands as hidden children, the OBJ bake — is in the [Blockout README](../Editor/Vixen.Editor.Blockout/README.md) |
| **`Vixen.Editor.Terrain`** — the sculpt and foliage modes, the brush, the five panels, the layer and hole commands | ✅ | Editor/Vixen.Editor.Terrain · Editor/Vixen.Editor.Terrain.Physics | Registered through `PluginContext` like a third-party plugin, with `TerrainModuleSession` binding the modes to whichever scene is open. `TerrainCursor` draws the brush on the heightfield it will land on, in both presenters; one drag is one undo entry; the panel is settings objects (`TerrainBrushInspector.vxml`) rather than dialog code. ⚠ **A sculpt stroke rebuilds colliders through `Vixen.Editor.Terrain.Physics`** — `TerrainModule` takes `ITerrainColliders` from `PluginServices` — **but the editor still runs no `PhysicsScene`**, so there is no simulation for the stroke to keep in step with. [Terrain README](../Editor/Vixen.Editor.Terrain/README.md) |
| Editor network panel | ⬜ | — | Nothing under `Editor/` references `BandwidthLedger` or `SnapshotInspector`, and everything it would show is already public on them |
| Editor UI automation harness | ✅ | Core/Vixen.Ui.Testing · Editor/Vixen.Editor.Testing | Golden **screenshots** for editor layouts not started. `ViewportOverlayImageTests` and `ComposedPaneCaptureTests` are the pixel evidence that exists |
| `PublishEditor`, signing, notarisation, `.dmg`/AppImage/MSI | 🟡 | [build/Build.Publish.cs](../build/Build.Publish.cs) | `PublishEditor` publishes `Vixen.Editor.Host` self-contained and single-file per RID into `artifacts/publish/<rid>`, cleaning first, with optional Vulkan loader staging and an opt-in five-frame smoke test (`--publish-smoke`, opt-in because a published apphost backgrounds itself under a build script). Owed is the packaging half [12](plan/12-build-ci-and-testing.md) asks for — `.app`/`.dmg`, AppImage, MSI/zip — and the `Sign`/`Notarize` that follow it; nothing in `build/` calls `codesign` |
| `Vixen.Editor.NodeGraph` — model, generated registry, compiler, port typing | ✅ | Editor/Vixen.Editor.NodeGraph | [NodeGraph README](../Editor/Vixen.Editor.NodeGraph/README.md) |
| `NodeGraphView` (pan/zoom/marquee/wires/minimap/search-to-create) | ✅ | Editor/Vixen.Editor.NodeGraph | Over `NodeCanvas`. A one-way projection, rebuilt per structural change; drags write positions in place |
| Sub-graphs; undo commands; auto-layout; drag-from-port; preview layer | ✅ | Editor/Vixen.Editor.NodeGraph | Sub-graphs are **inlined**, not called. The layer draws a colour *or* a render target |
| A shader-graph renderer that *fills* a preview thumbnail | ⬜ | — | Unblocked, and the editor now has a device and a compiled Raven tier to do it with. Compile one node's sub-expression, run it over a quad, keep the target alive across edits. `.ShaderGraph`'s, not the framework's |
| Selectable wires; in-place sticky-note editing; inlined-node → source-node map | ⬜ | — | Nothing in the tree names any of the three. The last is what lets a diagnostic inside a sub-graph name a node the author can select |
| Raven-span → node diagnostics mapping | ⬜ | — | `NodeDiagnostic` exists and carries no span; needs the emitter to record spans as it writes |
| `Vixen.Editor.ShaderGraph` — node library, `DynamicVector` typing, Raven emission | ✅ | Editor/Vixen.Editor.ShaderGraph | Unlit, Sprite and PBR masters. Property names are authored and live on the graph |
| The authoring surface — `.vxshadergraph`, canvas, show-generated-code, Create ▸ | ✅ | Editor/Vixen.Editor.AssetEditors | `Shading/`. Compiling runs the graph compiler **and** Raven's front end over what it emitted |
| Procedural nodes, custom-code node, Post + UI masters, preview thumbnails | ⬜ | — | Three masters shipped and no fourth or fifth; no noise, gradient or custom-code node in the library |
| A material that draws with a graph | ⬜ | — | The link and "Open shader graph" are live in `MaterialView`, and nothing consumes `ShaderGraphCompiler`'s Raven: turning it into the shader a `.vxmat` names is doc 08's material compiler |
| `Vixen.Editor.VfxGraph` — node library + dual-target compilation | ✅ | Editor/Vixen.Editor.VfxGraph | One method produces both the CPU graph and the compute shader |
| Operator nodes, blocks for the remaining opcodes, sub-emitters/trails, live preview | ⬜ | — | The library is blocks only; nothing in the assembly names an operator |
## 1.12 Networking

Phase 9 is the most complete phase in the repository — **all five exit criteria are met**.

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Vixen.Net` vocabulary, `ITransport`, `TransportConformance` suite | ✅ | Core/Vixen.Net | Time is a parameter, nothing delivered outside `Poll` |
| Transports: `Local` ✅, `NetworkSimulation` ✅, `Udp` ✅, `WebSocket` ✅, `Composite` ✅ | ✅ | Core/Vixen.Net.Transport.* | UDP has cookie-based connect, fragmentation, all four channels |
| Transport: **`Relay`** | ⛔ | — | No code anywhere. Blocked on a scope decision, not on work ([roadmap](plan/14-roadmap.md) § Phase 9): a relay *client* with no relay *server* is untestable. Transport **fallback** belongs with it |
| UDP adaptive congestion control, ack piggybacking, path MTU, DTLS | ⬜ | — | `UdpTransportOptions` caps unacknowledged datagrams, which bounds memory but is not a window that responds to loss |
| `Tick`, `TickManager` (drift correction), `PacketWriter`/`Reader` (never throws) | ✅ | Core/Vixen.Net | |
| `NetworkSession` — handshake, clock, players, reconnect window, host/offline modes | ✅ | Core/Vixen.Net | |
| Bandwidth budgeting and priority shedding at the session layer | ⬜ | — | `BandwidthBudget` exists one layer down, in `ReplicationServer`; the session has none. The writer's overflow flag is what it would build on |
| RPC — generated senders, six pre-dispatch checks, ownership, rate limiting, manifest hash | ✅ | Core/Vixen.Net + Generators | |
| Awaitable RPC (`CallAsync<T>` → `Task<T>`) | ✅ | Core/Vixen.Net | Doc 16 said `ValueTask<T>` and was corrected |
| Broadcasts (`IBroadcast<TSelf>`) | ✅ | Core/Vixen.Net | |
| `NetworkRules` policy (stricter-wins composition) | ✅ | Core/Vixen.Net | |
| `.vxnetrules` **asset** | ⬜ | — | Importer + serialised form + per-prefab reference; `NetworkRulesRegistry` is what it loads into. Was ⛔ on "the asset pipeline's half", which exists — nothing blocks it now. Same row as § 1.6's |
| Replication — bit packing, `[Quantize]`, capture-once/copy-many, two-stage filter, ack'd baselines, shedding | ✅ | Core/Vixen.Net | |
| Field-level delta (`DeltaCodec`) | ✅ | Core/Vixen.Net | 19.2 → 9.7 kbit/s a client on `Samples/08` |
| `SyncVar<T>` / `SyncList<T>` / `NetworkModule` | ✅ | Core/Vixen.Net.Engine | |
| System that marks dirty modules once a frame | ⬜ | — | `NetworkBehaviour.MarkChanged()` is called by hand and no system calls it; wants the engine scheduler |
| Interest management — resolver chain, rules, `InterestGrid` (a source, not a filter), hysteresis, `NetworkLOD` rate | ✅ | Core/Vixen.Net | Two corrections to doc 16 recorded. ⚠ Built and correct but **unreached** — `InterestChain` is constructed only by tests; no sample or game wires one |
| Team / room / fog-of-war rules; resolver **composition** | 🟡 | Core/Vixen.Net(.Engine) | Composition landed: `InterestChain` takes an `IInterestSource` plus ordered `IInterestRule`s, and `InterestGrid` + `SceneInterestRule` chain distance with scene. The named rules stay ⬜ deliberately — each is a game's idea |
| Motion — `SnapshotBuffer`, clamped extrapolation, `NetworkTransform`, owner smoothing | ✅ | Core/Vixen.Net(.Engine) | 88 bits against 224 in memory |
| Per-axis enable, parent-relative replication | ⬜ | — | ⚠ Blocks [doc 28](plan/28-gameplay-framework.md) § Movement's *transform* half — a rider parented to a vehicle has no way to replicate |
| Networked rigid bodies, authority as a `NetworkRules` audience | ✅ | Core/Vixen.Net.Physics | Correction via velocity, not teleport |
| Lag compensation (pose ring, rewind scope, `ClampFor`) | ✅ | Core/Vixen.Net.Physics | |
| Hit-claim message; per-bone rewind; rewind cost budget; drawing it | 🟡 | Gameplay/Vixen.Gameplay.Shooting | `HitClaim`, `HitClaimValidator` and `RewindBudget` landed with doc 28, one layer above networking. Per-bone rewind (wants animation pose history) and the debug drawing are owed |
| Networked animation (parameters reliable, state unreliable) + `NetworkBones` | ✅ | Core/Vixen.Net.Animation | |
| Per-bone quantisation by importance; pose interpolation | ⬜ | — | |
| Networked audio + `OwnershipClaim` | ✅ | Core/Vixen.Net.Audio | |
| Spawn / scenes / instances (`NetworkSpawner`, prefab id = hash of **address**) | ✅ | Core/Vixen.Net.Engine | Corrects doc 16's "asset GUID" |
| Prefab registry filled from the content catalog by label | ⬜ | — | `NetworkPrefabRegistry` is filled by hand at start-up today |
| Scene load/unload as session messages; client-requested spawns; `OnOwnerDisconnect` → `Despawn` | ⬜ | — | The local half is built — `NetworkSceneId`/`NetworkSceneMap` derive baked ids from the scene *name*, and `NetworkRulesRegistry.OnOwnerLeft` produces the actions. What is missing is a message on the wire, and a caller for the `Destroy` actions |
| Security — validation, rate limits, closed-set deserialization, handshake hashes, `Vixen.Fuzz` | ✅ | Core/Vixen.Fuzz | 20 targets, 5 oracles, ~12.1 M cases per build in ~18 s |
| Structure-aware mutation | 🟡 | Core/Vixen.Fuzz | `SyntaxDomain` mutates the parsed tree for `vxml` and `raven`, 1 case in 8 kept as byte havoc; the binary formats still get havoc only |
| `SharpFuzz` with real instrumentation | ⬜ | — | Targets are already `(ReadOnlySpan<byte>) -> outcome`; out-of-process is also what would name a stack overflow |
| Generated encoders pinned end to end in the wire corpus | ⬜ | — | The corpus has `packet` (the primitives) and the generator's source is pinned; the composition is not |
| Client-side prediction — input log, jitter buffer, rollback, tick-lead control, smoothing | ✅ | Core/Vixen.Net(.Engine) | |
| Predicted spawns; running the scheduler's fixed-step group | ⬜ | — | Needs a client-allocatable id space; scheduler must be re-entrant |
| Metrics over OpenTelemetry (`NetworkMetrics` + `Vixen.Net.Telemetry`, OTLP push) | ✅ | Core/Vixen.Net.Telemetry | Split so an offline game links no protobuf serializer |
| Traces (span per handshake); log bridge to OTLP; Grafana dashboard; client-side metrics | ⬜ | — | No `ActivitySource` anywhere in `Vixen.Net*` |
| Diagnostics — `BandwidthLedger` (type/field/RPC/connection), `SnapshotInspector` | ✅ | Core/Vixen.Net | Per-field costs a subtraction inside the delta encoder |
| RTT/jitter/loss graphs over time | ⬜ | — | Wants a ring of samples, not running totals |
| Dedicated-server variant boot path | ✅ | Tools/Vixen.App | `BuildVariant.Server` + headless platform + Null backend |
| Container image; server content profile | 🟡 | Tools/Vixen.Templates | Both `vixen-game` and `vixen-mmo` ship a real chiselled `Dockerfile` (asserted by `TemplateTests`). ⚠ **The server *content* profile does not exist** — nothing in the content build reads `VixenVariant`, so the mmo Dockerfile's "the content build writes the server profile" comment is currently false |
| **Out-of-process play mode** | 🟡 | Editor/Vixen.Editor.SceneView | `PlayerSessions` has the topology and its argument shapes, but **nothing constructs one** outside its own test — `EditorParity` still lists *Standalone Process* and *Server and Clients* as `Planned` for milestone E6. The *remote inspector* is ⬜ too |
| `ReplicationChannel` helper (ack has no transport of its own) | ⬜ | — | Every game will otherwise write the same six lines |
| `Samples/08-Multiplayer`, `09-NetworkSoak`, `10-VoiceChat` | ✅ | Samples/ | Soak: 30 min, 5 000 entities, 100 connections, 75.2 kbit/s, p99 tick 2.4 ms, 3 Gen0 |

## 1.13 Live — the online service layer (doc 27)

`Live/` is a top level: shipped and operated rather than run by a developer, and a game client may
link exactly one project in it. [Doc 27](plan/27-mmo-framework.md) § Cost sized the tier at 16
engineer-months across five milestones; **L0 through L4 have all landed**, and what is left is
editor-side (the `.vxplacement` asset). Every row below has a `Live/*/README.md` that owns its detail
and carries its ⚠ traps.

| Feature | Status | Where | Blocked by / note |
|---|---|---|---|
| `Live/` and `Gameplay/` as top levels, with the layer rule enforced | ✅ | build/Build.ArchitectureRules.cs | Nothing below may reference `Live/`; `Live → Tools` is one allow-listed pair, `Vixen.Live.Realm → Vixen.App` |
| `RealmSpec` — the one string a realm process boots from, argv or env | ✅ | Live/Vixen.Live.Abstractions | Hand-written `key=value` rather than JSON: the client links this transitively and is NativeAOT |
| Shard vocabulary — `ShardId`, `ShardKey`, `ShardState`, `ShardKind`, `ShardCapacity`, `RealmVersion`, `RealmEndpoint` | ✅ | Live/Vixen.Live.Abstractions | Endpoint is data, not configuration — M-Q1's relay seam is that property |
| `TransferTicket` + HMAC signer, five named refusals | ✅ | Live/Vixen.Live.Abstractions | ADR-020's ticket, checked at the door; the orchestrator mints one |
| `IRealmPlacement` — probe, start, stop, list, watch | ✅ | Live/Vixen.Live.Abstractions | `ListAsync` added to the ADR's four: reconciliation after an orchestrator restart needs it |
| `Placement.Process` — port pool, stdio lifecycle, Started/Ready/Stopped/Lost | ✅ | Live/Vixen.Live.Placement.Process | `IRealmProcessHost` is the seam that makes a fleet a unit test, as `Transport.Local` is for doc 16 |
| `Placement.Docker` — hand-written Engine API client, framed log stream, labelled containers | ✅ | Live/Vixen.Live.Placement.Docker | ADR-019's claim held: six calls, a `ConnectCallback`, no package. Reads logs and never writes — an orchestrated realm drains via its heartbeat's reply |
| `Placement.Kubernetes` — a `Pod` per realm, owner-referenced, `hostPort`, node external IP | ✅ | Live/Vixen.Live.Placement.Kubernetes | `KubernetesClient` 19.0.2 behind a six-method seam. ⚠ The only backend that overrules the realm about its own address: the realm's view is inside the pod's network namespace |
| Realm host — spec → session → admission → map → heartbeat, `Starting → Ready → Draining → Stopped` | ✅ | Live/Vixen.Live.Realm | `RealmApp.Run<TRealm>` rather than doc 27's `VixenApp.RunRealm`: `Vixen.App` is *below* `Live/` |
| `RealmDirectory` — ask-don't-await, drained once per update | ✅ | Live/Vixen.Live.Realm | ADR-016's rule as a type: it enforces *where the callback runs*, so Orleans plugs in behind it unchanged |
| `RealmHeartbeat` / `RealmHealth` — 2 s sample, tick p99 over a 256-tick window | ✅ | Live/Vixen.Live.Realm | |
| Map lifetime | ✅ | Live/Vixen.Live.Realm | Deliberately thin — the map is `AppConfig.StartupScene`, and this answers only "is it up", which is what separates `Starting` from `Ready` |
| Placement — hard filters, the megaserver score, `placement explain`, `.vxplacement` weights | ✅ | Live/Vixen.Live.Orchestrator | A pure function of counts, so doc 27 § Testing's three properties run 45 000 randomised fleets in under a second. ⚠ Every placement explains itself and it is not optional |
| Spawn/merge hysteresis (`MapFleet`) | ✅ | Live/Vixen.Live.Orchestrator | ⚠ Two traps the simulated traces found, both now policy fields: measure the arrival rate over the span arrivals landed in (not the nominal window), and allow one merge in flight rather than a dwell that resets per drain. Detail in the README |
| Grain contract — `IMapGrain`, `IShardGrain`, `IPlayerGrain`, `IFleetGrain`, and Orleans surrogates for the whole vocabulary | ✅ | Live/Vixen.Live.Cluster | Orleans 10.2.2, confined to `Live/` by `CheckArchitecture` and kept out of `Vixen.Live.Abstractions` — which is why the surrogates exist. ⚠ A vocabulary type missing from `Surrogates.cs` fails at the first grain call that carries it, not at compile time |
| The grains, as adapters over plain state machines | ✅ | Live/Vixen.Live.Orchestrator | `MapCoordinator`, `ShardLifecycle`, `PlayerLeaseState`. The lease's single-writer property (ADR-021) is asserted over 50 000 randomised operations |
| Silo host (`UseVixenOrchestrator`, `UseDevelopmentCluster`), self-ticking map grains | ✅ | Live/Vixen.Live.Orchestrator | Clustering is deliberately the caller's choice — ADR-016's providers tie a deployment to a target the brief keeps open |
| The realm's cluster client — heartbeat, ready, lease acquire/renew/release, roster | ✅ | Live/Vixen.Live.Realm.Cluster | A project doc 27 does not list, on `Vixen.Net.Telemetry`'s precedent: an L0 realm has no orchestrator and should not link a cluster framework. **M1 is asserted** |
| `.vxplacement` importer and inspector | ⬜ | — | `PlacementWeights.Parse` reads one at boot and no `[Importer]` claims the extension; making it an addressable asset is editor-side (doc 11). The one thing `Live/` still owes. ⚠ **Not the same omission `.cube` was, and it is worth saying why** — that was an attributed importer missing from a list, one line. There is no `.vxplacement` importer type at all, and there cannot be one in `Vixen.Editor.Assets` as things stand: `PlacementWeights` lives in `Live/Vixen.Live.Orchestrator`, and `CheckArchitecture` refuses `Editor/` → `Live/` because `Live/` is shipped and operated while `Editor/` is a developer tool. So this needs a decision first — the weights record moves down to a layer both can name, or the importer parses the format without naming the type and the schema is then in two places. Task #168 deliberately did not pick one |
| Transfer — the overlap protocol, every abort path, the reservation, the tick rebase | ✅ | Live/Vixen.Live.Transfer | Three state machines fed events: all eight aborts leave the player playing, and aborting a *committed* transfer is refused because that is the only way the duplication is expressible. 47 tests, no realm and no socket |
| The transfer oracle — four realms in one process, players walking a loop | ✅ | Live/Vixen.Live.Realm.Tests | Over a wire that misbehaves. The oracle checks after *every* step that no traveller is resident on two realms and that the world total never moves. ⚠ Two realm bugs only the lossy wire could show — an *admitted but not yet bound* session has no `PlayerId` and everything releases by id; and `RealmTransfers.Step` computed the sweep's kick-list and discarded it. #44 |
| The handoff payload, written with the replication codec | ✅ | Live/Vixen.Live.Realm | `HandoffCodec` lives in the realm, not in `Transfer`, because the codec is what needs a `World`. A payload that does not read cleanly is refused rather than half-applied |
| Accounts, characters, the double-entry ledger, idempotency, the schema | ✅ | Live/Vixen.Live.Persistence | Taken before L2 because nothing mints a ticket until a gate does. The world has accounts (`world/loot`, `world/vendor`), so conservation is total; ⚠ the lease reaches the database as a fence in the `where` clause, monotonic and raised by use. No driver package — `SqlPersistence` takes a `DbDataSource` |
| Gate — sign-in, characters, catalog, `POST /play`, the WSS service plane | ✅ | Live/Vixen.Live.Gate | `GateService` holds every decision and has no ASP.NET in it. ⚠ `PlayStatus.Starting` is an answer, not an error. Ships no credential store — `IAccountAuthority` is the seam |
| The service plane's wire shapes, source-generated | ✅ | Live/Vixen.Live.Abstractions | In the one assembly a client may see, so the gate and the client cannot hold two shapes of `PlayResponse` |
| The client half of the service plane | ✅ | Live/Vixen.Live.Client | AOT- and trim-clean, unlike the rest of `Live/` — a phone runs this. Nothing throws for a refusal, and *unreachable* is a separate answer from *refused* |
| Matchmaking | ✅ | Live/Vixen.Live.Matchmaking | Tickets, pools, `IMatchFunction`, and both an Elo and a TrueSkill-family rating model |
| **The gameplay ↔ persistence bridge** — the identity join, and doc 28's economy, lockouts, guilds and character state on doc 27's storage | ✅ | Live/Vixen.Live.Gameplay | The join docs 27 and 28 both assumed and neither built. Four bridges (`LedgerBridge`, `LockoutBridge`, `SocialBridge`, the profile codecs) plus the identity join. ⚠ **There are three player identities and the join is a widening, not a hash** — hashing a 256-bit key into 64 bits collides, and a collision writes another player's inventory; a gameplay `PlayerId` is realm-scoped and must never reach the database. ⚠ **A cold `LockoutBridge` is dangerous where a cold `LedgerBridge` is safe**: an unknown balance reads as zero and refuses, an unknown lockout reads as *not locked* — so `IsWarm` is what admission checks. ⚠ **A partial roster must never be written back as the whole truth**, so a guild is written as *operations* and `SaveGuild` is counted rather than obeyed. ⚠ **Every profile codec loads through a *seat*, never by replaying the rules** — replaying wipes talent builds, re-clamps training and re-fires reward chains on login. [Its README](../Live/Vixen.Live.Gameplay/README.md) has all of them, and the whole owed list. 200+ tests |
| **`IAccountGrain`** — the account-scoped writer | ✅ | Live/Vixen.Live.Cluster · .Orchestrator | A grain doc 27 § Grains does not have; doc 28's G8 showed it was missing. ⚠ **Keyed by the account, not the character** — a mount earned on one character is owned by all of them, and no key in doc 27's table could own it. No lease, because the single writer is the grain's own turn: unlocking is idempotent on the *address*, and the order is assigned here rather than trusted from the caller. Deliberately no way to un-earn. Traps in `AccountGrain.cs` |
| **`IGuildGrain`** — one guild's roster and ranks | ✅ | Live/Vixen.Live.Cluster · .Orchestrator | Declared now that doc 28's G4 built the feature. ⚠ **The grain decides ordering; the caller decides permission** — it re-checks only the arithmetic (you may not act on somebody at or above your own rank), which is what no local check can win. Rank 0 is single, and the last leader may only leave when they are the last member. ⚠ A record's generated equality compares an `ImmutableArray` by **reference**, so `GuildRecord` and `AccountHoldings` hand-write `Equals`. Traps in `GuildGrain.cs` |
| **`IGuildRepository`** — the durable half | ✅ | Live/Vixen.Live.Persistence | Doc 27 § Persistence names it; L3 deferred it until the grain that owns the aggregate existed. ⚠ **The fence is a *revision*, not a lease epoch**, compared in the same statement that writes. ⚠ **The roster is rows rather than a blob**, because "which guilds is this account in" is a query and grain storage cannot answer one. `GuildRow` is deliberately not the cluster's `GuildRecord` — this assembly must not link Orleans. Schema migration 2 |
| **`IInstanceGrain`** — one saved instance | ✅ | Live/Vixen.Live.Cluster · .Orchestrator | ⚠ **A lockout is fleet-wide, which is why it is a grain and not a realm's table** — doc 28: *"a lockout one shard knew about is a lockout a player evades by zoning"*. Progress belongs to the instance, not to each player; binding cannot be undone and there is deliberately no method for it; closing releases nobody; a boss reported twice is `Unchanged`. No clock in the type — every method is handed the time. Traps in `InstanceGrain.cs` |
| **`IQueueGrain`** — the matchmaking queue as a grain | ✅ | Live/Vixen.Live.Cluster · .Orchestrator | Doc 28's ticket as *"a grain-held record"*. ⚠ **Formed is not started** — a roster still needs a shard, so its tickets are held and the caller confirms or abandons, and a ticket in a formed match is not cancellable. Abandoning keeps the original enqueue time; backfill is preferred to a new match; a party is never split. The scoring stays a pure function in `Vixen.Live.Matchmaking`. Traps in `QueueGrain.cs` |
| Content diff — the additive classifier, with its reasons | ✅ | Live/Vixen.Live.Orchestrator | ⚠ Deliberately pessimistic: anything it cannot decide is not additive, because calling a non-additive change additive corrupts a running world. A removal is never additive. 3 600 randomised catalog pairs |
| Rolling upgrades — drain width, the grace, `VersionSpread` | ✅ | Live/Vixen.Live.Orchestrator | ⚠ Every step it produces is a *drain*; nothing is ever killed. Emptiest shard first. A rollback restarts the grace, or it would force against the version everybody is already on |
| Fleet view, placement explain, `vixen live` | ✅ | Tools/Vixen.Cli | `LiveRunner` + `VixenCommand.Live`: `status` (the fleet view, as a table), `drain`, `explain` and the build half of `upgrade`, each against a real cluster. ⚠ `up`, `down` and `upgrade --content` are **missing rather than stubbed** — the first two are doc 17's hosting story, and the third cannot answer honestly until the content build records a shape per address |
| The soak | ✅ | Samples/14-Mmo/Mmo.Soak | Doc 28's exit criterion as well as doc 27's. Eight shards over three maps, five hundred connections, thirty minutes, continuous transfers and a rolling upgrade mid-run. ⚠ **`MemoryEconomyLedger.Total` is the trap**: a double-entry journal's every-account total is zero by construction, so conservation means the sum over the *players*, checked every tick, counting players in transit. Four memory leaks found (13 MB grown, from 168) — all four written up in [its README](../Samples/14-Mmo/Mmo.Soak/README.md). #43 |
| `dotnet new vixen-mmo` / `vixen new mmo` — `.Contracts`, `.Shared`, `.Realm`, `.Client`, `.Content` | ✅ | Tools/Vixen.Templates | The first multi-project template. Doc 27's `.Cluster`, `.Orchestrator` and `.Gate` are left out: each needs a package that does not exist until L1 or L3 |
| `Samples/14-Mmo` — the soak, the exit criterion and the interface | ✅ | Samples/14-Mmo | Renumbered from doc 27's `13-Mmo`; thirteen is `13-ThirdPersonShooter`. **Complete**: 981 definitions and 19 scenes across six zones from a committed generator, ten projects (thirteen with tests), 142 tests, and `Mmo.Ui` — eight VXML components over signal-backed view models (#38). Every budget holds: zero conservation violations over 142 005 transfers, zero disconnections through a rolling upgrade, tick p99 178 µs, 35.5 KB a tick, 13 MB grown against 32. See § 1.14's row and the sample's READMEs |

## 1.14 Samples

Twelve numbered samples exist: `01`–`04`, `07`–`14`. `05` and `06` have no directory.

| Sample | Status | Note |
|---|---|---|
| `01-HelloTriangle` (+ `.Android`, `.iOS`) | 🟡 | Verified macOS, iOS Simulator, Android emulator. Windows/Linux and physical devices owed. ⚠ No CI leg runs it — `--vixen-frames` exists and nothing invokes it, because the Linux runner has no headless display |
| `02-HelloUi` | ✅ | Exit criteria met; no CI leg runs it; browser run is Phase 10's |
| `03-PbrShowcase` | ✅ | The standard frame's smallest complete project: seven knobs, shadows, occlusion, IBL from a baked sky — plus the terrain stack end to end, a multi-tile terrain with painted layers, a hole and a grass rule, spliced at `afterOpaque` |
| `04-EcsStressTest` | ✅ | |
| `05-PlatformerGame` | ⬜ | No directory. Phase 8's exit criterion; ⛔ on iOS by the Jolt slice |
| `06-CanvasStress` | ⬜ | No directory. P2, cut-list #4 — the editor is the application-platform proof |
| `07-AddressablesRemote` | ✅ | 144.6 KB cold → 48.6 KB update, asserted |
| `08-Multiplayer`, `09-NetworkSoak`, `10-VoiceChat` | ✅ | |
| `11-VideoPlayback` | ✅ | The half of video only a running frame exercises: three planes reaching the GPU at their own sizes, in order |
| `12-VirtualGeometry` | ✅ | The `Game` ⇄ `SceneRenderHost` join doc 22 phase 5 recorded as owed: a document-driven virtualized frame on a swapchain, presenting the visibility buffer as a debug view |
| `14-Mmo` | ✅ | 981 definitions and 19 scenes over six zones, ten projects (thirteen with tests), and the soak — the first thing that proves twenty gameplay libraries go together. ⚠ **Doc 28 has no creature type**, and the gap is structural: one needs `Items`, `Combat`, `Loot` and `Ai` at once and the spine allows only two, so it lives in the game — legal, because the spine is a rule about the libraries and not about their users. **#45** asks whether the engine should grow a `Vixen.Gameplay.Encounters`. ⚠ **Two things a game must do that nothing in the engine can do for it**: declare the composition (a `!Tag` resolves through `SerializerRegistry`, which a module initializer fills only when its assembly *loads*), and seed the composition's own tags into the catalog (`Event.Kill` is declared by `QuestModule` and mentioned by no quest file). ⚠ **A game's own definition type needs the generators named explicitly** — analyzers do not flow through a `ProjectReference`. [Its README](../Samples/14-Mmo/README.md) has the rest, including the `ReferenceTests`/`CoverageTests` holes and the `Mmo.Ui` VXML traps |
| `13-ThirdPersonShooter` | ✅ | A **project** rather than a sample: a `.vxproj` the editor opens, an `Assets/` the content build imports, and `VixenApp.Run<T>`. Doc 29's player, doc 22's virtualized path and doc 19's GI in one running frame. ⚠ Building it broke nine things in the engine and **seven of the nine produced a working program with a wrong answer rather than an error** |
## 1.15 Documentation and release (Phase 11)

| Item | Status |
|---|---|
| `docs/plan/` design record | ✅ — 45 numbered documents (00–44) plus [`plan/README.md`](plan/README.md) |
| `docs/manual/` — building a game and a server, diagnostic codes, log events | 🟡 (3 pages) |
| `docs/rhi-backend-mapping.md` | ✅ |
| ~~DocFX API reference~~ → generated from Roslyn symbols ([25](plan/25-documentation-generator-and-site.md), ADR-016) | ✅ ([Tools/Vixen.DocGen](../Tools/Vixen.DocGen/README.md)) — nodes are classified as what they are: controls, graph nodes, systems, shaders, components, importers, diagnostics, log events |
| vixenengine.org — the site | ✅ ([www/](../www)) — Angular 22 on xUI 2.1.0, prerendered to static files, served by nginx from a container image ([www/Dockerfile](../www/Dockerfile)), readable with JavaScript off. Landing page, per-page descriptions and canonicals, `sitemap.xml`, `robots.txt` |
| Site search (FlexSearch over API + guide) | ✅ — types, members and guide *sections*, exported at build time and read in a Web Worker; ⌘K, grouped results, kind chips, within the 300 kB / 2 MB budgets |
| Versioned docs and the release diff table | ✅ — [`docs/api-history/`](api-history/index.json) holds 0.1.0; `nuke Release` folds the baselines and writes the table into [CHANGELOG.md](../CHANGELOG.md) and `/docs/releases/<version>` |
| `vixen-mcp` + the `vixen` skill | ✅ ([www/mcp](../www/mcp/README.md), [.claude/skills/vixen](../.claude/skills/vixen/SKILL.md)) — six tools over the graph |
| Manual: getting started, per-subsystem guides, UI tutorial, Raven reference, Unity migration | 🟡 — [`docs/guide/`](guide) holds 155 pages across eleven areas. The per-type sweep is still P7, gated by `docs/DocsExempt.txt` |
| 12+ runnable samples | ✅ — 12 numbered samples (`05` and `06` are the two absent numbers), plus Android and iOS heads for `01` |
| `dotnet new` templates verified on six targets | 🟡 — four templates ship (`vixen-game`, `vixen-app`, `vixen-lib`, `vixen-mmo`); `vixen-plugin` is not written and no template is verified on a clean machine |
| `PublicAPI.Shipped.txt` freeze + API review | 🟡 — the fold is one command (`nuke Release` → `vixen-api-check --fold`, the same `Approved` path the gate uses). The reading nobody has done is now ~55 000 unshipped entries across 129 projects |
| Release automation (tag → signed builds + NuGet + GitHub Release) | 🟡 — `nuke Release` does the API fold, the graph archive and the changelog; `nuke PublishEditor` produces the bundle. Signing, notarisation, packaging and the GitHub release are not wired ([Build.Publish.cs](../build/Build.Publish.cs)) |
| 24 h editor / 24 h game soak | ⬜ — `nightly.yml` runs fuzz, properties, Postgres, Docker and Kubernetes legs, none of them a soak |
| Public triage process + compatibility policy | ⬜ |
| Third-party attribution manifest / `docs/manual/third-party.md` | ⬜ |
| Per-file SPDX enforcement in `CheckFormat` | ⬜ — every file carries the header; nothing checks it |

---

# Part 2 — Library inventory

## 2.1 Referenced and in use

Ground truth is [`Directory.Packages.props`](../Directory.Packages.props) — 50 pinned versions; the
plan of record is [`01-technology-decisions.md`](plan/01-technology-decisions.md).

| Package | Version | Status | Used by | Note |
|---|---|---|---|---|
| `Silk.NET.Core` | 2.23.0 | ✅ | `Vixen.Graphics` | |
| `Silk.NET.Vulkan` + `.Extensions.KHR` / `.EXT` | 2.23.0 | ✅ | `Vixen.Graphics.Vulkan` | Primary backend. `Vk.GetApi()` is never called (R11) |
| `Silk.NET.SDL` | 2.23.0 | ✅ | `Vixen.Platform.Desktop` | **SDL 2, not SDL 3** — doc 01 corrected. Bindings only; `libSDL2` comes from the system or `Platform.Native` |
| `Silk.NET.OpenGL` | 2.23.0 | ✅ | `Vixen.Graphics.OpenGL` | Desktop GL 4.5 core |
| `Silk.NET.OpenGLES` | 2.23.0 | ✅ | `Vixen.Graphics.OpenGL` | The GLES 3.0/3.2 and WebGL2 profiles — a second package because libGL and libGLESv2 are two libraries. ⚠ **No `Silk.NET.EGL` exists for 2.x** (it stops at 1.9.0), so `NativeEglApi` binds EGL itself |
| `Silk.NET.WebGPU` | 2.23.0 | ✅ | `Vixen.Graphics.WebGPU` | ⚠ Matches **no** wgpu-native release; the pin carries a refusal and a struct override |
| `Silk.NET.OpenXR` + `.Extensions.KHR` | 2.23.0 | ✅ | `Vixen.Xr.OpenXR` | Doc 14 lists VR/XR as a stretch; it landed early |
| `Silk.NET.OpenAL` + `.Extensions.*` | 2.23.0 | ✅ | `Vixen.Audio.Backend.OpenAL` | |
| `Silk.NET.OpenAL.Soft.Native` | 1.23.1 | ✅ | `Vixen.Audio.Backend.OpenAL` | OpenAL Soft's own version, unrelated to Silk.NET's |
| `Silk.NET.Assimp` | 2.23.0 | ✅ | `Vixen.Editor.Assets` | Import-time only, never in a runtime assembly. ⚠ On Linux the shipped native in `runtimes/` is not found by the Silk loader — a distribution `libassimp5` is installed by CI instead |
| `JoltPhysicsSharp` | 2.22.0 | ✅ | `Vixen.Physics` | No iOS slice — see §1.10 |
| `StbImageSharp` | 2.30.15 | ✅ | `Vixen.Editor.Assets` | **Replaced ImageSharp** (ADR-015 — 4.0.0 fails the build without a purchased key). Public domain; gained Radiance HDR, lost `.exr`/`.tif`/`.webp` |
| `ExCSS` | 4.3.2 | ✅ | `Vixen.Ui.Styling` | Spiked first. ⚠ Does not parse `@layer`; normalises through `var()` inconsistently; expands `transition`/`border-*` shorthands |
| `HarfBuzzSharp` (+ macOS/Linux/Win32 natives) | 14.2.1.1 | ✅ | `Vixen.Ui.Text` | Spiked first. ⚠ Exposes **no glyph outlines** — Vixen reads `glyf`/`CFF` itself. The WASM path is a version-coupled static link against Emscripten 3.1.56 |
| `K4os.Compression.LZ4` | 1.3.8 | ✅ | `Vixen.Core.Serialization` | |
| `ZstdSharp.Port` | 0.8.8 | ✅ | `Vixen.Core.Serialization` | Pure managed → works on WASM |
| `System.IO.Hashing` | 10.0.10 | ✅ | `Vixen.Core` | XxHash128 |
| `Microsoft.Extensions.Logging.Abstractions` | 10.0.10 | ✅ | `Vixen.Core.Diagnostics` | Interface only |
| `ZLogger` | 2.5.10 | ✅ | `Vixen.Core.Diagnostics` | The file sink only (ADR-008). Brings `Microsoft.Extensions.Logging` with it; publishes AOT with no trim warning |
| `NVorbis` | 0.10.5 | ✅ | `Vixen.Audio.Codecs` | |
| `Concentus` | 2.2.2 | ✅ | `Vixen.Audio.Codecs`, `Vixen.Video.Codecs` | Pinned to the **managed** path — the native libopus fallback ignored its bitrate |
| `OpenTelemetry` + OTLP/Console exporters + Runtime instrumentation | 1.17.0 | ✅ | `Vixen.Net.Telemetry` | Added beyond doc 01's register |
| `Microsoft.Orleans.Sdk` / `.Server` / `.Client` | 10.2.2 | ✅ | `Live/*` only | ADR-016/ADR-017. ⚠ Confined to `Live/` by an architecture rule: a cluster client handed to an untrusted machine makes every grain interface a public API. Its code generator is Roslyn, not an IL weaver, so ADR-002 survives; not NativeAOT-clean, and does not need to be |
| `KubernetesClient` | 19.0.2 | ✅ | `Vixen.Live.Placement.Kubernetes` | ADR-019's third placement backend |
| `YamlDotNet` | 18.1.0 | ✅ | `Vixen.Core.Yaml`, `Vixen.Editor.Core` | Low-level scanner/parser only; the emitter is Vixen's |
| `Npgsql` | 9.0.4 | 🟡 | `Vixen.Live.Persistence.Tests` **only** | The one database driver in the tree, and test-only by design: `Vixen.Live.Persistence` takes a `DbDataSource` so a game engine does not pin a driver |
| `Antlr4.Runtime` / `Antlr4.CodeGenerator` | 4.6.6 | 🟡 | `Vixen.Raven.Tests` **only** | Kept as a differential oracle after the Phase 5b migration |
| `Microsoft.CodeAnalysis.CSharp` / `.Analyzers` | 4.11.0 / 3.3.4 | ✅ | every `*.Generators` project, and `Vixen.Core.IO.Analyzers` | |
| `Nuke.Common` | 10.1.0 | ✅ | `build/_build.csproj` | |
| `System.CommandLine` | 2.0.10 | ✅ | `Vixen.Cli` and the tools | |
| `BenchmarkDotNet` | 0.15.8 | ✅ | `Benchmarks/*` | |
| `xunit.v3` + `runner.visualstudio` + `Microsoft.NET.Test.Sdk` | 3.2.2 / 3.1.5 / 18.8.1 | ✅ | every `*.Tests` | |
| `CsCheck` | 4.7.0 | ✅ | property-based suites | |

## 2.2 Planned, not yet referenced

Re-checked against `Directory.Packages.props`: all nine are still absent from it.

| Package | Planned for | Status | Blocks |
|---|---|---|---|
| `Silk.NET.SPIRV.Cross.Native` | `Vixen.Raven.Transpile` | ⬜ | HLSL/MSL/WGSL output (ADR-012) |
| `Silk.NET.Shaderc` / `.Native` | Raven's differential oracle | ⬜ | The `glslc`-vs-SPIR-V oracle is described as running; the package is not in the register |
| `Silk.NET.Direct3D.Compilers` | D3D12 backend | ✂️ | Postponed with the backend |
| `Silk.NET.Maths` | interop shim | ⬜ | Never needed — ADR-003 types carry their own conversions |
| `NSubstitute` 6.0.0, `Shouldly` 4.3.0 | test stack | ⬜ | Listed in doc 12; the props file deliberately omits unused versions |
| `Pfim` | DDS/TGA decode | ⬜ | `.dds` import |
| `SharpFuzz` | `Vixen.Fuzz` | ⬜ | Instrumented fuzzing alongside the build-time harness |
| `astcenc` (native) | `Vixen.Core.Imaging` | ⬜ | ASTC encoding — mobile texture budgets |
| `ispc_texcomp` (native) | `Vixen.Core.Imaging` | ⬜ | Full-quality BC7/BC6H |

## 2.3 Native dependencies

Ground truth is [`build/native-dependencies.json`](../build/native-dependencies.json), which holds
exactly two entries.

| Dependency | Status | Where |
|---|---|---|
| MoltenVK **1.4.1** (`ios-arm64` static, 431 `vk*` symbols; an optional simulator slice) | ✅ | `build/native-dependencies.json` + `Vixen.Platform.Native/build/MoltenVK.targets`. ⚠ Not 1.4.2 — deliberately, and the file says why |
| wgpu-native 0.19.4.1 (pinned + checksummed, five RIDs) | ✅ | `build/native-dependencies.json`. ⚠ The last release exporting the three entry points `Silk.NET.WebGPU` 2.23.0 calls; no `win-arm64` asset exists for it |
| Vulkan Loader + validation layers | ✅ | Developer/CI install; `VulkanLoader` probes Homebrew's path |
| lavapipe (Mesa software Vulkan) | ✅ | Linux CI leg |
| `libSDL2` | 🟡 | From the system; not in the acquisition manifest |
| OpenAL Soft | ✅ | Via `Silk.NET.OpenAL.Soft.Native` |
| HarfBuzz | ✅ | Via `HarfBuzzSharp.NativeAssets.*` |
| Jolt (`JoltPhysics.Native`) | 🟡 | No iOS slice |
| `astcenc`, `ispc_texcomp`, and R10's remaining three | ⬜ | The schema exists; the entries do not |

## 2.4 Rejected / reference-only (unchanged)

`Arch` (ADR-004), `ru-ace/Flexbox` + Yoga + Taffy (ADR-006), `SignalsDotnet` (ADR-007), Stride,
PurrNet — cloned into [`references/`](../references/README.md) by hand, gitignored, built by nothing.
`BepuPhysics`, `Veldrid`, `Vortice`, `SharpDX`, `Avalonia`, WPF, ImGui,
`Mono.Cecil`/`dnlib`/`Fody`/`ILRepack` (ADR-002, enforced by `CheckArchitecture`), `R3` /
`System.Reactive` (ADR-007), `SixLabors.ImageSharp` (ADR-015 — build-breaking licence gate, swapped).
Recast/Detour is reference material only; `Vixen.Navigation` re-derives and links nothing.

---

# Part 3 — Dependency tree for the unimplemented work

Read top-down: everything in a wave has **no unmet dependency on anything else in the same wave**, so
a wave is a set of tracks that can run in parallel. An arrow `A → B` means B cannot start (or cannot
be finished honestly) until A lands.

## 3.1 The four keystones

Four items unblocked disproportionately more than anything else. **All four are built.** They are
kept here because a dependency tree with the resolved edges deleted reads as though they were never
there — but only the *unresolved* edges are still listed.

```
K1  Compiled scene + prefab content (doc 08)                                 ✅ built
    SceneAsset/PrefabAsset/SceneContent in Vixen.Engine.Scenes; SceneImporter compiles
    .vxscene/.vxprefab; the authored format lives in Vixen.Editor.Core so the viewport and
    the importer read one model. Two of its seven downstream items landed with it.
    ├──→ Prefab overrides + nested prefabs — risk R7. PrefabInstances records the link for the
    │      editor session only; persisting it needs a field on SceneEntityData and an answer
    │      for "the prefab changed underneath this scene"
    ├──→ Networking: scene load/unload as session messages; scene-placed baked index
    ├──→ Samples/05-PlatformerGame — needs a shipped level
    └──⛔ Navigation: bake placements from a scene — NOT K1's after all. An importer can
           declare an asset GUID and cannot resolve one to a path

K2  Compute node in the compositor + GPU buffer upload/readback              ✅ built, SPENT
    ComputeRenderer, BufferUploadRenderer, BufferReadbackRenderer, authored as !Compute,
    !Upload and !Readback, so the edge that orders them is the graph's. All five downstream
    items are built.

K3  Per-OS platform assemblies, behind IPlatformSupplement                   ✅ built, SPENT
    Vixen.Platform.Windows/.Linux/.MacOS. All five downstream items built. The last of them,
    floating dock groups in OS windows, wanted multi-window + DPI rather than K3 — UiSurface
    + IUiWindowHost + Vixen.Platform.Ui, one document and N windows.

K4  Silk.NET.OpenGLES + an EGL context                                       ✅ built
    SilkGlesApi, and EglContext over a hand-loaded libEGL because there is no Silk.NET.EGL
    for Silk.NET 2. Nothing above IGlApi changed.
    └──→ All three downstream items are app-head work now, not binding work: an Android head
           that chooses GL (with its deny-list), a browser head for WebGL2, and Phase 10's
           browser exit criterion.
```

## 3.2 Wave 0 — startable today, fully parallel

Twenty-three tracks as first written; nineteen have landed. What is below the strike-through is
what is left.

| # | Track | Unblocks |
|---|---|---|
| ~~W0-1~~ | ~~**K1** — `SceneCompiler` + runtime scene/prefab asset~~ | Built. Two of its seven downstream items with it; four are owed and one turned out to be blocked elsewhere (§3.1) |
| ~~W0-2~~ | ~~**K2** — compute node + GPU buffer upload/readback~~ | Built and **spent** — all five downstream items are built |
| ~~W0-3~~ | ~~**K3** — `Vixen.Platform.Windows/.Linux/.MacOS`~~ | Built and **spent** — all five downstream items are built |
| ~~W0-4~~ | ~~**K4** — `Silk.NET.OpenGLES` + EGL~~ | Built. The three downstream items now want an app head that asks for a GL device, not a binding |
| ~~W0-5~~ | ~~`DescriptorBinding` sample type + comparison sampler (RHI)~~ | Built — `DescriptorSampleType`, translated and enforced by the WebGPU backend. WebGPU shadow maps now wait on **Raven** expressing a depth texture and a comparison sampler, since an effect's layout comes from its reflection |
| ~~W0-6~~ | ~~`Tools/Vixen.ApiCheck` + first `PublicAPI.Shipped.txt`~~ | Built; the gate is in CI. What is left is the Phase 11 reading of what it baselined |
| W0-7 | CI legs: ~~Windows/Linux Vulkan~~, NativeAOT publish, run-a-sample, WebGPU-on-lavapipe | The 3-OS test matrix is in `ci.yml` (`VIXEN_REQUIRE_VULKAN` on Linux, `VIXEN_REQUIRE_WEBGPU` on macOS). The WebGPU Linux leg is marked OWED in the workflow itself; the AOT-publish and run-a-sample legs do not exist |
| ~~W0-8~~ | ~~`UiDocument.Update` → `StyleUpdater` (incremental cascade)~~ | Built — the document records *what* changed rather than that something did |
| ~~W0-9~~ | ~~`UiDocument` "layout finished" callback~~ | Built — closes the resize lag in `ScrollView`, `TreeView`, `DataGrid`, `CodeEditor`, `NodeCanvas` and `Viewport` |
| ~~W0-10~~ | ~~Wire `LineWrapper` into `TextRun`/controls~~ | Built (`TextLayout`). The *editing* half — a caret that moves between lines — and `CodeEditor`'s own wrap are owed (#46, #47) |
| ~~W0-11~~ | ~~`Vixen.Core.Diagnostics` sinks + rate limiting~~ | Built — all five sinks, one shared `LogFilter`, `LogRateLimiter`. The downstream items want the editor UI and the inspector protocol; `RemoteSink` streams JSON lines into whatever `IRemoteLogTransport` that turns out to be |
| ~~W0-12~~ | ~~`Vixen.Editor.Plugin` (`AssemblyLoadContext`)~~ | Built — manifest, discovery, a collectible context per plugin, a registration scope that makes unloading undoing, an API baseline. `Vixen.Editor.App`'s AOT position is JIT, and it says why |
| ~~W0-13~~ | ~~`Tools/Vixen.Templates`~~ | Built — `vixen-game`, `vixen-app`, `vixen-lib` and `vixen-mmo`, one file tree that `dotnet new` packs and `vixen new` embeds. `vixen-plugin` is owed, not blocked. Phase 11's clean-machine criterion needs a feed, not a template |
| W0-14 | Pin a static `libjoltc.a` for `ios-arm64` | Physics on iOS → `Samples/05` on iOS. Still absent from `native-dependencies.json` |
| W0-15 | Add `astcenc` + `ispc_texcomp` to `native-dependencies.json` | ASTC/ETC2 · full BC7/BC6H · mobile texture budgets. Also proves R10's schema generalises. Still absent |
| ~~W0-16~~ | ~~ECS entity-handle **reservation**~~ | Built (`World.TryRecreate`), and spent: create/delete/rename are undoable in the scene view |
| W0-17 | Bindless material binding plan | **Built bar the table's pairing** — `BindlessTable`, descriptor indexing, Raven's `[Bindless]`/`[MaterialIndex]`, `GeometryBuffer`, `DrawIndexedIndirectCount` and compaction, all recorded in [plan/23](plan/23-bindless-materials.md) with the set-4 and push-constant traps. **Owed:** pairing beyond the one entry `WorldRenderer.Paired` writes — a material that renamed its map samples the fallback, and a surface declaring set 4 on a host that built no table gives a five-set layout with four bound |
| ~~W0-18~~ | ~~Light-probe exact predicates (robust Bowyer–Watson)~~ | Built and spent: `LightProbeVolume` interpolates tetrahedrally, and `ExactPredicates` is general — exact orientation and in-sphere live in `Vixen.Core.Mathematics` |
| ~~W0-19~~ | ~~`NodeGraphView` (pan/zoom/wires/minimap/search-to-create)~~ | Built. Shader-graph and VFX-graph authoring is a matter of nodes now, not of a canvas |
| ~~W0-20~~ | ~~Non-scene asset editors: texture · model · material · shader · UI · addressable groups · compositor~~ | All seven built, the UI one included (`MarkupDocument` lexes, parses and binds a `.vxml`). Owed is a **live** preview — a `.vxml` becomes a C# partial class, so running one is the hot-reload pipeline; the pane draws the static structure and says so |
| W0-21 | Relay **scope decision** (host one? in-box or addon?) | The `Relay` transport + transport fallback. No decision recorded and no code; `Vixen.Net.Transport.Relay` does not exist |
| W0-22 | `Vixen.Raven.Transpile` (SPIRV-Cross) | HLSL/MSL/WGSL targets + the cross-compilation test pass. The project does not exist |
| W0-23 | ~~CSS Grid~~ · `Canvas2D` · ~~pinch/rotate~~ · ~~multi-window + DPI~~ | `Canvas2D` is the only one left. Grid is built (`LayoutTree.Grid`/`.GridTracks`/`GridPlacement`); pinch and rotate are one two-pointer transform gesture in `Vixen.Ui/Gestures.cs`, driveable from `Vixen.Ui.Testing` |

## 3.3 Wave 1 — one dependency deep

| Track | Waits on | Note |
|---|---|---|
| ~~ECS world serialisation~~ | ~~W0-1~~ | Built — `WorldSerializer`/`WorldContent`, in `Vixen.Engine` because K1's binders are what a world is written through |
| ~~Scene + prefab **asset editors** over compiled content~~ | ~~W0-1~~ | Built — `CompiledSceneView`, a Compiled tab beside the hierarchy on both |
| Prefab overrides + nested prefabs | W0-1 | Risk R7. The editor-session half exists (`PrefabInstances`); the format half does not |
| Navigation: placements from a compiled scene | ~~W0-1~~ | Not K1's — an importer has no GUID-to-path resolution. See §1.10 |
| Networking: scene load/unload messages, baked scene index | W0-1 | Turns "waiting for its scene" from a state into a handshake |
| ~~VFX GPU dispatch · reaping · indirect draw~~ | ~~W0-2~~ | Built, and Phase 7's exit criterion with them. What is left is the extra mesh/ribbon/light renderers and the remaining updaters |
| GPU sort | — | ⚠ **Written and never dispatched.** `Vixen.Rendering/VfxGpuSort.cs` + `Raven/Library/Vfx/ParticleSort.rvn` exist and pass a device test, but the only caller is `Platform/Vixen.Vfx.Gpu.Tests`; `VfxRenderer` declares `VfxSortMode.ByDepth` and never runs it. Not blocked — workgroup-shared memory has shipped in `Culling.rvn` all along |
| ~~`AutoExposure` wiring~~ | ~~W0-2~~ | Built — the last of W0-2's downstream work |
| ~~Editor "open project…" dialog~~ | — | Built: `PickProjectDirectory` over `platform.Dialogs`, behind `file.open-project` |
| ~~Thread affinity · thermal state · clipboard images~~ | ~~W0-3~~ | Built, each on the platforms where the OS has an answer |
| ~~Floating dock groups in OS windows~~ | ~~W0-23~~ | Built, and multi-window + DPI with it |
| Android GLES fallback + capability deny-list | ~~W0-4~~ | `Vixen.Platform.Android` names neither GL nor EGL yet — this is head work now |
| `Samples/02` in three browsers + Playwright leg | ~~W0-4~~ | Phase 10 exit criterion. No Playwright leg in `ci.yml` or `nightly.yml` |
| WebGPU shadow maps; WebGPU Linux CI leg | ~~W0-5~~ + W0-7 | The RHI declares a sample type and the backend enforces it. The shadow map waits on Raven expressing a depth texture and a comparison sampler |
| API review pass | — | Unblocked: the gate is wired and the surface is written down. Reading ~55 000 entries and deciding which should not be `public` is the Phase 11 work |
| ASTC/ETC2 output; full-quality BC7 | W0-15 | Managed BC1/BC4/BC6H/BC7 encode is built (`BlockCompressor`); ASTC and ETC2 are enum values with no encoder. Then `ktx validate` + reference-decoder verification |
| ~~Undoable **reparenting** command + hierarchy drag-and-drop~~ | — | Built: `ReparentCommand` over `SetParentAfter`, with the outliner's drop wired through `TreeView.Moved` |
| ~~Viewport click-to-select~~ | — | Built on the CPU path — `SceneViewport.BeginSelect`/`EndSelect` → `IScenePicker.Under`. ⚠ The id-render-target path is **dead code**: `PickingRenderer` has no reference outside a doc comment and nothing assigns `SceneViewport.Picking` |
| ~~Compacted draws~~ | ~~W0-17~~ | Built — `GpuDrawArguments.Compact`, gated on no per-node contributor, which a clustered `ForwardPlus` frame no longer has |
| ~~Per-object reflection probes~~ | ~~W0-17~~ | Built — `ForwardLightingRenderFeature` picks a probe per object and writes `ProbeIndex`/`ProbeWeight` into the per-draw block; `ClusteredShading.rvn` reads them behind `UseReflectionProbe` |
| Shader-graph procedural/custom-code nodes, Post + UI masters, previews | — | Unblocked: `NodeGraphView` is in and its preview layer draws a render target. The library ships Input/Math/Texture/Vector nodes and Unlit/Sprite/PBR masters |
| VFX-graph operator nodes, remaining opcode blocks, live preview | — | Unblocked: the GPU path landed. The view half is in; the live preview is the runtime's |
| `Relay` transport + transport fallback | W0-21 | |
| Cross-compilation test pass (ESSL/HLSL/MSL/WGSL) | W0-22 | |
| ~~`Vixen.Editor.Profiler` · `.Debugger` · editor console~~ | — | Built. The console reads `RingBufferSink` live and the profiler reads the sample rings; the GPU and memory *tracks* underneath are still owed (#10) |
| Editor network panel | — | Unblocked: everything it shows is already public. No editor project references `Vixen.Net` yet |
| `.vxnetrules` asset | W0-1 (asset-pipeline shape) | |
| Prefab registry filled from the content catalog | W0-1 | |
| `Samples/05-PlatformerGame` | W0-1 + W0-14 | Phase 8 exit criterion |

## 3.4 Wave 2 — two or more deep

| Track | Waits on |
|---|---|
| Remote inspector attached to out-of-process players | The inspector **protocol** — the player-launch half is built and so is the sink (W0-11), which wants an `IRemoteLogTransport` to hand its bytes to |
| Deferred pipeline (shading-model dispatch, forward routing, decals) | Bindless materials (W0-17). ⚠ The G-buffer half is *not* the gap — `Deferred.rvn`/`GBuffer.rvn`/`GBufferPass.rvn` exist, `RenderStage` knows a G-buffer stage, and the standard frame already writes albedo/normal/specular beside `SceneHdr`. What no compositor declares is a deferred *shading* pass |
| ~~Volumetric fog~~ · contact shadows · light shafts · SSS blur · FSR1 | Volumetric fog is built (`VolumetricFogRenderer` + `VolumetricFog.rvn`). The rest need shaders that do not exist |
| Mesh shaders / meshlet culling | Deferred pipeline + capability flags |
| SMAA · MSAA resolve; ~~GTAO~~ · ~~SSR~~ | GTAO and screen-space reflections are built (`AmbientOcclusionRenderer` over `Ssao.rvn`; `ReflectionRenderer` over `ReflectionTrace.rvn`). SMAA has no shader; MSAA resolve is a feature flag only and wants the Vulkan resolve path |
| Signing · notarisation · `.dmg`/AppImage/MSI | `nuke PublishEditor` builds the bundle; the packaging, `Sign` and `Notarize` steps are not written |
| Release automation (tag → signed builds + NuGet + Release) | Signing |
| 24 h editor / 24 h game soak | A complete editor and a complete game sample |
| Perf bars measured on the IHV matrix | Real hardware + all CI legs |
| Manual + samples + templates verified clean-machine | Effectively everything |
| Video **material** (a video lit on a mesh); XR render feature + single-pass multiview | The material system / `VK_KHR_multiview` in the RHI. Both modules themselves are ✅ |

## 3.5 Independent of everything (pure additions)

No dependency in either direction; pick any up whenever there is a gap. UDP congestion control /
ack piggybacking / path MTU / DTLS; `SharpFuzz` instrumentation and structure-aware mutation;
per-axis `NetworkTransform`; team/room/fog-of-war interest rules and resolver composition; `SyncVar`
dirty-marking system; `ReplicationChannel` helper; OpenTelemetry traces and the client-side metrics
route; Raven string interpolation; blend shapes; parallel asset import; ECS read/write inference
generator; `WhenAny` in coroutines; `GpuUploadRing`; transform decomposition. (Punctual shadow
caching has since been built — `ShadowMapRenderer.StaticAtlas` and `PunctualShadowRenderer`.)

---

# Part 4 — What is owed

Every item a document explicitly marks **Owed**, **Not here yet**, **Known gap** or **Still to
come**. "Owed" means the subsystem is otherwise done and this is the named remainder — deliberately
distinct from "not started" in Part 1. **The numbers are stable identifiers** and other documents
cite them (`plan/28-gameplay-framework.md` cites item 69); do not renumber. Detail lives in the
owning `README.md` and plan doc, not here.

| # | Subsystem | Owed | Kind | Blocked by |
|---|---|---|---|---|
| 1 | `Vixen.Core.Memory` | `GpuUploadRing` | Feature | — |
| 2 | `Vixen.Core.Collections` | `RobinHoodDictionary`, `FixedBitSet<N>` | Feature | ECS component budget (for `N`) |
| 3 | `Vixen.Core.Threading` | `VIXEN_JOB_SAFETY` access declarations | Correctness | ECS declarations |
| 4 | `Vixen.Core.Threading` | The scheduler *using* thread affinity — the platform half is built (K3) | Perf | — |
| 5 | `Vixen.Core.Threading` | Job priority tier for streaming/decode | Perf | — |
| 6 | `Vixen.Core.IO` | The synchronous-IO ban (the `System.IO.Path` half is built) | Discipline | A decision about `IOdbBackend`'s synchronous contract |
| 7 | `Vixen.Core.Reflection` | Generic *declaring* types — refused today with `VXS0201`; generic collection members are handled | Feature | — |
| 8 | ~~`Vixen.Core.Diagnostics`~~ | ~~Sinks~~ | Built (W0-11) — all five, sharing one `LogFilter` | — |
| 9 | `Vixen.Core.Diagnostics` | UTF-8 record packing in the ring (~~rate limiting~~ built: `LogRateLimiter`) | Perf | — |
| 10 | `Vixen.Core.Diagnostics` | Memory attribution; Perfetto **protobuf** (the exporter emits Chrome JSON). GPU profiling is built and reached — `Vixen.Graphics/GpuProfiler.cs` ← `AppGraphics`, `EditorHost`, surfaced by `GpuOverlay` and `GpuTimelineView` | Feature | Allocator reporting surface |
| 11 | `Vixen.Core.Imaging` | ASTC/ETC2 encoders (enum values only today); a full-quality BC7 path. Managed BC1/BC4/BC6H/BC7 encode is built (`BlockCompressor`) | Feature | Native encoder acquisition (W0-15) |
| 12 | `Vixen.Core.Imaging` | `ktx validate` + reference-decoder verification | Correctness | — |
| ~~13~~ | ~~`Vixen.Ecs`~~ | ~~World serialisation~~ | Built — `WorldSerializer`, in `Vixen.Engine` | — |
| 14 | `Vixen.Ecs` | Read/write inference generator; `VIXEN_ECS_EVENTS` | Feature | — |
| 16 | `Vixen.Engine` | Depth-split transform hierarchy | Perf | Shared components |
| 17 | `Vixen.Engine` | Doc 13's render-mode, UI-debug and streaming overlays — the frame-stats, log, console, frame-graph and GPU ones are built and reached | Feature | Shader debug views; a reporting seam out of `Vixen.Ui` and `Vixen.Assets` |
| 18 | `Vixen.Engine` | `WhenAny` in coroutines | Feature | — |
| 18b | `Vixen.Engine` | Virtual cameras: target groups, orbit recentring; and a **shipping** `ISplineSource` — the dolly track (`TrackedDollyBody`) and `SplineAsset` are built, but the only implementation of the seam is a test fake ([26](plan/26-virtual-cameras.md)) | Feature | — |
| ~~19~~ | ~~`Vixen.Graphics`~~ | ~~`DescriptorBinding` sample type~~ | Built (`DescriptorSampleType`) | — |
| 20 | `Vixen.Graphics` | Placed resources (true aliasing) | Perf | Two backends cannot express it |
| 21 | `Vixen.Graphics.RenderGraph` | Async-compute queue scheduling | Perf | — |
| 22 | `Vixen.Graphics.Vulkan` | Swapchain acquire/present *coverage*; timeline semaphores (a capability flag only); MSAA resolve. Query pools are built | Coverage / feature | Windowed test host |
| 23 | `Vixen.Graphics.OpenGL` | `glBindImageTexture` (storage images) — `GlDevice.Replay` throws on one today | Feature | — |
| 24 | `Vixen.Graphics.WebGPU` | Timestamp queries — `WriteTimestamp` throws and the device refuses to create a query pool; Linux CI leg | Feature | — (what an *effect* cannot yet declare is Raven's, #50) |
| ~~25~~ | ~~`Vixen.Platform.Desktop`~~ | ~~Pickers, clipboard, affinity, thermal~~ | Built (K3), through `IPlatformSupplement` | — |
| 26 | `Vixen.Platform.Native` | R10's remaining five native dependencies | Infra | — |
| 27 | `Vixen.Platform.iOS` | Physical-device run; sensors, haptics, HDR layer; scene-delegate lifecycle | Verification | Provisioning profile |
| 28 | `Vixen.Platform.Android` | GLES fallback + deny-list; key translation; safe-area insets; sensors; default-runtime AOT gate | Feature | — (K4 supplies the binding and the context) |
| 29 | `Vixen.Platform.Web` | Playwright smoke test; `AudioWorklet` path; a browser transport | Coverage / feature | CI leg |
| 30 | `Vixen.Assets` / pipeline | Parallel import; persisted per-entry index; the import-budget gate | Perf | — |
| ~~31~~ | ~~Asset pipeline~~ | ~~`SceneCompiler` + scene/prefab asset~~ | Built (**K1**) | — |
| 32 | Asset pipeline | ✅ **Closed.** The `.cube` LUT importer was written, tested and unregistered, and so were the four AI importers; all five are in `BuiltInImporters.Create()` now and `EveryAttributedImporterTests` fails the build on the next one. Still open here: the server content profile | Feature | — |
| 33 | `Vixen.Sdk` | CLI shipped in the package; platform packaging; diagnostic file paths | Infra | — |
| 34 | `Vixen.Cli` | Signing/notarisation/packaging; `plugin`/`tool` templates; `doctor systems` | Infra | `Build.Release.cs` (signing) |
| ~~35~~ | ~~`Vixen.Ui.Styling`~~ | ~~Incremental cascade~~ | Built. The next-narrowest change — an inline style, a new element — still lacks a path of its own | — |
| ~~35b~~ | ~~`Vixen.Ui.Reactive`~~ | ~~A frame-pass caller for `EffectScheduler.Flush()`~~ | Built — `UiDocument.Update` drains before the pass | — |
| 36 | `Vixen.Ui.Styling` | Transform decomposition | Feature | A transform property |
| 37 | `Vixen.Ui.Text` | `TextEditor` model with IME + caret affinity | Feature | — |
| 38 | `Vixen.Ui.Text` | `CVAR`, `CFF2` variation, direct `HVAR` | Feature | — |
| 39 | `Vixen.Ui` | Rich-text runs from markup (which stretch is bold) | Feature | — |
| 40 | `Vixen.Ui` | LIS reorder pass (~~named slot projection~~ built — `Binder.BindSlot` → `ComponentEmitter` → `BuildContext.Slot`) | Feature | — |
| ~~41~~ | ~~`Vixen.Ui`~~ | ~~Pinch and rotate~~ | Built — one two-pointer transform gesture in `Gestures.cs`, driveable from `Vixen.Ui.Testing` | — |
| 42 | `Vixen.Ui` | Per-corner radii on `DrawCommand` — the command carries one `float Radius`; four live in the `BoxStyle` side-table, and consumers that read only the command fall back to `CornerRadii.Uniform` | Feature | — |
| 43 | `Vixen.Ui` | Computed-value stage for `line-height`/`letter-spacing`/`word-spacing`/`text-indent` | Correctness | — |
| 44 | `Vixen.Ui.Markup` | `bind:` update events; CLI path emitting generated C# to disk | Feature | — |
| 45 | `Vixen.Ui.HotReload` | Driven against a running window | Verification | — |
| 46 | `Vixen.Ui.Controls` | Variable-height virtualisation (~~`TextArea`'s editing half~~ built — `AcceptsNewlines`, Up/Down, line-relative Home/End, Enter inserting `\n`, Ctrl-Enter still submitting) | Feature | — |
| 47 | `Vixen.Ui.Controls.Advanced` | Undo (`CodeBuffer` states outright that it has no stack); `CodeEditor` wrap + caret blink; `AppendChild` O(n); `Canvas2D`. ~~`OkLch` gamut mapping~~ is built — `Core.Mathematics/GamutMap.cs`, reached from `UiGeometryBuilder` and the VCSS parser | Feature / perf | — |
| 48 | `Vixen.Ui.Testing` | Group opacity; a third finger; layout-box assertions | Feature | Compositor decision (opacity) |
| 49 | `Vixen.Ui.Renderer` | Reconcile per-vertex box params with `Raven/Library/Ui`'s per-uniform ones | Consistency | Raven taking over UI shader compilation |
| 50 | Raven | String interpolation; a depth texture and a comparison sampler in the type system — what WebGPU shadow maps wait on (~~workgroup-shared memory~~ built, shipping in `Culling.rvn`) | Language | — |
| 51 | Raven | `Vixen.Raven.Transpile`; cross-compilation pass | Feature | — |
| 52 | Raven | `CompileShaderLibrary` Nuke target; SPDX enforcement | Infra | — |
| 53 | Raven | Negative diagnostic fixtures (~~both numeric gates~~ built, in `Platform/Vixen.Raven.Gpu.Tests` — the one place the compiler and a driver meet) | Coverage | — |
| 54 | Raven | Stream interpolation control; per-module flat IR namespace | Feature | — |
| ~~55~~ | ~~`Vixen.Rendering`~~ | ~~Compacted draws~~ | Built — `GpuDrawArguments.Compact` | — |
| 56 | `Vixen.Rendering` | Transmission — ⚠ `Raven/Library/Shading/Transmission.rvn` exists and **nothing imports it**; no shading model reaches it. Also bindless material textures (W0-17's pairing) and blend shapes, of which there is no symbol anywhere | Feature | Pass-level scene colour |
| 57 | `Vixen.Rendering` | Light probes **on the GPU** — upload a volume and sample it in a shader. ⚠ The CPU `LightProbeVolume` is reached only by its own tests. (~~Per-object reflection probes~~ and ~~punctual shadow caching~~ are built and reached) | Feature | Binding plan (W0-17) |
| 58 | `Vixen.Rendering.PostFx` | SMAA, MSAA resolve (~~GTAO~~, ~~SSR~~, ~~volumetric fog~~, ~~AutoExposure~~, ~~DoF~~, ~~motion blur~~, ~~LUT asset~~, ~~local exposure~~, ~~lens flare~~ all built) | Feature | SMAA has no shader; MSAA resolve wants the Vulkan resolve path |
| 59 | `Vixen.Physics` | iOS slice; per-pair suppression; vehicles/ragdolls/soft bodies; double precision | Platform / feature | Static `libjoltc.a` (W0-14) |
| 60 | `Vixen.Audio` | Measured HRTF sets; per-title certification work | Content | — |
| 61 | `Vixen.Input` | An input debug panel; sensors/pen/MIDI/HID (~~action-map editor~~ built — `InputActionsView`, registered in `StandardEditors`) | Feature | Platform contracts (devices) |
| 62 | `Vixen.Navigation` | Placements from a compiled scene — today `NavMeshImporter`'s are authored in the file and the editor bakes from the live world | Feature | Not K1: a GUID-to-path resolution an importer can reach, or a component that names a path. See §1.10 |
| 63 | `Vixen.Vfx` | A **caller** for the GPU sort — ⚠ `VfxGpuSort` + `ParticleSort.rvn` are written and device-tested, and `VfxRenderer` declares `VfxSortMode.ByDepth` without ever dispatching them. Also extra renderers/updaters, a second view, screen-space collision. (~~Dispatch~~, ~~reaping~~, ~~indirect draw~~ built) | Feature | — |
| 64 | `Vixen.Net` | `Relay` transport + fallback | Feature | Scope decision (W0-21) |
| 65 | `Vixen.Net` | UDP congestion control, ack piggybacking, path MTU, DTLS | Feature | — |
| 66 | `Vixen.Net` | Session bandwidth budgeting / priority shedding | Feature | — |
| 67 | `Vixen.Net` | `.vxnetrules` asset — the registry it would fill is built and reached, the format and importer do not exist; a prefab registry filled from the catalog rather than by hand; scene messages; client-requested spawns; `OnOwnerDisconnect` → `Despawn` | Feature | **K1** (partly) |
| 68 | `Vixen.Net` | Team/room/fog-of-war rules; resolver composition | Feature | — |
| 69 | `Vixen.Net` | Per-axis / parent-relative `NetworkTransform`; per-bone quantisation; pose interpolation | Feature | — |
| 70 | `Vixen.Net` | Hit-claim message; per-bone rewind; rewind cost budget; rewind visualisation | Feature | Animation pose history |
| 71 | `Vixen.Net` | `SyncVar` dirty-marking system; `ReplicationChannel` helper; generator packaged into the NuGet | Ergonomics | Engine scheduler |
| 72 | `Vixen.Net` | Predicted spawns; scheduler fixed-step group | Feature | Re-entrant scheduler |
| 73 | `Vixen.Fuzz` | `SharpFuzz` instrumentation; structure-aware mutation for the *binary* formats; generated encoders end to end | Coverage | — |
| 74 | `Vixen.Net.Telemetry` | Traces; log bridge to OTLP; Grafana dashboard; client-side route | Observability | — |
| 75 | Networking | Editor network panel; RTT/jitter/loss graphs | Tooling | Panel host |
| 76 | Server variant | An engine-side container image (the `vixen-game` and `vixen-mmo` templates ship one); server content profile | Infra | CI / asset pipeline |
| 77 | `Vixen.Editor.Ui` | `Strings.Resource` generation (~~keybinding editor~~ `KeyBindingsView` and ~~notification panel~~ `NotificationCenter`/`MessageLogView` are built and opened from `EditorShell`) | Feature | — |
| 78 | `Vixen.Editor.Inspector` | Curve multi-edit; a *thumbnail grid* for the asset **picker**. ⚠ `ThumbnailCache`/`ThumbnailSurface` exist and are wired — but into the ProjectBrowser's `AssetGrid` only; `AssetPicker` is still a search box over a text list | Feature | — |
| 79 | `Vixen.Editor.SceneView` | **Textured** surfaces — `CompileFallback` binds one constant-colour `MetalRoughnessFeature` for every mesh. ⚠ `PickingRenderer` is dead code: nothing assigns `SceneViewport.Picking`, and ~~click-to-select~~ goes through the CPU `ScenePicker` instead (~~reparent~~, ~~drag-and-drop~~, ~~meshes~~, ~~a material~~ built) | Feature | A texture needs a descriptor set the tool pipeline has not got |
| ~~80~~ | ~~`Vixen.Editor.App`~~ | ~~File dialog; plugin-management panel~~ | Both built — `PluginManagerView`, added by `EditorSettingsPanels` | — |
| 81 | `Vixen.Editor.NodeGraph` | Selectable wires; sticky-note editing; a node in two groups; inlined-node → source-node map; Raven-span diagnostics | Feature | Emitter span recording, for the last |
| 82 | `Vixen.Editor.ShaderGraph` | Procedural + custom-code nodes; Post/UI masters (Unlit, Sprite and PBR ship); previews; diagnostic mapping; an importer that compiles the emitted Raven | Feature | Emitter span recording; doc 08's material compiler |
| 83 | `Vixen.Editor.VfxGraph` | Operator nodes; remaining opcode blocks; sub-emitters/trails; live preview | Feature | — |
| 84 | Editor | Golden screenshots; `PublishEditor` with signing and notarisation; redraw-on-change; the shell perf bar; a plugin *browser* (the manager lists what is installed; installing is still copying a folder) | Feature | — |
| 85 | Build/CI | NativeAOT leg; sample-running leg; Playwright leg; 3-OS *determinism* run (the 3-OS test matrix itself is in `ci.yml`) | Infra | — |
| 86 | Build/CI | Per-file SPDX enforcement; third-party attribution manifest | Licence obligation (ADR-015) | — |
| 87 | Samples | `05-PlatformerGame`; `06-CanvasStress`; `01` on Windows/Linux and physical devices | Coverage | **K1**, #59, CI legs |
| 89 | `Vixen.Video` | MP4; a material; frame-accurate seek; audio-track choice; subtitles; 10-bit / BT.2020; Vorbis; >2 channels | Feature | A wider pixel format, for the last two |
| 90 | `Vixen.Xr` | A render feature; single-pass multiview; hand/eye tracking; passthrough; anchors. ⚠ `XrSession` has no reference outside its own two projects — nothing in a frame reaches XR | Feature | `VK_KHR_multiview`, which appears nowhere in the Vulkan backend |
| 88 | Docs | Manual sweep; template verification; release automation; soak tests; triage + compatibility policy | Phase 11 | Everything |

## 4.1 Owed by weight

| Bucket | Count | Comment |
|---|---|---|
| ~~Blocked on **K1** (scene format)~~ | 4 | Unblocked. Two built (world serialisation; the compiled-content asset editors); one left the bucket unbuilt — the navmesh bake is blocked on GUID-to-path resolution, not on K1 |
| ~~Blocked on **K2** (compute/readback)~~ | 0 | **Spent** — all five downstream items built |
| ~~Blocked on **K3** (per-OS assemblies)~~ | 0 | **Spent** — all five downstream items built |
| ~~Blocked on **K4** (`OpenGLES` + EGL)~~ | 0 | Built. The three are app-head work now: a head that asks for a GL device on Android or in a browser |
| Blocked on a **decision**, not code | 2 | Relay scope (W0-21); D3D12 (answered: post-1.0) |
| Blocked on **hardware or an account** | 3 | iPhone provisioning; an Android device; the IHV matrix |
| Genuinely independent | ~40 | Pick up in any order (§3.5) |
| Written but **never called** | 6 | This engine's characteristic defect, so it gets a bucket: `VfxGpuSort` (#63) · `PickingRenderer` (#79) · `CubeLutImporter` (#32) · `Transmission.rvn` (#56) · `LightProbeVolume` (#57) · `XrSession` (#90). Each compiles and is tested; none is reached from a frame or a pipeline |
| Closed since the first revision of this page | ~30 | Every one is struck through in §3.2–§3.4 and the table above; the four keystones account for fourteen of them |

---

## Appendix — headline numbers

Counts marked *(measured)* were re-counted against the tree for this revision; the rest come from
the last generator or gate run and are older.

| | |
|---|---|
| `.csproj` on disk *(measured)* | 395 — `Core` 170 · `Editor` 49 · `Gameplay` 44 · `Platform` 35 · `Live` 28 · `Samples` 28 · `Tools` 28 · `Benchmarks` 7 · `Raven` 3 · `build` 1 · doc spikes 2. Counting test siblings and generators per ADR-014, and excluding the seven `.csproj` inside `Tools/Vixen.Templates/templates/` |
| Test projects *(measured)* | 172 |
| Planned projects not created *(measured)* | `Vixen.Graphics.Direct3D12` (✂️ post-1.0), `Vixen.Net.Transport.Relay` (⛔ scope decision), `Vixen.Raven.Transpile` |
| Pinned NuGet versions *(measured)* | 50, in [`Directory.Packages.props`](../Directory.Packages.props) |
| Golden image fixtures *(measured)* | 49, in `Platform/Vixen.Graphics.Golden.Tests` |
| Guide pages written *(measured)* | 155 across eleven areas ([`docs/guide/`](guide)) |
| Plan documents *(measured)* | 45, numbered 00–44 |
| Fuzz target files *(measured)* | 11 domains under `Core/Vixen.Fuzz/Targets`; ~12.1 M cases per build in ~18 s |
| Public API surface awaiting review *(measured)* | ~55 000 `PublicAPI.Unshipped.txt` entries across 129 projects; almost nothing is shipped yet |
| Documentation graph | 3 679 types · 29 354 members, as recorded for the **0.1.0 baseline** dated 2026-07-31 ([`api-history/index.json`](api-history/index.json)). The current graph is larger and has not been re-measured |
| Conformance cases green | 534 Yoga · 22 048 UAX#14/#29 · 91 707 UAX#9 · 328/413 shaping · 100 variable-font |
| Phases complete | 0, 1, 2, 3 (bar CI legs and physical devices), 4, 5b, 6 (the exit sentence; the tooling around it is not), 9 |
| Phases partial | 5 (renderer — PostFx and D3D12), 7 (the CPU/GPU VFX criterion is met; the shader-graph one still wants a preview renderer), 8 (samples), 10 (WebGPU, Video and XR landed early; deferred shading and the browser run did not) |
| Phases not started | 11 (polish and 1.0) |
| Roadmap estimate remaining | ~8–11 EM of the original ~48, concentrated in Phase 6's tooling, Phase 10's deferred shading pass, and Phase 11 |

*Generated from the documentation set and the repository as of 2026-08-18. Licensed under Apache-2.0.*
